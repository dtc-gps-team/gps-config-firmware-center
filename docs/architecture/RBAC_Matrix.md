# RBAC Matrix — GPS Config & Firmware Center

**อ้างอิงจาก:** `../planning/03_GPS_Detailed_Build_Steps.md` Phase 0 ข้อ 4 (`[A]` ทำ RBAC Matrix ให้เสร็จก่อนเขียนโค้ด Auth), `../planning/01_GPS_Build_Reference.md` Section 2, `../planning/02_GPS_Development_Plan.md` แถวที่ 5, **`docs/api/openapi.yaml` บน `main` (v0.1.0) — ใช้เป็นแหล่งอ้างอิงหลักสำหรับ Role enum, Config status enum, และรายชื่อ Endpoint ที่มีจริง (เช็คล่าสุดหลัง `git pull` main สดๆ)**
**สถานะ:** ร่างจาก paveekornk (A) — ต้องคุยเนื้อหากับ kittiphong (B) ก่อนปิด เพราะ B ต้องเอาไปใช้ทำ RBAC ฝั่ง Mobile และ Guard ฝั่ง Backend ต้อง implement ตาม Matrix นี้เป๊ะๆ
**ห้าม:** เขียน Auth Guard (`backend/src/common/guards/`) หรือ `middleware.ts` ฝั่ง Web ก่อนตารางนี้ถูก sign-off

---

## 1. คำนิยาม Role

> **✅ แก้ครั้งที่ 37 (2026-09-17, `docs/13_Role_Redesign_Proposal.md` → PR #174):** ยกเลิก Role `SW` ตัวเดียว แยกเป็น **3 Role ใหม่ตามหน้าที่จริง** (ConfigEngineer / FirmwareEngineer / QAEngineer) ตาม PDF ต้นฉบับ §13.1 ที่แยกหน้าที่นี้ไว้ชัดเจนอยู่แล้ว — ชื่อเดิม "SW" (Software Engineer) สื่อถึงตำแหน่งงาน ไม่ใช่สิ่งที่ role ทำได้จริงในระบบ ทำให้สับสนเวลาคุยกับทีมที่ไม่คุ้นชื่อ · `OT` เปลี่ยนชื่อแสดงผลจาก "Operation-Technician" เป็น "Operation Technician" (ตัดขีดกลาง) — โค้ด/`Role.code` ไม่เปลี่ยน ยังเป็น `OT` เหมือนเดิม

| Role (`Role.code` ในตาราง RBAC — ดู `backend/prisma/seed.ts`) | ชื่อเต็ม | แพลตฟอร์มที่ใช้ | บทบาทโดยสรุป |
|---|---|---|---|
| **ConfigEngineer** | Config Engineer | Web | สร้าง/แก้ไข/Import Config, รัน Config Simulation, ตัดสินใจผ่าน-ไม่ผ่านด้วยตัวเองก่อนส่งให้ Operation อนุมัติ (เดิมเป็นส่วนหนึ่งของ `SW` — แยกออกมาแก้ครั้งที่ 37) |
| **FirmwareEngineer** | Firmware Engineer | Web | อัปโหลด Firmware, แก้ Compatibility Tag, รัน Firmware Simulation (เดิมเป็นส่วนหนึ่งของ `SW` — แยกออกมาแก้ครั้งที่ 37) |
| **QAEngineer** | QA Engineer | Web | ตรวจ/อนุมัติคุณภาพ Firmware ก่อนใช้งานจริง (resource `firmware-decision` — ใหม่ทั้งหมด ไม่เคยมีสิทธิ์นี้มาก่อนตอนยังเป็น `SW`) — ดู Firmware Approval Lifecycle ใน `docs/13_Role_Redesign_Proposal.md` §3.2 |
| **Operation** | Operation | Web | อนุมัติ/ปฏิเสธ Config (Approval Center → status `approved`), จัดการ Campaign (ติดตาม/บำรุงรักษาอุปกรณ์เป็นกลุ่ม — **ไม่ใช่มอบหมายงานให้ช่างหน้างาน** ตั้งแต่แก้ครั้งที่ 29, ระบบมอบหมายงานจริงเป็นของบริษัทแยกต่างหาก), ตัดสินใจ Rollback |
| **ST** | Senior Technician | Web + Mobile | ช่างเทคนิคระดับอาวุโส — มีสิทธิ์ Override Config/Firmware ตรงที่หน้างาน/ระบบโดยไม่ต้องผ่าน flow อนุมัติปกติ (ใช้กรณีแก้ปัญหาเฉพาะกล่องที่ Approval Center ไม่ทันการ) และดูแลการแก้ไข Incident เชิงเทคนิค |
| **OT** | Operation Technician | Web + Mobile | ช่างเทคนิคที่ทำงานสังกัดฝั่ง Operation — สนับสนุนงานปฏิบัติการประจำวัน (มอบหมาย/ติดตามงานช่าง, จัดการ Change Request ที่ส่งเข้ามา) และมีสิทธิ์ Override Config/Firmware เช่นเดียวกับ ST |
| **Auditor** | Auditor | Web | ดูข้อมูลอย่างเดียวทุกจอเพื่อตรวจสอบ (compliance) — ห้าม Create/Update/Approve/Override ทุกกรณี |
| **Admin** | System Admin | Web | จัดการผู้ใช้/สิทธิ์ในระบบ (ยกเว้นบัญชี Admin/SuperAdmin), ปลดระวางกล่อง (Decommission Device), ดูข้อมูลทุกจอ — **ไม่ใช่ผู้อนุมัติ Config แทน Operation** (คงหลัก Separation of Duty) |
| **SuperAdmin** | System Super Admin | Web | ทำได้ทุกอย่างที่ Admin ทำ **+** อนุมัติ/ปฏิเสธคำขอลบ Config (auto delete-request), สร้าง/ปิด/เปลี่ยน role ของบัญชี Admin และ SuperAdmin, เพิ่ม Role ใหม่ + แก้ `RolePermission` — **ไม่ใช่ผู้อนุมัติ Config/Firmware/Campaign แทน Operation และไม่ข้าม Separation of Duty ใด ๆ** · ต้องมี SuperAdmin ≥ 1 คนเสมอ · ทุก action ลง Audit Log · เพิ่มตามมติ Sprint 1 review (`../09_Sprint1_Review_Decisions.md`) — สิทธิ์ resource ใหม่ (`config-deletion`, `admin-management`, `role-management`) อยู่ใน Section 2 (คอลัมน์ SuperAdmin) + Section 4.2 แล้ว · endpoint จริงทยอยมากับ `docs/11` (Part A = `config-deletion`, Part B2 = อีก 2 resource) — ดู footnote ³ ของ Section 2 |

> **✅ ยืนยันแล้วผ่าน [PR #13](https://github.com/dtc-gps-team/gps-config-firmware-center/pull/13) (merged): ตัด Role `FieldTechnician` ออกทั้งเอกสาร — ไม่มี Role นี้อยู่จริง** เดิม `../planning/03_GPS_Detailed_Build_Steps.md` ระบุว่ามี 7 Role รวม "Field Technician" แต่ทีมยืนยันแล้วว่าเป็นชื่อตกค้างจากตอนออกแบบครั้งแรก คนที่ทำงานหน้างาน (ติดตั้งกล่อง/รับ Task/Confirm Install/ส่ง Change Request ผ่านมือถือ) ที่จริงคือ **ST/OT ที่ login เข้าแอป Mobile** ไม่ใช่ Role แยกต่างหาก — enum `role` ใน `openapi.yaml` ก็ไม่เคยมี `FieldTechnician` มาตั้งแต่ต้น จึงตรงกับโค้ดจริงพอดี (ก่อนหน้านี้เอกสารฉบับร่างเข้าใจผิดคิดว่าต้องเพิ่ม `FieldTechnician` เข้า enum — **ไม่ต้องทำแล้ว**) เพราะฉะนั้น ST/OT จึงใช้ทั้ง **Web + Mobile** (ดู Section 3 ที่แก้ตามนี้ทั้งตาราง)
>
> enum สถานะ Config (`DeviceConfigDraft.status` และ query param `status` ของ `GET /config`) บน `main` ตอนนี้คือ `[draft, testing, approved, rejected, synced]` — **เป็นคำรวม "approved" คำเดียว ไม่ได้แยก `sw_approved`/`operation_approved`** ตามที่ร่างก่อนหน้าสมมติไว้ผิด (คิดว่าใครแก้ enum ให้ละเอียดขึ้นแล้ว แต่ที่จริงคือ noise จาก branch เดียวกัน) → ดูผลกระทบต่อ Action ในตารางข้อ 2 และ Gap ที่เกี่ยวข้องในข้อ 6

### รหัส Action ที่ใช้ในตาราง

| รหัส | ความหมาย |
|---|---|
| **C** | Create — สร้างข้อมูล/รายการใหม่ |
| **R** | Read — ดูข้อมูลได้ |
| **U** | Update — แก้ไขข้อมูลที่มีอยู่ |
| **A** | Approve — อนุมัติ/ปฏิเสธ (เปลี่ยนสถานะแบบมีผลผูกพัน) |
| **O** | Override — บังคับเปลี่ยนค่า/ข้ามขั้นตอนปกติ (ใช้กับ Config/Firmware Override โดยเฉพาะ) |
| **-** | ไม่มีสิทธิ์เข้าถึงจอ/action นี้เลย |

---

## 2. Matrix — หน้าจอ/โมดูลฝั่ง Web

> **หมายเหตุคอลัมน์ (แก้ครั้งที่ 37):** 3 คอลัมน์แรก (ConfigEngineer/FirmwareEngineer/QAEngineer) แทนคอลัมน์ `SW` เดิมตัวเดียว — แต่ละ role เห็นเฉพาะแถวที่ตรงโดเมนงานตัวเอง (Config → ConfigEngineer, Firmware → FirmwareEngineer/QAEngineer) แถวที่ไม่เกี่ยวกับโดเมนตัวเองเป็น "-" ทั้งหมด ต่างจาก `SW` เดิมที่ทำได้ทั้ง Config และ Firmware ในคนเดียว

| หน้าจอ / Action | ConfigEngineer | FirmwareEngineer | QAEngineer | Operation | ST | OT | Auditor | Admin | SuperAdmin |
|---|---|---|---|---|---|---|---|---|---|
| **Login** | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) | R (ตนเอง) |
| **Dashboard / Main** | R | R | R | R | R | R | R | R | R |
| **Device Search / Device Detail** | R | R | R | R | R | R | R | R | R |
| **Config Editor** (สร้าง/แก้ Draft ผ่านฟอร์ม — สถานะ `draft`) | C, R, U ² | - | - | R | R | R | R | R | R |
| **Config Import จากไฟล์ (JSON)** (เข้า flow เดียวกับฟอร์ม) | C | - | - | R | R | R | R | R | R |
| **Config Simulation (dry-run)** — รันทดสอบผ่าน `POST /config/{id}/simulate` (**ไม่เปลี่ยนสถานะ** — คืนแค่ผลทดสอบ กดซ้ำได้ระหว่างที่ยังเป็น `draft`) | C, R, U | - | - | R | R | R | R | R | R |
| **Approval Center** — Operation อนุมัติ/ปฏิเสธ Config (อนุมัติ → `approved`, ปฏิเสธ → กลับ `draft` ทั้งหมด) | R | - | - | R, **A** | R | R | R | R | R |
| **คำขอลบ Config อัตโนมัติ** (auto delete-request — อนุมัติ/ปฏิเสธ · resource `config-deletion`) ³ | - | - | - | - | - | - | - | - | **R, A** |
| **Config Simulation Gate** (บล็อก/แก้ไข/ผ่าน ก่อนเข้า Approval) | R | - | - | R | R | R | R | R | R |
| **Device Config Override** ⁴ | - | - | - | - | **O** | - | - | - | - |
| **Firmware Repository** (อัปโหลด + Compatibility Tag) | - | C, R, U | R | R | R | R | R | R | R |
| **Firmware Approval** (ตรวจ/อนุมัติคุณภาพก่อนใช้จริง — resource `firmware-decision`, ใหม่ทั้งหมดแก้ครั้งที่ 37 · implement เป็น `approveFirmware`/`rejectFirmware` จริงแล้วแก้ครั้งที่ 38 — ดู 4.1) | - | R | R, **A** | R | R | R | R | R | R |
| **Firmware Override รายเครื่อง** | - | R | R | R | **C, R, U, O** | **C, R, U, O** | R | R | R |
| **Campaign Wizard** (สร้างแคมเปญ — Campaign Approval แก้ครั้งที่ 39: สร้างแล้วเป็น `pending_approval` ต้องรอ Operation อีกคน (ไม่ใช่ผู้สร้างเอง — Separation of Duty) กด **A** ก่อนถึงจะ `active`) | R | - | - | C, R, U, **A** | R | R | R | R | R |
| **Campaign Monitor** (ติดตาม Failure Rate) | R | - | - | R, U | R | R | R | R | R |
| ~~**Task Management**~~ (สร้าง/มอบหมาย/ติดตามงานช่าง) — **ยกเลิกถาวร ไม่มีหน้านี้แล้ว** (มติพี่เลี้ยงล่าสุด — ดู Section 6 แก้ครั้งที่ 26) เดิมตั้งใจให้มอบหมายงานย้ายไปอยู่ในขั้นตอนหนึ่งของ **Campaign Wizard** แทน **แก้ครั้งที่ 29 (2026-09-14):** ตัดออกจาก Campaign Wizard ด้วยเช่นกัน (Campaign ไม่ใช่เครื่องมือมอบหมายงาน) — **ปัจจุบันไม่มีช่องทางมอบหมายงานให้ช่างหน้างานในระบบนี้เลย** การมอบหมายงานเป็นหน้าที่ของระบบแยกที่บริษัทมีอยู่แล้ว — สิทธิ์แก้ `status` งานตัวเองของ ST/OT ผ่าน Mobile (ตาราง 4.3) ยังเหมือนเดิมทุกประการ ไม่ถูกกระทบ · **แก้ครั้งที่ 37:** grant `tasks.Read` ที่เคยให้ `SW` ถูกตัดทิ้งตอนแยก role (เช็คแล้วไม่เคยใช้งานจริง) ConfigEngineer/FirmwareEngineer/QAEngineer จึงเป็น "-" ทั้งหมด ต่างจาก `SW` เดิมที่เคยมี R ตกค้างไว้ | - | - | - | C, R, U | R¹ | R¹ | R | R | R |
| **Change Request Inbox** (จากมือถือ) | R | - | - | R, U | R | R, U | R | R | R |
| **Incident & Rollback** | R | R | R | C, R, **U** (สั่ง Rollback) | R, U (แก้ไขเชิงเทคนิค) | R | R | R | R |
| **Audit Log** | - | - | - | R | R | R | **R** | R | R |
| **Decommission Device** | - | - | - | C, U | R | R | R | C, U | C, U |
| **User / Role Management** (บัญชีทั่วไป — ไม่รวม Admin/SuperAdmin) — implement แล้วแก้ครั้งที่ 40 (`listManagedUsers`/`createUser`/`updateUser`, resource `user-management`) | - | - | - | - | - | - | - | **C, R, U** | **C, R, U** |
| **จัดการบัญชี Admin/SuperAdmin + Role/Permission** (resource `admin-management`, `role-management`) ³ | - | - | - | - | - | - | - | - | **C, R, U** |
| **Notification Center** (ของตนเอง) | R, U (mark read) | R, U | R, U | R, U | R, U | R, U | R, U | R, U | R, U |

¹ ST/OT บน Web ดู Task ที่ตัวเองถูก assign ได้อย่างเดียว — การแก้ `status` ของงานตัวเอง (รับงาน/ปิดงาน) ทำผ่าน **Mobile** (ดู Section 3 และ 4.3)

² **ปิด open question: Config ไม่ scope ตาม creator** — ยืนยันโดย paveekornk (A) เจ้าของ module `config`: ConfigEngineer ทุกคนแก้ไข/ลบ Config ที่ยังเป็น `draft` ร่วมกันได้ ไม่ใช่แยกเป็นของใครของมัน (ต่างจาก Task ที่ ST/OT เห็น/แก้เฉพาะงานตัวเอง — ดู footnote ¹) `ConfigService.update`/`remove` จึงไม่ filter ด้วย `createdBy` โดยตั้งใจ — ถ้าทีมต้องการเปลี่ยนเป็นแยกตามเจ้าของทีหลัง ต้องแก้ทั้งแถวนี้และ service layer ใหม่ (เดิมเป็นกฎของ `SW` ก่อนแยก role — แก้ครั้งที่ 37)

³ **สิทธิ์เฉพาะ SuperAdmin (`docs/11` Config Deletion + SuperAdmin, Sprint 3)** · สถานะปัจจุบัน:
  - **Part B1 (เสร็จ)** — seed Role `SuperAdmin` + user `superadmin.test` + grant = ทุกสิทธิ์ที่ Admin มี **+** `config-deletion` (Read, Approve) ล่วงหน้า · วันนี้ SuperAdmin จึงทำได้เท่ากับ Admin ทุกอย่าง
  - **Part A (เสร็จ — ทีม B)** — `config-deletion` มีจริงแล้ว: model `ConfigDeletionRequest` + soft-delete `Config.deletedAt` + scheduled job รายวัน + 4 endpoint (`GET /config-deletion-requests`, `.../{id}/approve`, `.../{id}/reject`, `POST /config/{configId}/deletion-requests/keep`) — ดูตาราง 4.1 + changelog แก้ครั้งที่ 22
  - **Part B2 (ทีม A, ตามหลัง)** — endpoint `admin-management` / `role-management` + guard "Admin แตะบัญชี Admin/SuperAdmin ไม่ได้" + กติกา SuperAdmin ≥ 1 คน · grant 2 resource นี้ seed พร้อม endpoint (ยังไม่ seed ตอนนี้)

⁴ **Per-Field Config Override ACL (issue #185, Phase 1 — เจ้าของ `config` A)** — resource
  `config-override` action `Override` ให้เฉพาะ **ST เท่านั้น** ไม่ให้ OT เลยสักฟิลด์ (ต่างจาก
  ที่เอกสารฉบับก่อนหน้าเขียนเป็นเป้าหมาย `C, R, U, O` ให้ทั้ง ST/OT เท่ากัน — ตัวเลขนั้นคือ
  aspiration เดิมก่อนเช็คโค้ดจริงแล้วพบว่าฟีเจอร์นี้ไม่เคยมีอยู่จริงเลย ดู issue #185 §2)
  ระดับ **Value** (ค่าบนอุปกรณ์ผ่าน `Config.fields`) ไม่ใช่ Definition — field ที่ override ได้
  ต้องถูก ConfigEngineer เปิด `stOverridable: true` ไว้ที่ Parameter Library ก่อน (per-field ACL
  ไม่ใช่สิทธิ์ตาม role เฉยๆ) · Override ข้าม Approval Center ปกติ ต้องบันทึก AuditLog ทุกครั้ง
  แบบบังคับ (กฎข้อ 3 ด้านล่าง) และสร้าง `ConfigVersion` ใหม่ทุกครั้ง (เผื่อ Rollback #28) ·
  ขยายเป็น role-list ได้ทีหลังถ้ามี role ที่สามต้องการจริง (YAGNI — ตัดสินใจร่วมกับ B 2026-09-18)
  · **Phase 2 (Mobile, ทีม B) — implement แล้ว (แก้ครั้งที่ 52):** หน้า Override
  ฝั่ง Mobile เปิดจาก Device Detail (ปุ่มเห็นเฉพาะ ST) ดึง Config ปัจจุบันของ
  อุปกรณ์ผ่าน `GET /devices/{deviceId}/config` (resource ใหม่ `device-current-config`)
  ก่อน แล้วเรียก `overrideConfig` (endpoint เดิม) ต่อ — UX mirror
  `config-override-panel.tsx` ฝั่ง Web ทุกจุด
  · **⚠️ แก้ไข (แก้ครั้งที่ 53, issue #223):** พบระหว่างรีวิว PR #222 ว่า
  `overrideConfig` เดิมแก้ `Config.fields` **ทั้งชุด** กระทบทุกอุปกรณ์ที่ใช้
  Config เดียวกัน ไม่ใช่แค่เครื่องที่เปิดหน้าอยู่ — เพิ่ม mechanism ใหม่
  **"Per-device Config Override"** (`POST /devices/{deviceId}/config-override`,
  resource ใหม่ `device-config-override` action `Override`, ST เท่านั้นเหมือนกัน)
  เขียนลงตารางใหม่ `DeviceConfigOverride` แทน ไม่แตะ `Config` เดิมเลย — Mobile
  เปลี่ยนไปเรียก endpoint ใหม่นี้ตั้งแต่ #223 เป็นต้นไป **endpoint เดิม
  `POST /config/{configId}/override` ยังอยู่คู่กัน** (Web ยังใช้อยู่ — ฝั่ง A) —
  เป็น known tech debt รอ paveekornkwork-dev (A) ตัดสินใจว่าจะรวม/deprecate
  endpoint เดิมทีหลังไหม (ดู PR #223 description) **ไม่ใช่ bug ที่ลืมแก้**

---

## 3. Matrix — หน้าจอฝั่ง Mobile (Operation/ST/OT)

**หมายเหตุ:** ตาม [PR #13](https://github.com/dtc-gps-team/gps-config-firmware-center/pull/13) — ผู้ใช้ Mobile ทั้งหมดคือ **Operation/ST/OT** (ไม่มี Role `FieldTechnician` แยกต่างหากอีกต่อไป) คงคอลัมน์ Role ไว้เผื่ออนาคตอยากแยกสิทธิ์เฉพาะบางแถวระหว่าง Operation/ST/OT

| หน้าจอ / Action | Role | Action |
|---|---|---|
| **Login** | Operation/ST/OT | R (ตนเอง) |
| **Task List** (งานที่ได้รับมอบหมาย) | Operation/ST/OT | R, U (อัปเดตสถานะงานของตัวเอง) |
| **Device Registration** (ลงทะเบียนกล่องใหม่ตอนติดตั้ง) | Operation/ST/OT | C, R |
| **ทดสอบกับ Device Simulator ก่อนติดตั้ง** (ไม่บังคับ — v3.5) | Operation/ST/OT | C, R |
| **Confirm Install** (ยืนยันติดตั้งสำเร็จ) | Operation/ST/OT | U |
| **Change Request** (ส่งคำขอเปลี่ยนแปลงเข้า Inbox เว็บ) | Operation/ST/OT | C, R (เฉพาะของตัวเอง) |
| **Push Notification / แจ้งเตือน** | Operation/ST/OT | R, U (mark read) |
| **Device Status ของกล่องที่ตนดูแล** | Operation/ST/OT | R |

> ผู้ใช้ Mobile (Operation/ST/OT) **ไม่มีสิทธิ์เข้าหน้า Config Editor, Approval Center, Campaign Wizard, Audit Log ผ่านมือถือ** — งานเหล่านี้ทำผ่าน Web เท่านั้น ยกเว้น **Override** ที่ ST/OT มีสิทธิ์เต็ม (C,R,U,O) อยู่แล้วผ่าน Web ตาม Section 2 (ยังไม่ได้เปิดให้ทำผ่าน Mobile — ถ้าต้องการเพิ่มในอนาคตต้องระบุแยก)

---

## 4. Mapping ไปยัง Backend Endpoint (สำหรับเขียน Auth Guard)

แหล่งอ้างอิงหลัก: **`docs/api/openapi.yaml` บน `main` (v0.1.0)** — ตาราง 4.1 คือ endpoint ที่**มีอยู่จริง**ใน spec วันนี้ ใช้ implement Guard ได้ทันที ส่วนตาราง 4.2 คือ endpoint ที่ Matrix นี้อ้างถึงในข้อ 2/3 แต่**ยังไม่มีใน spec** — ต้องเพิ่มเข้า openapi.yaml ก่อนเขียน Guard จริง (ห้ามเดา path/method เอาเองแล้วชงเข้าโค้ด)

### 4.1 Endpoint ที่มีจริงใน `openapi.yaml` (main) วันนี้

| Endpoint | Method | operationId | Role ที่อนุญาต |
|---|---|---|---|
| `/auth/login` | POST | `login` | Public (`security: []` — ระบุไว้ใน spec แล้ว) — enum `role` ปัจจุบัน `[ConfigEngineer, FirmwareEngineer, QAEngineer, Operation, ST, OT, Auditor, Admin, SuperAdmin]` ครอบคลุมครบแล้ว (เดิม `SW` ตัวเดียวก่อนแยก role — แก้ครั้งที่ 37) ไม่ต้องเพิ่ม Role ใหม่ (ตัด FieldTechnician ออกตาม PR #13) |
| `/config` | GET | `listConfigs` | ทุก Role ที่ login แล้ว |
| `/config` | POST | `createConfig` | ConfigEngineer |
| `/config/import` | POST | `importConfig` | ConfigEngineer |
| `/config/{configId}/simulate` | POST | `simulateConfig` | ConfigEngineer, Operation/ST/OT (endpoint เดียวกันทั้ง Web/Mobile ตาม summary ใน spec) |
| `/config/{configId}/decide` | POST | `decideConfig` | **ConfigEngineer** (resource `config-decision` action `Approve`) — ปักผลผ่าน/ไม่ผ่านหลังดู `simulate` · `passed:true` → `draft`→`testing` · +body `suggestedApproverId` (optional, v3.19 — ConfigEngineer เจาะจง Operation ที่อยากให้ดู · backend เช็ค role Operation · **ไม่ผูกมัด** Operation คนอื่นก็ approve ได้) — ดู changelog แก้ครั้งที่ 25 |
| `/config/{configId}/approve` | POST | `approveConfig` | Operation เท่านั้น |
| `/config/{configId}/reject` | POST | `rejectConfig` | Operation เท่านั้น |
| `/config/{configId}/override` | POST | `overrideConfig` | **ST เท่านั้น** (resource `config-override` action `Override`, issue #185) — body `{ fields, reason }` ทุก key ของ `fields` ต้อง `stOverridable: true` ไม่งั้น 400 · `reason` บังคับ · ต้อง Config สถานะ `approved`/`synced` เท่านั้น (409 ถ้าไม่ใช่) · สร้าง `ConfigVersion` ใหม่ + AuditLog บังคับทุกครั้ง · **แก้ Config.fields ทั้งชุด กระทบทุกอุปกรณ์ที่ใช้ Config นี้ร่วมกัน** — Web ยังใช้ endpoint นี้อยู่ (known tech debt คู่กับ `overrideDeviceConfig` ด้านล่าง ดู footnote ⁴ + changelog แก้ครั้งที่ 53) |
| `/config/{configId}/versions` | GET | `listConfigVersions` | resource `config` action `Read` — เหมือน `getConfig` (ConfigEngineer/Operation/ST/OT/Auditor/Admin) · version history เป็นแค่ snapshot ของ `fields` ที่ Read config เห็นอยู่แล้ว + Auditor (compliance) ต้องดูได้ จึง**ไม่แยก resource** ต่างจาก `simulate`/`decide` (Stage 5, #26 ข้อ 9) |
| `/config/{configId}/versions/{versionNumber}` | GET | `getConfigVersion` | เหมือน `listConfigVersions` |
| `/config-definitions` | GET | `listConfigDefinitions` | ConfigEngineer, Operation, ST, OT (resource `config-definition` · catalog อ่านอย่างเดียว ไม่ใช่ข้อมูลอ่อนไหว — Auditor/Admin ยังไม่ให้ เพราะยังไม่มี use case) |
| `/config-definitions` | POST | `createConfigDefinition` | **ConfigEngineer เท่านั้น** (resource `config-definition` action `Create` · Semantic Validation, #26 — ConfigEngineer สร้าง field definition ใหม่เองได้เลย ไม่ต้องผ่านอนุมัติ เพราะ field ที่มีปัญหาจริงจะโดนจับตอนเอาไปสร้าง Config Template แล้วเข้า simulate/decide/approve อยู่ดี — ตัดสินใจร่วมกับทีมและพี่เลี้ยง 2569-09) · รับ `defaultValue` ได้ (ไม่บังคับ, issue #202, แก้ครั้งที่ 43) · **ไม่แตะ contract** — Web ฟอร์มสร้างเปลี่ยนวิธีเลือก `supportedModels` เป็นเลือกแค่รุ่นอุปกรณ์แล้ว expand protocol ให้เอง (issue #203, แก้ครั้งที่ 48) |
| `/notifications` | GET | `listNotifications` | ทุก Role ที่ login แล้ว — ดึงเฉพาะของ user ตัวเอง (ผูกกับ JWT ไม่ใช่ query param) |
| `/notifications/{notificationId}/read` | PATCH | `markNotificationRead` | ทุก Role ที่ login แล้ว — เฉพาะ notification ของตัวเอง |
| `/notifications/device-tokens` | POST | `registerDeviceToken` | ทุก Role ที่ login แล้ว — ลงทะเบียน/อัปเดต FCM token ของเครื่องตัวเอง (upsert ตาม token, `userId` ผูกกับ JWT ไม่รับจาก client) · **JwtAuthGuard อย่างเดียว ไม่มี PermissionGuard/resource ใหม่** — self-scoped ล้วน ทุก Role ที่ login ผ่าน Mobile ต้องรับ push ของตัวเองได้ ไม่มี Role ไหนควรถูกห้าม (mirror `/notifications` GET · Section 5 ข้อ 8) — Push Notification groundwork #17 (ดู changelog แก้ครั้งที่ 19) |
| `/notifications/device-tokens` | DELETE | `deleteDeviceToken` | ทุก Role ที่ login แล้ว — ถอนทะเบียน token ของตัวเองตอน logout · IDOR: `deleteMany({ where: { token, userId } })` + `count === 0` → 404 (ลบ token คนอื่นไม่ได้) · `token` เป็น query param |
| `/tasks` | GET | `listTasks` | ทุก Role ที่มี grant `tasks` Read (Operation, ST, OT, Auditor, Admin, SuperAdmin) — **แก้ครั้งที่ 41:** เฉพาะ Operation/Auditor/Admin/SuperAdmin (`UNSCOPED_TASK_ROLES`) เท่านั้นที่เห็นทุก Task ได้ (เลือก `assignedTo` เองผ่าน query ได้ด้วย) role อื่นทั้งหมด (รวม ST/OT และ role ใหม่ที่อาจได้ grant นี้เพิ่มทีหลังโดยไม่ได้ตั้งใจ) ถูกบังคับ self-scope เสมอเป็นค่า default ที่ปลอดภัย ไม่ใช่แค่ ST/OT อย่างเดียวเหมือนเดิม — ดู issue #73 |
| `/tasks` | POST | `createTask` | Operation เท่านั้น (ปิด open question — ดู 4.3) |
| `/tasks/{taskId}` | GET | `getTask` | **แก้ครั้งที่ 49:** เฉพาะ `UNSCOPED_TASK_ROLES` (Operation/Auditor/Admin/SuperAdmin) เห็นได้ทุก Task · ST/OT เฉพาะงานที่ตัวเองถูก assign (404 ถ้าไม่ใช่) · role อื่นที่ไม่อยู่ในทั้งสอง list เลย (เช่น ConfigEngineer) **ไม่เห็นเลย (404 default-deny)** — เดิมเป็น default-allow (เห็นได้หมดยกเว้น ST/OT ที่ไม่ใช่เจ้าของ) endpoint นี้ไม่มี PermissionGuard คุมเลย (แค่ `JwtAuthGuard`) การเช็คใน service จึงเป็นด่านเดียว — ดู issue #73 ข้อ 3 |
| `/tasks/{taskId}` | PATCH | `updateTask` | Operation (ทุก field) · ST/OT (เฉพาะ field `status` ของ Task ที่ตัวเองถูก assign **และตั้งได้แค่ `in_progress`/`completed` เท่านั้น** — แก้ครั้งที่ 49, mirror ตัวเลือกที่ Mobile จำกัดไว้ที่ `_fieldStaffStatusChoices` — ตั้ง `cancelled`/`pending` เอง = 403, ยกเลิกงานเป็นสิทธิ์ Operation ไม่ใช่ผู้ถูก assign) — ดู issue #73 ข้อ 2 |
| `/firmware` | GET | `listFirmware` | ทุก Role ที่ login แล้ว (resource `firmware` action `Read` · เรียง `uploadedAt desc`) — ดู changelog แก้ครั้งที่ 29 |
| `/firmware` | POST | `uploadFirmware` | **FirmwareEngineer เท่านั้น** (resource `firmware` action `Create`) — อัปโหลดขึ้น Object Storage (MinIO) จริง แบบ synchronous · `uploadStatus: stored`/`failed` ตามผลจริง (ไม่ throw 500) · ขนาดไฟล์สูงสุด 50MB |
| `/firmware/{firmwareId}` | GET | `getFirmware` | เหมือน `listFirmware` — resource `firmware` action `Read` เดียวกัน · 404 ถ้าไม่พบ |
| `/firmware/{firmwareId}` | PATCH | `updateFirmwareCompatibility` | **FirmwareEngineer เท่านั้น** (resource `firmware` action `Update`) — แก้ Compatibility Tag แทนที่ `deviceModelCompatibility` ทั้ง array (ไม่ merge) แยกจากขั้นตอนอัปโหลด |
| `/firmware/{firmwareId}/simulate` | POST | `simulateFirmware` | **FirmwareEngineer, QAEngineer, Operation, ST, OT** (resource `firmware-simulation` action `Read` — แยกจาก `firmware` ธรรมดา mirror `config`/`config-simulation` กัน Auditor/Admin เรียกได้โดยไม่ตั้งใจ · QAEngineer เพิ่มเข้ามาแก้ครั้งที่ 37 — ต้องดูผล simulation ก่อนตัดสินใจอนุมัติคุณภาพ) · mock เช็ค compatibility เท่านั้น · 409 ถ้า `uploadStatus` ยังไม่ `stored` |
| `/firmware/{firmwareId}/approve` | POST | `approveFirmware` | **QAEngineer เท่านั้น** (resource `firmware-decision` action `Approve` — mirror `config-decision`) — Firmware Approval Lifecycle (docs/13 §3.2, แก้ครั้งที่ 38) `approvalStatus`: `pending_review`→`approved` + ตั้ง `approvedBy` · 409 ถ้าไม่ใช่ `pending_review` |
| `/firmware/{firmwareId}/reject` | POST | `rejectFirmware` | **QAEngineer เท่านั้น** (resource เดียวกับ `approveFirmware`) — `approvalStatus`: `pending_review`→`rejected` (ไม่ตั้ง `approvedBy`) · ไม่มี body · 409 ถ้าไม่ใช่ `pending_review` · FirmwareEngineer ต้องอัปโหลดเวอร์ชันใหม่แก้ไข ไม่มีการแก้ไฟล์เดิมซ้ำ |
| `/devices` | GET | `listDevices` | ทุก Role ที่ login แล้ว (resource `devices` action `Read` · Device Search — list + filter `search`/`deviceModel`/`protocol`/`status`/`customerId` · ดู changelog แก้ครั้งที่ 23, 45) |
| `/devices/{deviceId}` | GET | `getDevice` | ทุก Role ที่ login แล้ว (resource `devices` action `Read` · Device Detail — field ของ record Device เอง) |
| `/devices/{deviceId}/status` | GET | `getDeviceStatus` | ทุก Role ที่ login แล้ว (หมายเหตุ: มีแต่ spec ใน openapi.yaml ยังไม่มีโค้ดจริง — ดู changelog แก้ครั้งที่ 13) |
| `/devices/{deviceId}/test-connection` | POST | `testDeviceConnection` | **ST, OT เท่านั้น** (resource `device-connection-test` · ทดสอบสัญญาณอุปกรณ์ที่ติดตั้งจริง — A ยืนยันบน PR #52 ว่ายังไม่เปิด SW (เดิม)/Operation) |
| `/devices/{deviceId}/apply-config` | POST | `applyConfigToDevice` | **ST, OT เท่านั้น** (resource `device-config-apply` action `Read` · ช่างหน้างานใส่ Config ที่อนุมัติแล้วเข้าอุปกรณ์ — mirror `device-connection-test` · mock fire-and-forget ไม่ persist — ดู changelog แก้ครั้งที่ 17) |
| `/devices/{deviceId}/confirm-firmware-install` | POST | `confirmFirmwareInstall` | **ST, OT เท่านั้น** (resource `device-firmware-confirm` action `Create` — **ต่างจาก `device-config-apply` ที่ใช้ `Read`** เพราะ endpoint นี้เขียน AuditLog จริง ไม่ใช่ fire-and-forget ที่ไม่ persist อะไรเลย · ช่างยืนยันว่าติดตั้ง Firmware เสร็จแล้วจริง — ไม่คำนวณ `deviceUpdateStatus` จริง (มติ issue #181 ข้อ 3) — ดู changelog แก้ครั้งที่ 51) |
| `/devices/{deviceId}/simulate-config` | POST | `simulateConfigOnDevice` | **ST, OT เท่านั้น** (reuse resource `device-connection-test` action `Read` · dry-run เช็คความพร้อม Config ก่อน apply จริง — configCheck + compatibilityCheck + connectionCheck รวมเป็นผลเดียว · ไม่ persist · ปิด open question Section 6 เรื่อง endpoint ช่างหน้างาน — ดู `docs/08_ConfigSimulator_Phase2_Proposal.md` + changelog แก้ครั้งที่ 19) |
| `/devices/{deviceId}/config` | GET | `getDeviceCurrentConfig` | **ST, OT เท่านั้น** (resource ใหม่ `device-current-config` action `Read` · Config Override Phase 2 Mobile, issue #211 — derive จาก `Task.configId` ของ Task ล่าสุด `status: completed` ที่ผูกกับอุปกรณ์นี้ เพราะ Device ไม่มี FK ตรงไป Config · 404 ถ้าไม่มี Task completed ที่ผูก configId เลย หรือ Config ถูก soft-delete ไปแล้ว · **แก้ครั้งที่ 53 (issue #223):** response merge `DeviceConfigOverride` ล่าสุดของอุปกรณ์นี้ (ถ้ามี) ทับ base Config ก่อนคืนกลับ + เพิ่ม field `hasDeviceOverride` — ดู changelog แก้ครั้งที่ 52, 53) |
| `/devices/{deviceId}/config-override` | POST | `overrideDeviceConfig` | **ST เท่านั้น** (resource ใหม่ `device-config-override` action `Override`, issue #223 — mirror `config-override` เดิม, OT ไม่มีสิทธิ์เลยเหมือนกัน) — body `{ fields, reason }` เหมือน `overrideConfig` เดิมทุกประการ (validate ด้วย `validateOverridableFields` ตัวเดียวกัน) **ต่างกันที่ scope**: เขียนลง `DeviceConfigOverride` ผูกกับอุปกรณ์เครื่องนี้เท่านั้น ไม่แตะ `Config` เดิมเลย จึงไม่กระทบอุปกรณ์อื่นที่ใช้ Config เดียวกัน — base Config มาจาก `getDeviceCurrentConfig` (404 ถ้าไม่มี) — ดู changelog แก้ครั้งที่ 53 |
| `/config-deletion-requests` | GET | `listConfigDeletionRequests` | **SuperAdmin เท่านั้น** (resource `config-deletion` action `Read` · คิวคำขอลบ Config อัตโนมัติ — ไม่ระบุ `status` = `pending`) — `docs/11` Part A, ดู changelog แก้ครั้งที่ 22 |
| `/config-deletion-requests/{id}/approve` | POST | `approveConfigDeletionRequest` | **SuperAdmin เท่านั้น** (resource `config-deletion` action `Approve` · อนุมัติ → soft delete Config · 409 ถ้าคำขอไม่ `pending`) |
| `/config-deletion-requests/{id}/reject` | POST | `rejectConfigDeletionRequest` | **SuperAdmin เท่านั้น** (resource `config-deletion` action `Approve` · body `{ note }` · Config อยู่ต่อ + cooldown 90 วัน · 409 ถ้าคำขอไม่ `pending`) |
| `/config/{configId}/deletion-requests/keep` | POST | `keepConfigDeletionRequest` | **ConfigEngineer** (resource `config` action `Update` เดิม · ผู้สร้าง/ConfigEngineer กด "เก็บไว้" ระหว่าง grace period → `cancelled` + reset นาฬิกา 90 วัน) — key ด้วย config id ไม่ใช่ request id (ผู้ใช้ได้ notification เรื่อง Config ไม่รู้ request id) |
| `/incidents` | GET | `listIncidents` | ทุก Role ที่ login แล้ว (resource `incidents` action `Read` · read-only list + filter `status`/`relatedConfigId`/`relatedFirmwareId` — ดู changelog แก้ครั้งที่ 24 · Web ต่อจริงแล้ว แก้ครั้งที่ 46) |
| `/incidents/{id}` | GET | `getIncident` | ทุก Role ที่ login แล้ว (resource `incidents` action `Read` · detail 1 รายการ · 404 ถ้าไม่พบ) |
| `/audit-logs` | GET | `listAuditLogs` | ทุก Role **ยกเว้น ConfigEngineer/FirmwareEngineer/QAEngineer** (เดิม SW ตัวเดียวก่อนแยก role — แก้ครั้งที่ 37) (resource `audit-logs` action `Read` — grant Operation/ST/OT/Auditor/Admin, SuperAdmin ได้อัตโนมัติจากการ copy สิทธิ์ Admin) · read-only list + filter `userId`/`auditModule`/`action` เรียง `createdAt desc` · ไม่ join ชื่อผู้ใช้ (คืนแค่ `userId` ดิบ) — client resolve ชื่อเอง · **การเขียนไม่มี endpoint ตรง** แต่ละโมดูลเขียน `AuditLog` เองตอนเกิด mutation จริง (ตอนนี้: `config` ครบ create/update/delete/decide/approve/reject, `device` เฉพาะ `applyConfig`, `config-deletion` ทำไปแล้วตั้งแต่แก้ครั้งที่ 22, `task` ครบ create/update ตั้งแต่แก้ครั้งที่ 29 — `notification` ไม่เขียนเลยโดยตั้งใจ ดูเหตุผลในแก้ครั้งที่ 29) — ดู changelog แก้ครั้งที่ 27/29 |
| `/users` | GET | `listUsers` | ทุก Role ที่ login แล้ว — **JwtAuthGuard อย่างเดียว ไม่มี PermissionGuard/resource** (mirror `/notifications/device-tokens`) · คืนแค่ id/ชื่อ/role code ของ user ที่ active · filter `?role=` · ใช้ทำ dropdown "เจาะจงผู้อนุมัติ" ใน Approval Center (#19) — **ไม่ใช่** จอ User / Role Management (ดูแถวถัดไปด้านล่าง) · ดู changelog แก้ครั้งที่ 25 |
| `/users` | POST | `createUser` | **Admin เท่านั้น** (resource `user-management` action `Create`) — สร้างบัญชีทั่วไปใหม่ · `role` ต้องไม่ใช่ Admin/SuperAdmin (400 ถ้าเป็น — จัดการผ่าน `admin-management` ที่ยังไม่ finalize) · Admin ตั้งรหัสผ่านเริ่มต้นเอง (ยังไม่มี flow ลืมรหัสผ่าน) · 409 ถ้า username ซ้ำ — ดู changelog แก้ครั้งที่ 40 |
| `/users/managed` | GET | `listManagedUsers` | **Admin เท่านั้น** (resource `user-management` action `Read`) — คืน username/isActive ด้วย (ต่างจาก `listUsers`) ตัดบัญชี Admin/SuperAdmin ออกทั้งหมดเสมอ — ดู changelog แก้ครั้งที่ 40 |
| `/users/{userId}` | PATCH | `updateUser` | **Admin เท่านั้น** (resource `user-management` action `Update`) — แก้ `role`/`isActive` (ไม่มี hard delete) · บัญชีเป้าหมายเป็น Admin/SuperAdmin → 404 (มองว่าไม่มีอยู่จาก resource นี้) · แก้ `role` เป็น Admin/SuperAdmin → 400 (กัน escalation) — ดู changelog แก้ครั้งที่ 40 |
| `/device-models` | GET | `listDeviceModels` | ทุก Role ที่ login แล้ว — **JwtAuthGuard อย่างเดียว ไม่มี PermissionGuard/resource** (mirror `/users`) · เรียงตามชื่อรุ่น · ใช้ทำ dropdown เลือกรุ่นตอนสร้าง Config/เพิ่มอุปกรณ์ — ดู changelog แก้ครั้งที่ 47 |
| `/device-models` | POST | `createDeviceModel` | **Admin เท่านั้น** (resource `device-model` action `Create` — master/reference data เพิ่มไม่บ่อย mirror `user-management`) — `supportedProtocols` บังคับอย่างน้อย 1 ค่า (400 ถ้าว่าง) · 409 ถ้าชื่อรุ่นซ้ำ — ดู changelog แก้ครั้งที่ 47 |
| `/device-models/{id}` | GET | `getDeviceModel` | เหมือน `listDeviceModels` — resource เดียวกัน (ไม่มี guard) · 404 ถ้าไม่พบ |
| `/device-models/{id}` | PATCH | `updateDeviceModel` | **Admin เท่านั้น** (resource `device-model` action `Update`) — ไม่มี `name` ให้แก้ (ต้องตรงกับ string เดิมที่กระจายอยู่ทั่วระบบเป๊ะ) ไม่มี hard delete ใช้ `status: discontinued` แทน — ดู changelog แก้ครั้งที่ 47 |
| `/campaigns` | GET | `listCampaigns` | ทุก Role ที่ login แล้ว (resource `campaign` action `Read` · เรียง `createdAt desc` · filter optional `status`) — ดู changelog แก้ครั้งที่ 28 |
| `/campaigns` | POST | `createCampaign` | **Operation เท่านั้น** (resource `campaign` action `Create`) — สร้าง Campaign+CampaignTarget[] ใน transaction เดียว **แก้ครั้งที่ 39: เป็น `pending_approval` แทน `active` ทันที** (ต้องรออนุมัติก่อน — ดูแถว approve/reject ด้านล่าง) · **ไม่สร้าง Task อีกต่อไป** (ตัดออกแก้ครั้งที่ 29 — Campaign ไม่ใช่เครื่องมือมอบหมายงาน) · `payloadType: Firmware` **รองรับแล้ว** (เปิดใช้งานแก้ครั้งที่ 34) — ต้อง `uploadStatus: stored` **และ** `approvalStatus: approved` ทั้งคู่ (เพิ่มเงื่อนไขหลังแก้ครั้งที่ 38) |
| `/campaigns/{id}` | GET | `getCampaign` | เหมือน `listCampaigns` — resource `campaign` action `Read` เดียวกัน · 404 ถ้าไม่พบ |
| `/campaigns/{id}/approve` | POST | `approveCampaign` | **ใหม่ทั้งหมด แก้ครั้งที่ 39 (Campaign Approval)** — resource `campaign` action `Approve`, **Operation เท่านั้น** — `pending_approval` → `active` พร้อม `approvedBy`/`approvedAt` · **SoD: ผู้กดต้องไม่ใช่ผู้สร้าง Campaign เดียวกัน (403)** — เช็คที่ service layer ไม่ใช่ grant (grant บอกแค่ว่า role Operation ใช้ endpoint นี้ได้ ไม่ได้บอกว่า instance ไหนอนุมัติได้) |
| `/campaigns/{id}/reject` | POST | `rejectCampaign` | resource เดียวกับ `approveCampaign` — `pending_approval` → `rejected` (แยกจาก `draft` mirror `ConfigStatus.rejected`) ไม่ตั้ง `approvedBy`/`approvedAt` · SoD เดียวกับ approve · แก้ไขแล้วส่งอนุมัติใหม่ได้ผ่าน `createCampaign` อีกครั้ง (ยังไม่มี endpoint แก้ไข Campaign เดิม รอ Campaign Monitor แถวที่ 22) |

### 4.2 Endpoint ที่ Matrix อ้างถึง แต่ยังไม่มีใน `openapi.yaml` — ต้องเพิ่มก่อนเขียน Guard

| Endpoint ที่ต้องเพิ่ม (ชื่อ/path เป็นข้อเสนอ ยังไม่ fix) | Method | Role ที่อนุญาต (ตามข้อ 2) | เหตุผลที่ยังไม่มี |
|---|---|---|---|
| Config: ให้ ConfigEngineer ปักผลตัดสินใจผ่าน/ไม่ผ่านเอง (แยกจาก `simulate` ที่แค่คืนผลทดสอบ ไม่เปลี่ยน status) | POST | ConfigEngineer | **ปิดแล้ว** — implement เป็น `decideConfig` จริงแล้ว (ตาราง 4.1) เก็บแถวนี้ไว้เป็นบันทึกประวัติ open question เดิมเท่านั้น |
| `/config/{configId}/override` | POST | ST, OT | ยังไม่มีโมดูล Override เลยใน spec |
| `/firmware/{firmwareId}/override` | POST | ST, OT | เช่นเดียวกับข้างบน |
| Firmware: ให้ QAEngineer อนุมัติ/ปฏิเสธคุณภาพ Firmware (resource `firmware-decision` action `Approve`) | POST | QAEngineer | **ปิดแล้ว** — implement เป็น `approveFirmware`/`rejectFirmware` จริงแล้ว (ตาราง 4.1, แก้ครั้งที่ 38) เก็บแถวนี้ไว้เป็นบันทึกประวัติ open question เดิมเท่านั้น |
| `/devices` (registration) | POST | Operation/ST/OT | ยังไม่มี endpoint สร้าง Device |
| `/devices/{deviceId}/decommission` | POST | Operation, Admin | ยังไม่มีโมดูล Device lifecycle |
| `/campaigns` | ~~GET/POST~~ / PATCH | ~~ทุก Role (อ่าน), Operation (สร้าง)~~, Operation (แก้ — รอ Campaign Monitor) | **GET/POST implement แล้ว** (resource `campaign` action `Read`/`Create`) → ย้ายขึ้นตาราง 4.1 — ดู changelog แก้ครั้งที่ 28 · PATCH (แก้สถานะ/ยกเลิกแคมเปญระหว่างทาง) ยังไม่เปิดผ่าน API รอ Campaign Monitor แถวที่ 22 |
| `/change-requests` | POST | Operation/ST/OT (ฝั่ง Mobile) | ยังไม่มีโมดูลนี้ |
| `/change-requests` | GET/PATCH | Operation, OT | เช่นเดียวกับข้างบน |
| `/incidents` | ~~GET~~ / POST/PATCH | ระบบสร้างอัตโนมัติ (POST), ~~ทุก Role อ่าน (GET)~~, Operation/ST แก้ (PATCH) | **GET implement แล้ว** (read-only list+detail, resource `incidents` action `Read` ทุก Role) → ย้ายขึ้นตาราง 4.1 เป็น `/incidents` + `/incidents/{id}` — ดู changelog แก้ครั้งที่ 24 · POST (auto จาก config-sync-writer) + PATCH (Rollback flow แถว 28) ยังไม่เปิดผ่าน API |
| User / Role Management: `/users` POST, `/users/managed` GET, `/users/{id}` PATCH | Admin เท่านั้น (filter `role.code NOT IN ('Admin','SuperAdmin')` — บัญชี Admin/SuperAdmin จัดการผ่าน `admin-management` เท่านั้น) | **ปิดแล้ว** — implement เป็น `createUser`/`listManagedUsers`/`updateUser` จริงแล้ว (ตาราง 4.1, แก้ครั้งที่ 40) เก็บแถวนี้ไว้เป็นบันทึกประวัติ open question เดิมเท่านั้น |
| ~~`/config/{configId}/deletion-requests`~~ | ~~GET/PATCH~~ | — | **implement แล้ว** (Part A, ทีม B) → ย้ายขึ้นตาราง 4.1 เป็น `/config-deletion-requests` (+ `/approve` `/reject`) และ `/config/{configId}/deletion-requests/keep` — ดู changelog แก้ครั้งที่ 22 |
| `/admin/accounts` (path เป็นข้อเสนอ) | GET/POST/PATCH | **SuperAdmin** (resource `admin-management` — สร้าง/ปิด/เปลี่ยน role บัญชี Admin/SuperAdmin · ต้องมี SuperAdmin ≥ 1) | `docs/11` Part B2 (ทีม A) — grant seed พร้อม endpoint |
| `/roles`, `/roles/{roleId}/permissions` (path เป็นข้อเสนอ) | GET/POST/PATCH | **SuperAdmin** (resource `role-management` — เพิ่ม Role + แก้ `RolePermission`) | `docs/11` Part B2 (ทีม A) — grant seed พร้อม endpoint |

> ตามหมายเหตุท้าย `openapi.yaml`: "ทุกครั้งที่เพิ่ม Endpoint ใหม่ในแต่ละ Phase ถัดไป ให้กลับมาอัปเดตไฟล์นี้ด้วย" — ตาราง 4.2 นี้คือ backlog ของสิ่งที่ต้องอัปเดตเข้า spec ก่อน ไม่ใช่สิ่งที่ Guard เขียนได้ตอนนี้

### 4.3 Task module — รายละเอียดสิทธิ์ (ปิด open question: Task creator = Operation)

> **❌ ยกเลิกแล้ว (แก้ครั้งที่ 26, 2026-09-11)** — Task Management (Sprint 2 #7) ถูกยกเลิก
> ถาวรตามมติพี่เลี้ยง ไม่ทำในรูปแบบไหนทั้งสิ้น (ทั้งหน้า Web และแนวคิด "Operation สร้าง/
> มอบหมาย Task" ด้านล่างนี้) — และตั้งแต่แก้ครั้งที่ 29 (2026-09-14) Campaign ก็ไม่สร้าง
> `Task` แทนอีกต่อไปเช่นกัน (มติพี่เลี้ยง: การมอบหมายงานให้ช่างหน้างานเป็นหน้าที่ของระบบ
> แยกที่บริษัทมีอยู่แล้ว ทำเองในระบบนี้จะซ้อนทับ) **ผลคือไม่มี "ผู้ใช้งานจริง" ทางไหนสร้าง
> `Task` ได้อีกแล้ว** (หน้า Web ถูก comment out ไปตาม `docs/09` ข้อ 1, Campaign ก็เลิกสร้าง
> ตามแก้ครั้งที่ 29) — **แต่ backend endpoint `POST /tasks` (ตาราง 4.1 บรรทัด `createTask`)
> ยังไม่ถูกแตะ ยังเป็น API endpoint จริงที่เรียกได้อยู่** (กลายเป็น endpoint ที่ไม่มี client
> ไหนเรียกแล้ว — ตัดสินใจว่าจะ deprecate/ลบทิ้งหรือเก็บไว้เป็นของทีม B แยกต่างหาก ตามที่
> ตกลงกับ kittiphong ไว้ในแก้ครั้งที่ 29 ข้อ 6) — เก็บ section นี้ไว้เป็นบันทึกประวัติการ
> ตัดสินใจเท่านั้น อย่ายึดเป็นสิทธิ์ที่ยังมี client ใช้งานจริง — ดู changelog แก้ครั้งที่ 26/29

ยืนยันโดย kittiphong (B) เจ้าของ module `task` ตามแพทเทิร์นเดิมของ Matrix นี้ — **Operation สั่งงาน/อนุมัติ, ST/OT ปฏิบัติงาน**

| Action | Role ที่ทำได้ |
|---|---|
| สร้าง Task / มอบหมาย Task | Operation |
| แก้ไข / ลบ Task ทั้งหมด (ทุก field) | Operation |
| ดู Task ที่ตัวเองถูกมอบหมาย | ST, OT |
| แก้ `status` ของ Task ตัวเอง (เช่น รับงาน / ปิดงาน) | ST, OT (เฉพาะ Task ที่ตัวเองถูก assign เท่านั้น) |
| ดู Task ทั้งหมด (ทุก Task ในระบบ) | เดิม SW (read-only) — grant `tasks.Read` ถูกตัดทิ้งตอนแยก role เพราะไม่เคยใช้จริง (แก้ครั้งที่ 37) ConfigEngineer/FirmwareEngineer/QAEngineer จึงไม่มีสิทธิ์นี้อีกต่อไป, Operation (จัดการได้ — ดูแถวบน), Auditor, Admin (read-only) |

> **ST/OT ห้ามสร้าง Task เอง** และ **ห้ามแก้ field อื่นนอกจาก `status`** ของ Task ที่ตัวเองถูก assign — ป้องกันการมอบหมายงานให้ตัวเอง / แก้ไขข้อมูล Task ของคนอื่น Guard ฝั่ง Backend ต้องบังคับทั้ง role check และ ownership check (`assignedTo` = user id ที่ login — ดู Section 5 ข้อ 8)

---

## 5. กติกา Separation of Duty (สำคัญ — ต้องคุยกับ B ให้ตรงกันก่อนปิด)

1. **ConfigEngineer ห้ามอนุมัติ Config ของตัวเอง** (เดิม SW ก่อนแยก role — แก้ครั้งที่ 37) — คนที่กด Create/Import Config (ConfigEngineer) ต้องไม่ใช่คนเดียวกับที่กด Approve (Operation) แม้ในทางเทคนิคจะ login คนละ account อยู่แล้วก็ตาม แต่ Guard ต้องบล็อกที่ระดับ Role ไม่ใช่แค่ระดับ user id
2. **สถานะ Config ต้องไล่ตาม enum จริงบน `main`**: `draft` → `testing` → `approved` → `synced` (หรือ `rejected` แล้วย้อนกลับ `draft`) — ห้าม Guard/Service ข้าม state ใดๆ — **หมายเหตุสำคัญ:** enum ปัจจุบันยังไม่มีสถานะแยกระหว่างที่ ConfigEngineer ตัดสินใจผ่านแล้วกับที่ Operation อนุมัติแล้ว (ทั้งคู่ใช้ path ไปสู่ `approved` เดียวกัน) เป็น Gap ที่ต้องปิดก่อนเขียนโค้ดจริง (ดู 4.2)
3. **Override (ST/OT) ต้องบันทึกลง Audit Log ทุกครั้งแบบบังคับ** ไม่มีข้อยกเว้น เพราะเป็นการข้าม flow อนุมัติปกติ (ต่างจาก Config ปกติที่ผ่าน Approval Center อยู่แล้ว)
4. **Operation ปฏิเสธ Config → สถานะย้อนกลับไป `draft` เสมอ** (ตามที่ยืนยันไว้ใน Formal.md Section 3.1) ไม่มี Role ไหนสามารถ Override ขั้นตอนนี้ได้ นอกจาก ST/OT ที่ใช้ path "Override" แยกต่างหาก ซึ่งไม่ผ่าน Approval Center เลย
5. **Admin ไม่ใช่ Approver และไม่ใช่ Override** — สิทธิ์ Admin จำกัดเฉพาะ User/Role Management และ Decommission Device เพื่อคงหลัก Separation of Duty ไม่ให้ Admin กลายเป็น "ซูเปอร์ยูสเซอร์" ที่ผ่านทุกขั้นตอนได้คนเดียว (ถ้าทีมต้องการให้ Admin override ได้ในกรณีฉุกเฉินจริง ต้องระบุเพิ่มและใส่เหตุผลใน Audit Log)
   - **`SuperAdmin` ก็ไม่ใช่ Approver / ไม่ Override / ไม่ข้าม Separation of Duty เช่นกัน** — SuperAdmin อนุมัติ Config/Firmware/Campaign แทน Operation **ไม่ได้** แม้ในกรณีฉุกเฉิน (ถ้า Operation ไม่อยู่ = SuperAdmin ตั้ง Operation เพิ่มอีกคน ซึ่งทำได้อยู่แล้ว) · สิทธิ์พิเศษของ SuperAdmin จำกัดที่ อนุมัติคำขอลบ Config (auto delete-request), จัดการบัญชี Admin/SuperAdmin, แก้ role/permission เท่านั้น (มติ Sprint 1 review — `../09_Sprint1_Review_Decisions.md`)
6. **Auditor เป็น Read-only 100%** ทุกจอ ไม่มีข้อยกเว้น แม้แต่จอที่ ConfigEngineer/Operation เข้าถึงได้แบบ Read ก็ตาม — **ยกเว้น**การ mark-as-read บน Notification ของตัวเอง (U ใน "Notification Center") ซึ่ง**ไม่ใช่ข้อยกเว้นของกฎนี้** แต่ไม่เข้าข่ายกฎนี้ตั้งแต่ต้น เพราะเป็น action ส่วนตัวต่อ inbox ของตัวเอง ไม่ใช่การแก้ไขข้อมูลธุรกิจ (business data เช่น Config/Firmware/Campaign) ที่กฎ Read-only นี้ต้องการคุม — เทียบง่ายๆ คือเหมือนเปลี่ยนรหัสผ่านตัวเอง ก็ไม่ถือว่าขัดกับ Read-only เช่นกัน
7. **Decommission Device เป็นสิทธิ์ร่วมของ Operation และ Admin — ยืนยันกับทีมแล้ว** (ไม่ใช่ Assumption อีกต่อไป): Operation ทำได้เพราะเป็นคนดูแลสถานะกล่องประจำวัน, Admin ทำได้เพราะเป็นงานเชิงระบบ (device lifecycle) ทั้งสอง Role นี้เท่านั้นที่มีสิทธิ์ ไม่มี Role อื่นเพิ่มเติม
8. **ผู้ใช้ Mobile (Operation/ST/OT) เห็นเฉพาะข้อมูลของตัวเองในหน้าจอ Mobile** — Task/Change Request/Notification ที่ query ต้อง filter ด้วย user id ของตัวเอง ไม่ใช่แค่ซ่อน UI แต่ต้องกรองที่ Backend ด้วย (`GET /tasks` มี query param `assignedTo` อยู่แล้ว — Guard ต้อง**บังคับ**ค่านี้ให้เท่ากับ user id ที่ login ไม่ใช่ให้เลือกเองจากฝั่ง client; `GET /notifications` ต้องผูกกับ user จาก JWT โดยตรง ไม่รับ user id จาก client เลย) — กฎนี้ใช้กับทุก Role ที่ login ผ่าน Mobile เหมือนกัน ไม่ใช่เฉพาะ Role ใด Role หนึ่ง

---

## 6. Assumption / Gap ที่ยังไม่ยืนยัน — ต้องคุยกับ kittiphong (B) ก่อนปิด Matrix

- **enum สถานะ Config ไม่มีจุดแยกระหว่างขั้น ConfigEngineer กับขั้น Operation** (เดิม SW ก่อนแยก role — แก้ครั้งที่ 37) — ทั้งสองขั้นจบที่ `approved` เหมือนกันหมด ต้องตกลงว่าจะ (ก) เพิ่ม status ใหม่ (เช่น `configengineer_approved`) หรือ (ข) ใช้ field อื่นเก็บว่าใครกดผ่านขั้นไหนแทนการเพิ่ม enum
- **ขอบเขต ST vs OT**: ในเอกสารต้นทางระบุแค่ "Override เฉพาะ ST/OT" โดยไม่แยกรายละเอียด ในร่างนี้แบ่งให้ ST เน้นงานเทคนิค/Incident ระดับอาวุโส และ OT เน้นงานปฏิบัติการ (Task/Change Request) แต่ทั้งคู่ Override ได้เท่ากัน — ถ้าทีมต้องการแบ่งสิทธิ์ Override ให้ต่างกัน ต้องแก้ตารางส่วนที่ 2 และ 4
- **User/Role Management**: ยังไม่มีจอนี้ระบุไว้ใน Checkpoint Features ของแผน Sprint ใดเลย — เพิ่มเข้ามาในร่างนี้เพราะ Admin role ต้องมีอย่างน้อย 1 หน้าที่ใช้งานจริง ต้องตกลงว่าจะทำ Sprint ไหน
- **GPS_Data_Dictionary.xlsx (`CAMPAIGN_ASSIGNMENT.assigned_by`)**: ยืนยันเจ้าของแล้ว — **paveekornk (A) รับไปแก้เอง แยกเป็น PR ต่างหาก ไม่รวมกับ RBAC Matrix นี้** — **เริ่มลงมือแล้ว (แก้ครั้งที่ 26):** พบระหว่างรีวิว PR #144 (B, mobile Confirm Install) ว่า `Task.configId`/`deviceId`/`assignedTo` เป็น `null` หมดในทางปฏิบัติ เพราะไม่มีหน้าไหนป้อนข้อมูลจริงเข้ามา — **สรุปกับพี่เลี้ยงแล้วว่า Task Management (Web, #7) ยกเลิกถาวร ไม่ทำในรูปแบบไหนทั้งสิ้น** (ดู `web/src/app/(app)/_tasks/README.md`) การมอบหมายงานจริงจะเกิดจากขั้นตอน "มอบหมายผู้รับผิดชอบหน้างาน" ตอนสร้าง Campaign แทน (ตรงกับ concept `CAMPAIGN_ASSIGNMENT` เดิม) — เพิ่ม `model CampaignTarget` (เก็บ target device list จริงของ Campaign หนึ่งตัว) + `Task.campaignId` (nullable, `onDelete: SetNull`) กลับเข้า schema แล้ว (migration `20260911043203_add_campaign_target`, additive ล้วน) — **backend `campaign` module implement แล้ว (แก้ครั้งที่ 28):** `POST /campaigns` สร้าง Campaign+CampaignTarget[]+Task[] ใน transaction เดียว **ยังไม่ทำ:** Web Campaign Wizard UI — ตกลงร่วมกับ kittiphong (B) แล้วว่า Rollout strategy (canary/batch/auto-pause) ยังตัดออกในรอบแรกเหมือนเดิม (ดู comment เหนือ `model Campaign`)
- **`POST /config/{configId}/simulate` ยังไม่รองรับ "ช่างทดสอบอุปกรณ์ที่ติดตั้งจริง"** (พบระหว่างรีวิว Stage 3 โดย kittiphong): `SIMULATABLE_CONFIG_STATUSES` (ดู `config-status.ts`) อนุญาตแค่สถานะ `draft`/`testing` เพราะ endpoint นี้ออกแบบไว้สำหรับ SW dry-run Config **ระหว่างร่างอยู่** เท่านั้น — แต่ Config ที่ติดตั้งบนอุปกรณ์จริงหน้างานจะเป็น `approved`/`synced` เสมอ (ผ่านการอนุมัติมาแล้ว) ทำให้ช่าง (ST/OT ผ่าน Mobile) เรียก endpoint นี้เพื่อทดสอบสัญญาณจากอุปกรณ์ที่ติดตั้งจริงไม่ได้เลย (โดน 409 ทุกครั้ง) — เป็นคนละความหมายกับ dry-run: อันหนึ่งตรวจ field value ของ Config template อันหนึ่งควรตรวจการเชื่อมต่อจริงของอุปกรณ์เครื่องนั้น ต้องออกแบบ endpoint แยกต่างหากสำหรับเคสช่างหน้างาน (อาจต้องคุย mock/real mode ของมันเองแยกจาก `DEVICE_SIMULATOR_MODE`) — **ยังไม่แก้ใน Stage 3 นี้** เพราะผูกกับหน้า "ทดสอบสัญญาณ" ที่ B ออกแบบฝั่ง Mobile อยู่ — ติดตามและออกแบบร่วมกับ kittiphong (B) ต่อผ่านคอมเมนต์บน PR Stage 3 นี้ (ไม่เปิด issue แยก). **✅ ปิดแล้ว (แก้ครั้งที่ 19):** ทำเป็น endpoint แยก `POST /devices/{deviceId}/simulate-config` (ดู `docs/08_ConfigSimulator_Phase2_Proposal.md` + ตาราง 4.1) — รับ config `approved`/`synced` + device `installed`, reuse `DeviceSimulator` + `DeviceConnectionTester` เดิม, deviceModel/protocol mismatch เป็น `compatibilityCheck.passed:false` ใน 200 (ไม่ใช่ 409)
- **Notification เปลี่ยนเป็น FCM จริง — รอ A รีวิว**: kittiphong (B) เสนอเปลี่ยน Notification จาก in-app inbox เป็น push notification จริงผ่าน Firebase Cloud Messaging (Android/iOS/Web) — รายละเอียดเต็มดู `docs/05_Mobile_Notification_FCM.md` — ต้องการ schema ใหม่ (`DeviceToken`) ที่ยังไม่มีในระบบ **A ยังไม่ได้รีวิว**
- ~~**ตาราง join `CONFIG_DEFINITION_MODEL_SUPPORT`... ยังไม่ได้ออกแบบ**~~ **ปิดแล้ว (แก้ครั้งที่ 14)** — ดู Change Log: paveekornk (A) ออกแบบเป็น `ConfigFieldDefinitionModelSupport` และ implement Semantic Validation ครบแล้ว
- **`device-status` — บางส่วนปิดแล้ว (แก้ครั้งที่ 51, issue #181), บางส่วนยังเปิดอยู่** เดิมพบระหว่างออกแบบ Firmware Repository (2026-09-11): `Firmware.deviceUpdateStatus`/สถานะ "อุปกรณ์ใช้ Config/Firmware เวอร์ชันล่าสุดหรือยัง" ที่วางแผนไว้ใน `01_GPS_Build_Reference.md` §3, §6 ต้องมี 2 ส่วน: (1) **desired state** — มีข้อมูลบางส่วนอยู่แล้วผ่าน `Task.configId`/`deviceId` (จาก Campaign) แค่ยังไม่ถูกดึงมาคำนวณเป็นสถานะต่ออุปกรณ์ **ยังไม่แก้** (2) **actual state** — กล่องคุยกับระบบเดิมโดยตรง ไม่คุยกับเราเลย ทาง "ช่างยืนยันเองแทนคำยืนยันอัตโนมัติจากกล่อง" implement แล้วครบทั้งคู่: "Confirm Install" (Config, มีอยู่ก่อนแล้ว) + **"Confirm Firmware Install"** (`POST /devices/{deviceId}/confirm-firmware-install`, แก้ครั้งที่ 51) — ทั้งคู่เป็นแค่**บันทึกว่ามีการยืนยันเกิดขึ้น (AuditLog)** ไม่ได้คำนวณ `deviceUpdateStatus` จริงตามที่ตั้งใจไว้แต่แรก (มติ issue #181 ข้อ 3 — ตัดสินใจเจตนาจำกัด scope ไม่ให้ใหญ่เกินไปรอบเดียว) **ยังไม่ปิด:** การคำนวณ `deviceUpdateStatus` จริงจากการยืนยันเหล่านี้ (ยังคงเป็น `unknown` เสมอ) + Mobile UI ฝั่ง B ที่เรียก endpoint ใหม่นี้ + ระดับที่ยืนยันอัตโนมัติจริงจากกล่องซึ่งยังต้องรอระบบเดิมเปิดทาง (TBD เดียวกับที่บล็อก `config-sync-writer` production — `01_GPS_Build_Reference.md` §8 แถวที่ 1) — **Deviation จาก GPS_Config_Firmware_Center_Design.pdf §10.2-10.3 โดยตั้งใจ**: PDF ออกแบบ Workflow Firmware เป็น state machine อัตโนมัติเต็มรูปแบบที่ backend คุยกับอุปกรณ์เองทุกขั้น ทำไม่ได้เพราะระบบเป็น PULL model — endpoint นี้เป็นแค่ placeholder ระดับที่ทำได้ในขอบเขตฝึกงาน

---

## Change Log

| วันที่ | ผู้แก้ไข | รายละเอียด |
|---|---|---|
| 2026-08-28 | paveekornk | สร้างฉบับร่างแรก ตาม Gap ที่ระบุใน `../planning/03_GPS_Detailed_Build_Steps.md` Phase 0 ข้อ 4 — ยืนยันคำเต็ม ST=Senior Technician, OT=Operation-Technician กับ paveekornk แล้ว |
| 2026-08-28 | paveekornk | แก้ครั้งที่ 2 โดยอ้างอิง openapi.yaml จาก branch `docs/rbac-matrix` (ภายหลังพบว่ามีคอมมิตทดลองที่ไม่ตรงกับ main — ข้อมูลบางส่วนคลาดเคลื่อน) |
| 2026-08-28 | paveekornk | แก้ครั้งที่ 3 — `git pull` main ใหม่แล้วยึด `docs/api/openapi.yaml` บน `main` จริงเป็นหลัก: ย้อน role enum กลับเป็น `[SW, Operation, ST, OT, Auditor, Admin]` (ยังไม่มี FieldTechnician จริง), ย้อนสถานะ Config กลับเป็น `[draft, testing, approved, rejected, synced]` (ไม่มี sw_approved/operation_approved แยก), เพิ่ม endpoint `/notifications` และ `/notifications/{id}/read` ที่ kittiphong เพิ่มเข้ามาใหม่เข้าตาราง 4.1 |
| 2026-08-28 | paveekornk | แก้ครั้งที่ 4 — ตอบ Comment รีวิวของ kittiphong 5 ข้อ: (1) ย้ายสิทธิ์ Device Registration จาก FieldTechnician → Operation/ST/OT ทั้งใน Section 1 (platform ST/OT เพิ่ม Mobile), Section 3, และตาราง 4.2, (2) ย้าย Decommission Device จาก Assumption (ข้อ 6) มาเป็นกติกายืนยันแล้วใน Section 5 ข้อ 7, (3) เพิ่ม footnote อธิบายว่าทำไม Auditor mark-as-read บน Notification ไม่ขัดกับกฎ Read-only (ไม่ใช่ข้อยกเว้น แต่ไม่เข้าข่ายกฎตั้งแต่ต้น), (4) merge/rebase `origin/main` เข้า branch ก่อน push เวอร์ชันนี้ตามที่ขอ, (5) บันทึกยืนยันว่า paveekornk รับไปแก้ `GPS_Data_Dictionary.xlsx` (`CAMPAIGN_ASSIGNMENT.assigned_by`) เอง แยก PR ต่างหาก |
| 2026-08-28 | paveekornk | แก้ครั้งที่ 5 — ตาม [PR #13](https://github.com/dtc-gps-team/gps-config-firmware-center/pull/13) (merged): **ตัด Role `FieldTechnician` ออกทั้งเอกสาร** เปลี่ยนเป็น Operation/ST/OT ให้ครบทุกจุดที่เหลือ (Section 1 role table + note, Section 3 ทั้งตาราง, ตาราง 4.1 ทุกแถวที่เคยอ้างถึง, ตาราง 4.2 แถว `/change-requests`, Section 5 กติกาข้อ 8) — ปิด Gap เรื่อง FieldTechnician ที่เคยเปิดไว้ใน Section 6 ไปด้วย เพราะไม่มี Role นี้แล้ว คงเหลือ Open question อื่นตามที่ทีมแจ้ง: ขอบเขต ST vs OT override scope, ใครเป็นคนสร้าง Task หลัก, Sprint ของ User Management, และสถานะ `GPS_Data_Dictionary.xlsx` (`CAMPAIGN_ASSIGNMENT.assigned_by`) ที่ paveekornk รับผิดชอบแต่ยังไม่ได้ลงมือแก้ |
| 2026-08-28 | paveekornk | แก้ครั้งที่ 6 — ตอบ comment รีวิว PR #15 ของ kittiphong: ลบเครื่องหมาย `*` ที่ลอยค้างอยู่ท้าย "Web + Mobile" ในช่อง platform ของ ST/OT (Section 1) ออก เพราะ footnote ที่เคยผูกกับ `*` ถูกลบไปแล้วตอนแก้ FieldTechnician ในรอบ 5 คำอธิบายเรื่อง ST/OT ใช้ Mobile ด้วยยังคงอยู่ในย่อหน้าใต้ตารางตามเดิม (อ้างอิง PR #13) ไม่ต้องเพิ่ม footnote ใหม่ |
| 2026-08-28 | kittiphong | แก้ครั้งที่ 7 — **ปิด open question: Task creator = Operation** ตัดสินใจโดย kittiphong (B) เจ้าของ module `task` ตามแพทเทิร์น Operation สั่งงาน/อนุมัติ, ST/OT ปฏิบัติงาน: (1) เพิ่มตาราง 4.3 รายละเอียดสิทธิ์ module `task`, (2) Section 2 แถว Task Management — OT จาก `C, R, U` เหลือ `R` (ST/OT ไม่สร้าง/จัดการ Task บน Web แก้ `status` งานตัวเองผ่าน Mobile), (3) ตาราง 4.1 — `/tasks` POST `createTask` เหลือ `Operation` เท่านั้น, `/tasks/{taskId}` PATCH `updateTask` = Operation ทุก field / ST-OT เฉพาะ field `status` ของงานตัวเอง, (4) ลบรายการ "Task Management ฝั่งใครเป็นคนสร้าง" ออกจาก open question list (Section 6) |
| 2026-08-28 | kittiphong | แก้ครั้งที่ 8 — เก็บ inconsistency ภายในเอกสารเองที่เกิดจากรอบ 7: (1) ตาราง 4.1 `GET /tasks` (`listTasks`) — เดิมยังบอกว่า "Operation/ST/OT ถูกกรองเฉพาะ `assignedTo` = ตนเอง" ซึ่งขัดกับ 4.3 ที่ให้ Operation จัดการ Task ทั้งหมด → แก้เป็น "ST/OT ที่ใช้ Mobile ถูกกรอง, Operation เห็นทุก Task" ให้ตรงกับ `getTask`/`updateTask` ในตารางเดียวกัน, (2) ตาราง 4.3 แถว "ดู Task ทั้งหมด" ระบุครบว่ารวม SW (read-only) + Operation (จัดการได้) ไม่ใช่แค่ Auditor/Admin, (3) Section 2 แถว Task Management เพิ่ม footnote ¹ ให้ ST/OT ว่าแก้ `status` งานตัวเองผ่าน Mobile — ไม่มีการเปลี่ยนสิทธิ์ใดๆ เป็นการทำให้ข้อความในเอกสารสอดคล้องกันเท่านั้น |
| 2026-09-01 | paveekornk | แก้ครั้งที่ 9 — ตอบ comment รีวิว PR #46 (Stage 1 CRUD, #26) ของ kittiphong ข้อ 2: **ปิด open question ที่ไม่เคยถูกถามมาก่อน — Config ไม่ scope ตาม creator** ยืนยันโดย paveekornk (A) เจ้าของ module `config`: SW ทุกคนแก้/ลบ Config ที่ยังเป็น `draft` ร่วมกันได้ ไม่แยกเป็นของใครของมัน (1) Section 2 แถว Config Editor เพิ่ม footnote ² อธิบายการตัดสินใจนี้ (2) โค้ดจริง (`ConfigService.update`/`remove`) ทำแบบนี้อยู่แล้วตั้งแต่ Stage 1 โดยไม่ได้ filter `createdBy` — เอกสารรอบนี้แค่ตามให้ทันโค้ด ไม่ได้เปลี่ยนพฤติกรรม |
| 2026-09-02 | paveekornk | แก้ครั้งที่ 10 — เริ่ม Stage 3 (Simulate) ของ issue #26: Section 2 แถว Config Simulation ระบุชัดว่า `simulate` ไม่เปลี่ยนสถานะ Config เลย (คืนแค่ผลทดสอบ) ตรงกับโค้ดจริง — เรื่อง open question "endpoint สำหรับ SW ปักผลตัดสินใจผ่าน/ไม่ผ่าน" (Section 6 ข้อ 1) ยังไม่แตะในรอบนี้ (เดิมเคยลองเสนอทางเลือกไว้ในดราฟต์นี้ แต่ถอนออกตาม comment รีวิวของ kittiphong บน PR Stage 3 — endpoint ที่ยังไม่ได้ให้ B รีวิว design ไม่ควรมากับ PR ของ feature อื่น จะไปเสนอแยกเป็น PR ต่างหากแทน) |
| 2026-09-02 | paveekornk | แก้ครั้งที่ 11 — ตอบ comment รีวิวรอบ 2 ของ kittiphong บน PR Stage 3 ข้อ 7: เพิ่ม Gap ใหม่ใน Section 6 — `simulate` (`SIMULATABLE_CONFIG_STATUSES`) รองรับแค่สถานะ `draft`/`testing` (บริบท SW dry-run ตอนร่าง) ไม่รองรับ `approved`/`synced` (บริบทช่างทดสอบอุปกรณ์ที่ติดตั้งจริงหน้างานผ่าน Mobile) ต้องออกแบบ endpoint แยกต่างหาก — ยังไม่แก้ใน Stage 3 นี้ — ติดตามและออกแบบร่วมกับ kittiphong (B) ต่อผ่านคอมเมนต์บน PR Stage 3 นี้ (ไม่เปิด issue แยก) เพราะผูกกับหน้า "ทดสอบสัญญาณ" ฝั่ง Mobile ที่ B ดูแลอยู่ |
| 2026-09-02 | kittiphong | แก้ครั้งที่ 12 — เริ่ม task #12 (Config Definition Lookup, แผน Agile แถว 12) โดย kittiphong (B): (1) ตาราง 4.1 เพิ่มแถว `GET /config-definitions` (`listConfigDefinitions`) resource ใหม่ `config-definition` action `Read` — SW/Operation/ST/OT (catalog อ่านอย่างเดียว) (2) Section 6 เพิ่ม Gap ใหม่ — ตาราง join `CONFIG_DEFINITION_MODEL_SUPPORT` (ผูก field definition กับ deviceModel/protocol) ยังไม่ได้ออกแบบ **A ยังไม่ได้รีวิว** ต้องคุยกับ A ก่อนสร้าง — task #12 จงใจทำแค่ catalog อ่านอย่างเดียว (seed `ConfigFieldDefinition` เริ่มจาก field `APN` + endpoint `GET /config-definitions` + spec) ยังไม่แตะ validation ใน `config.service.ts`/`device-simulator.ts` |
| 2026-09-02 | kittiphong | แก้ครั้งที่ 13 — implement `POST /devices/{deviceId}/test-connection` (Device module ใหม่) ตาม spec ที่ตกลงกับ paveekornkwork-dev (A) บนคอมเมนต์ PR #52 (`docs/06_Device_Connection_Test_Spec.md`): (1) ตาราง 4.1 เพิ่มแถว `testDeviceConnection` resource ใหม่ `device-connection-test` action `Read` — **ST/OT เท่านั้น** (A ยืนยันไม่เปิด SW/Operation) (2) `MockDeviceConnectionTester` คืน `signalStrength` คงที่ (-65 dBm) ให้ Mobile ทำ UI ได้ตั้งแต่ mock — env `DEVICE_CONNECTION_TEST_MODE` แยกจาก `DEVICE_SIMULATOR_MODE` (3) 409 ถ้า Device ยังไม่ `installed`, 404 ถ้าไม่พบ `deviceId` — **จงใจตัด `GET /devices/{deviceId}/status` ออกจากรอบนี้** (มีแต่ spec ยังไม่มีโค้ด, การคำนวณ configStatus/firmwareStatus ต้องออกแบบใหม่เพราะ Device ไม่มี FK ตรงไป Config/Firmware — รอ PR แยก) |
| 2026-09-03 | paveekornk | แก้ครั้งที่ 14 — implement **Semantic Validation** (Phase 1, #26) เจ้าของ module `config`/`config-definition`: ปิด Gap ที่ kittiphong เปิดไว้ในแก้ครั้งที่ 12 (ตาราง join `CONFIG_DEFINITION_MODEL_SUPPORT` ยังไม่ได้ออกแบบ) — ตัดสินใจร่วมกับ kittiphong (B) และพี่เลี้ยง 2569-09 มีดังนี้: (1) เพิ่ม model `ConfigFieldDefinitionModelSupport` ผูก `ConfigFieldDefinition` กับ (deviceModel, protocol) ได้หลายคู่ — สร้างพร้อมกันตอน `createConfigDefinition` เท่านั้น ไม่มี endpoint แก้แยก (2) ตาราง 4.1 เพิ่มแถว `POST /config-definitions` (`createConfigDefinition`) resource `config-definition` action `Create` — **SW เท่านั้น ไม่มีขั้นตอนอนุมัติ** เพราะ field ที่มีปัญหาจริงจะโดนจับตอนเอาไปสร้าง Config Template แล้วเข้า simulate/decide/approve อยู่ดี (3) field ที่ไม่มีนิยามในคลัง หรือมีนิยามแต่ไม่รองรับ deviceModel/protocol ที่ระบุ → **block ทั้งคู่ (409/400 แบบ hard block ไม่มี warn-only)** ไม่มีทางลัดให้ field "พิเศษเฉพาะลูกค้า" ข้าม validation ได้ (4) การกรอง "รุ่นไหนใช้ parameter ตัวไหน" ทำที่ฝั่ง UI ตอนสร้าง Config Template โดยอ่านจาก `supportedModels` ของแต่ละ field — ไม่ต้องเพิ่ม endpoint filter แยก (5) เจตนาไม่ทำตอนนี้ (deferred, ไม่ใช่ Gap): ระดับความเข้มงวด 2 ระดับ (`syntactic_only` vs `semantic` ตาม `../planning/03_GPS_Detailed_Build_Steps.md`) — YAGNI จนกว่าจะมี use case จริง เป็น additive migration เพิ่มทีหลังได้โดยไม่กระทบของเดิม — ดู `config-definition.service.ts`/`config.service.ts` สำหรับโค้ดจริงและ `docs/api/openapi.yaml` (v3.11) สำหรับ spec |
| 2026-09-04 | paveekornk | แก้ครั้งที่ 15 — implement **Config Version History** (Phase 1 ข้อ 9, #26) เจ้าของ module `config`: (1) `ConfigService.approve()` เขียน `ConfigVersion` snapshot (append-only, `versionNumber` running ต่อ config) ใน `$transaction` เดียวกับการเปลี่ยน status → `approved` — `reject()` ไม่เขียน · ยังไม่ snapshot ตอน `synced` (Phase 2) (2) ตาราง 4.1 เพิ่ม 2 แถว `GET /config/{configId}/versions` (`listConfigVersions`) + `/versions/{versionNumber}` (`getConfigVersion`) — **ใช้ resource `config` action `Read` เดิม ไม่แยก resource** ต่างจาก `simulate`/`decide`: version เป็นแค่ snapshot ของ `fields` ที่ Read config เห็นอยู่แล้ว และ Auditor (compliance) ต้องดูประวัติได้ (3) schema: เพิ่ม `ConfigVersion.versionNumber Int` + `@@unique([configId, versionNumber])` + `onDelete: Restrict` — additive migration (ตาราง ConfigVersion ว่างทุก env) (4) เป็นฐานของ Rollback ใน Phase 4 (#28) — `getConfigVersion` คืน `fields` เต็มให้ rollback ดึงไปสร้าง config ใหม่ — ดู `docs/04_Phase1_A_ConfigWorkflow.md` §Stage 5 |
| 2026-09-04 | paveekornk | แก้ครั้งที่ 16 — ปิด item "Validation strictness metadata" ของ Checkpoint Phase 1 ข้อ 6 (#26): **`ConfigFieldDefinition.unknownSpec` คือ Metadata "รู้กฎ vs รู้แค่ Data Type" ที่ Checkpoint ต้องการ** — `unknownSpec: false` = รู้กฎครบ (semantic), `true` = รู้แค่ชื่อ + dataType (syntactic_only) · **ไม่เพิ่ม field `validationLevel` แยก** · deferred อย่างเป็นทางการ (ติด blocker เดียวกับ #68 — ยังไม่มีสเปกฟิลด์จริง): (1) `validateFields()` ยังไม่ branch ตาม `unknownSpec` (2) ยังไม่มี field เก็บกฎ semantic รายฟิลด์ (`minValue`/`maxValue`/`pattern`) (3) กฎ "Timeout/Interval ห้ามติดลบ" ยังอยู่ใน `MockDeviceSimulator` — ดู `docs/04_Phase1_A_ConfigWorkflow.md` §Validation strictness |
| 2026-09-04 | paveekornk | แก้ครั้งที่ 17 — implement **`POST /devices/{deviceId}/apply-config`** (A1 — ตามที่ kittiphong (B) ขอไว้ให้ Mobile ทำจอ "Confirm Install" #26 Sprint 3): ช่างหน้างานใส่ Config ที่อนุมัติแล้วเข้าอุปกรณ์ที่ติดตั้งจริง (1) ตาราง 4.1 เพิ่มแถว `applyConfigToDevice` resource ใหม่ `device-config-apply` action `Read` — **ST/OT เท่านั้น** (mirror `device-connection-test`) · action `Read` เพราะ **ไม่ persist อะไรเลย** (fire-and-forget — B ยืนยัน scope: ยังไม่มีจอ "config ล่าสุดของกล่อง", ถ้าจะเก็บประวัติค่อยเปิด scope ใหม่ + เพิ่ม model) (2) `MockConfigApplier` ตรวจค่าเบื้องต้น (field ≥ 1, Timeout/Interval ไม่ติดลบ — กฎชุดเดียวกับ `MockDeviceSimulator`) แล้วคืน `{ applied, details, appliedAt }` · **ไม่มีสถานะอุปกรณ์ใน response** เพราะกล่องรับค่าตอนเปิดเครื่องครั้งถัดไปเท่านั้น (Build Reference §4.2 — ยืนยันกับ B) (3) 404 (device/config ไม่พบ) · 409 (device ยังไม่ `installed` / config ยังไม่ `approved`,`synced` / คนละ deviceModel-protocol) (4) env `DEVICE_CONFIG_APPLY_MODE` แยก (mirror `DEVICE_CONNECTION_TEST_MODE`) — `real` ยัง throw รอ `config-sync-writer` #32 (5) seed `Device` ตัวอย่าง 5 ตัว (`DEV-000x` — รูปแบบเลขเครื่องจริงรอ Device Registration Phase 5) · **ไม่แตะ schema.prisma** (ไม่มี model/field/migration ใหม่) — ดู `backend/src/device/config-applier.ts` + `docs/api/openapi.yaml` (v3.13) · **ยังเปิด:** apply-config ต่อ `config-sync-writer` ไหม → คุยใน #32 |
| 2026-09-04 | kittiphong | แก้ครั้งที่ 18 — **Push Notification (FCM) groundwork** (Sprint 3 #17, PR A ของแผน 4-PR: A model+endpoint / B ส่งจริงผ่าน FCM / C Mobile setup+registration / D Mobile handle push+deep link) เจ้าของ module `notification` (B): (1) schema เพิ่ม model `DeviceToken` (`userId`, `token @unique`, `platform`, timestamps, `@@index([userId])`, FK `onDelete: Cascade` — เบี่ยงจาก implicit Restrict ของ Notification/Task โดยตั้งใจ เพราะ device token เป็น client-registration ชั่วคราว ไม่มีค่าเชิง audit) — additive migration `20260904094500_add_device_token` (2) ตาราง 4.1 เพิ่ม 2 แถว `POST /notifications/device-tokens` (`registerDeviceToken` — upsert ตาม token) + `DELETE /notifications/device-tokens?token=` (`deleteDeviceToken` — IDOR-safe `deleteMany where {token,userId}` + 404) — **JwtAuthGuard อย่างเดียว ไม่เพิ่ม resource/PermissionGuard** เพราะ self-scoped ล้วน (`userId` จาก JWT เสมอ) ทุก Role ที่ login ผ่าน Mobile ต้องลงทะเบียน token ของตัวเองได้ ไม่มี Role ไหนควรถูกห้าม — เหมือน `/notifications` GET/read (Section 5 ข้อ 8) (3) `.env.example`: แทน `FCM_SERVER_KEY` (FCM Legacy HTTP API — Google deprecate 2023 → shut down 2024) ด้วย `FCM_SERVICE_ACCOUNT_PATH` (FCM HTTP v1 ใช้ Service Account JSON) — **เบี่ยงจาก `../planning/01_GPS_Build_Reference.md` ที่อ้าง server key โดยตั้งใจ** เพราะ API เดิมตายแล้ว ใช้ server key เดี่ยวไม่ได้อีก (4) **รอบนี้ยังไม่ส่ง push จริง** — `NotificationService.send()` mode `fcm` ยัง throw เหมือนเดิม, ไม่เพิ่ม `firebase-admin` (เก็บไว้ PR B) — ดู `backend/src/notification/` + `docs/api/openapi.yaml` (v3.14) |
| 2026-09-07 | paveekornk | แก้ครั้งที่ 19 — implement **`POST /devices/{deviceId}/simulate-config`** (Sprint 2 #13 · config_simulator Phase 2 · design ตกลงกับ kittiphong (B) บน PR #92 → `docs/08_ConfigSimulator_Phase2_Proposal.md`): ช่างหน้างาน (ST/OT ผ่าน Mobile) dry-run เช็คความพร้อม Config ก่อน `applyConfigToDevice` จริง (1) ตาราง 4.1 เพิ่มแถว — **reuse resource `device-connection-test` action `Read`** (ST/OT เท่านั้น) ไม่ seed เพิ่ม เพราะเช็คสัญญาณอุปกรณ์จริงเหมือนกัน (2) รวม 3 ส่วนเป็นผลเดียว `DeviceSimulateConfigResult`: `configCheck` (reuse `DEVICE_SIMULATOR` เดียวกับ `/config/{id}/simulate`) + `compatibilityCheck` (deviceModel/protocol Config ตรงกับ Device ไหม) + `connectionCheck` (reuse `DEVICE_CONNECTION_TESTER` เดียวกับ `/test-connection`) (3) precondition mirror `apply-config`: 404 (device/config ไม่พบ) · 409 (device ยังไม่ `installed` / config ยังไม่ `approved`,`synced`) (4) **ต่างจาก `apply-config`:** deviceModel/protocol mismatch เป็น `compatibilityCheck.passed:false` ใน 200 (ไม่ใช่ 409) เพราะเป็น readiness check ต้อง report ให้ช่างเห็น — **รอ B ยืนยันบน PR** (5) **ไม่แตะ schema.prisma** · env reuse `DEVICE_SIMULATOR_MODE` + `DEVICE_CONNECTION_TEST_MODE` (ไม่มี env ใหม่) · ปิด open question Section 6 เรื่อง endpoint ช่างหน้างาน — ดู `backend/src/device/simulate-config-result.ts` + `docs/api/openapi.yaml` (v3.15) |
| 2026-09-08 | paveekornk | แก้ครั้งที่ 20 — **มติ Sprint 1 review** (`../09_Sprint1_Review_Decisions.md`, PR #99): (1) Section 1 เพิ่ม Role `SuperAdmin` — = Admin + อนุมัติคำขอลบ Config + จัดการบัญชี Admin/SuperAdmin + แก้ role/permission · **ไม่ข้าม Separation of Duty** (อนุมัติ Config/Firmware/Campaign แทน Operation ไม่ได้) · ต้องมี SuperAdmin ≥ 1 เสมอ · ทุก action ลง Audit Log (2) Section 5 ข้อ 5 เพิ่มย่อหน้า SuperAdmin ไม่ข้าม SoD (3) header ตาราง Section 1 แก้ "enum" → `Role.code` (Role เป็นตาราง RBAC ตั้งแต่ PR #34) · **ยังไม่ finalize** Section 2/4: สิทธิ์ resource ใหม่ (`config-deletion`, `admin-management`, `role-management`) + seed grants ไปกับ proposal `docs/11` (Config Deletion + SuperAdmin, Sprint 3) · CLAUDE.md §Role Enum + §Audit Pattern + `GPS_Data_Dictionary.xlsx` ROLE table แก้คู่กันใน PR เดียวกันนี้ |
| 2026-09-09 | paveekornk | แก้ครั้งที่ 21 — **`docs/11` Part B1 (SuperAdmin seed) — ปลดล็อก Part A ของทีม B** เจ้าของ module `auth` (A): (1) `seed.ts` — เพิ่ม Role `SuperAdmin` (`INITIAL_ROLES`) + user ทดสอบ `superadmin.test` + grant = **ทุกสิทธิ์ที่ Admin มี** (derive อัตโนมัติจาก grant ของ Admin กันหลุด sync) **+** `config-deletion` (Read, Approve) seed ล่วงหน้าให้ handoff Part A ไหลลื่น — **ไม่มี migration** (Role เป็นตาราง, `code` เป็น free string) (2) Section 2 เพิ่มคอลัมน์ `SuperAdmin` (ทุกแถว = Admin) + 2 แถวสิทธิ์เฉพาะ SuperAdmin (`config-deletion`, `admin-management`/`role-management`) + footnote ³ (สถานะ Part B1/A/B2) (3) Section 4.2 เพิ่ม 3 แถว endpoint ที่รอ (`/config/{id}/deletion-requests`, `/admin/accounts`, `/roles`) + หมายเหตุ `/users` filter `role.code NOT IN ('Admin','SuperAdmin')` (4) `schema.prisma` — comment `Role.code` เพิ่ม SuperAdmin (comment-only ไม่ใช่ schema change) · **ยังไม่ทำ (Part B2, ทีม A):** endpoint + guard `admin-management`/`role-management` + logic "Admin แตะบัญชี Admin/SuperAdmin ไม่ได้" + กติกา SuperAdmin ≥ 1 — grant 2 resource นี้ seed พร้อม endpoint · **Part A (ทีม B):** `config-deletion` flow — grant มีรอแล้ว |
| 2026-09-09 | kittiphong | แก้ครั้งที่ 22 — implement **`docs/11` Part A (คำขอลบ Config อัตโนมัติ)** เจ้าของ flow (B): (1) `schema.prisma` — model `ConfigDeletionRequest` (`configId` FK `onDelete: Cascade`, `reviewedBy` FK User nullable, enum `ConfigDeletionRequestStatus` = pending/approved/rejected/cancelled, `@@index([configId])`/`@@index([status])`) + `Config.deletedAt DateTime?` (soft-delete) + relations — migration additive `20260909091727_add_config_deletion_request` (ตารางใหม่ว่าง + คอลัมน์ nullable ไม่ต้อง backfill) · **ไม่แตะ enum NotificationType** (2 ค่า `config_deletion_pending`/`config_deletion_grace` มาแล้ว PR #121) (2) `+ @nestjs/schedule` + `ScheduleModule.forRoot()` ใน `app.module.ts` (ยังไม่มี module ไหนใช้ `@Cron`) (3) module ใหม่ `src/config-deletion/` — service + controller + `@Cron` job (ตี 1 ทุกวัน) : job กวาด Config ตามเกณฑ์ §2 ครบ 7 ข้อ (draft/rejected · ไม่ผูก Task/Campaign/Incident · `updatedAt` เก่ากว่า 90 วัน · ไม่มีคำขอ pending ค้าง · ไม่อยู่ใน cooldown 90 วันหลังเคยถูก reject) → สร้างคำขอ `pending` + ยิง `config_deletion_pending` (SuperAdmin ทุกคน) + `config_deletion_grace` (ผู้สร้าง) แบบ never-throws (mirror `task.service.ts`) — **job ไม่เขียน `AuditLog`** (ไม่มี real actor · ไม่คิด pattern "system actor" ใหม่) (4) 4 endpoint (ตาราง 4.1): `GET /config-deletion-requests?status=` (`config-deletion` Read · default pending) · `POST /config-deletion-requests/{id}/approve` (soft delete Config + AuditLog `action: approve` · 409 ถ้าไม่ pending) · `POST /config-deletion-requests/{id}/reject` (body `{note}` → `decisionNote` + AuditLog `action: reject` · ไม่แตะ Config · เข้า cooldown 90 วัน) · `POST /config/{configId}/deletion-requests/keep` (`config` Update — SW · pending → `cancelled` + reset นาฬิกา `Config.updatedAt` + AuditLog `action: keep`) — **path `keep` ต่างจาก docs/11 §5** (`/config-deletion-requests/{id}/keep`) โดยตั้งใจ: key ด้วย config id เพราะ SW/ผู้สร้างได้ notification เรื่อง "Config X" ไม่รู้ request id + ใช้สิทธิ์ `config` Update ที่ SW มีอยู่แล้ว (5) `config.service.ts` — เติม `deletedAt: null` ใน `findAll` + เช็คหลัง `findUnique` ใน `findOne` (soft-deleted = 404) · **ไม่แตะ `remove()`** (hard delete draft ของ SW เป็นฟีเจอร์คนละอัน) (6) `openapi.yaml` (v3.16) — tag `config-deletion` + 4 path + schema `ConfigDeletionRequest` · `redocly lint` ผ่าน (7) **ตีความ spec ที่ต้องเลือกเอง** — ดู PR description: "grace period" = state เดียว (คำขอโผล่ในคิว SuperAdmin ทันที, "7 วัน" เป็นแค่ข้อความเตือนผู้สร้าง ตาม §10 ข้อ 2) · "reset นาฬิกา" ใน keep = แตะ `Config.updatedAt` (§3) · `keep` ไม่เซ็ต `reviewedBy` (นั่นหมายถึง SuperAdmin ที่ตัดสิน — ใครกด keep ดูจาก AuditLog) · **Part B2 (admin-management/role-management, ทีม A) ไม่เกี่ยวกับ PR นี้** — ดู `backend/src/config-deletion/` + `docs/api/openapi.yaml` (v3.16) |
| 2026-09-09 | paveekornk | แก้ครั้งที่ 23 — implement **`GET /devices` + `GET /devices/{deviceId}`** (Sprint 2 #11 · Device Search / Device Detail) เจ้าของ module `device` (A): (1) ตาราง 4.1 เพิ่ม 2 แถว — **reuse resource `devices` action `Read`** ที่ seed ให้ทุก Role อยู่แล้ว (`prisma/seed.ts` — เดิมเตรียมไว้ให้ `getDeviceStatus` ที่ยังไม่ implement) ไม่ seed เพิ่ม · ตรงกับ Section 2 แถว "Device Search / Device Detail" = R ทุก Role (2) `GET /devices` — filter optional `search` (match `deviceId`+`simNumber` contains/insensitive) / `deviceModel` / `protocol` / `status` · ไม่มี paging (3) `GET /devices/{deviceId}` — key ด้วย `Device.deviceId` (เลขเครื่องจริง) ไม่ใช่ `Device.id` · 404 ถ้าไม่พบ · คืนแค่ field ของ record Device เอง — **ยังไม่ implement `GET /devices/{deviceId}/status`** (Device ไม่มี FK ไป Config/Firmware) (4) **ไม่แตะ schema.prisma** · `openapi.yaml` (v3.17) เพิ่ม 2 path + schema `Device` — ดู `backend/src/device/device.controller.ts` |
| 2026-09-10 | kittiphong | แก้ครั้งที่ 24 — implement **`GET /incidents` + `GET /incidents/{id}`** (Sprint 2 · read-only rollout) — cross-boundary: โมดูล `incident` เป็นของ A, B implement ตาม contract ที่ตกลงกัน (PR #139, รอ A รีวิว): (1) ตาราง 4.1 เพิ่ม 2 แถว + ตาราง 4.2 แถว `/incidents` mark GET ว่า implement แล้ว (เหลือ POST/PATCH) — **resource `incidents`** (พหูพจน์ ตั้งชื่อตาม path เหมือน `devices`/`tasks`/`notifications`) action `Read` **grant ให้ทุก Role** (`prisma/seed.ts` เพิ่ม 1 seed row `...ALL_ROLE_CODES.map(...)` — Role/RolePermission เป็นตาราง ไม่ต้อง migration) · ตรงกับ Section 2 แถว "Incident & Rollback" = R ทุกคอลัมน์ (SW/Operation/ST/OT/Auditor/Admin/SuperAdmin) อยู่แล้ว (2) `GET /incidents` — filter optional `status`/`relatedConfigId`/`relatedFirmwareId` · เรียง `createdAt desc` · ไม่มี paging (MVP เดียวกับ `devices`) (3) `GET /incidents/{id}` — 404 ถ้าไม่พบ · `Incident.id` เป็น `text` ไม่ใช่ `@db.Uuid` → id ผิดรูปแบบ = `findUnique` คืน null = 404 (ไม่ใช้ `ParseUUIDPipe`, mirror `DeviceController` กับ `deviceId`) (4) **ยังไม่มี Create/Update/Rollback ผ่าน API** — Create เป็น auto จาก config-sync-writer (`createFromSyncFailure`, ไม่แตะ) · Update (ST แก้เชิงเทคนิค / Operation สั่ง Rollback) รอ Rollback flow จริง (Sprint Checklist แถว 28) (5) **ไม่แตะ schema.prisma** · `openapi.yaml` (v3.18) เพิ่ม tag `incident` + 2 path + schema `Incident` — ดู `backend/src/incident/incident.controller.ts` |
| 2026-09-10 | paveekornk | แก้ครั้งที่ 25 — **Approval Center (Sprint 3 #19) — เจาะจงผู้อนุมัติ + endpoint list users** เจ้าของ `config`/`auth` (A): (1) `schema.prisma` — `Config.suggestedApproverId String?` (nullable FK → User, `ON DELETE SET NULL` — mirror `ConfigDeletionRequest.reviewedBy`) · migration additive `20260910092957_add_config_suggested_approver` (คอลัมน์ nullable ไม่ต้อง backfill) (2) ตาราง 4.1 เพิ่ม `POST /config/{configId}/decide` (เดิมตกหล่นจาก 4.1 ตั้งแต่ Stage 4) + `GET /users` · `decideConfig` รับ body `suggestedApproverId` optional (เฉพาะ passed:true) — SW เจาะจง Operation ที่อยากให้ดูเป็นพิเศษ · backend เช็คว่าเป็น role Operation ที่ active จริง → **400** ถ้าไม่ใช่ · **ไม่ผูกมัด**: `approveConfig` ยังเช็คแค่ role Operation เหมือนเดิม Operation คนอื่นก็ approve Config นั้นได้ (3) module ใหม่ `src/user/` — `GET /users?role=<code>` คืน `UserSummary` (id + fullName + role code ของ user ที่ active, เรียงตามชื่อ) · **JwtAuthGuard อย่างเดียว ไม่มี PermissionGuard/resource** (mirror `/notifications/device-tokens` — self-serve, คืนแค่ชื่อไม่มีข้อมูล sensitive) · **ไม่ใช่** จอ User / Role Management (Section 2 = Admin เท่านั้น · POST/PATCH `/users` ยังอยู่ตาราง 4.2 · docs/11 Part B2 ยังไม่ทำ) — จะมาต่อยอด module นี้ (4) Web resolve ชื่อผู้อนุมัติจาก `GET /users?role=Operation` เอง — ไม่ embed relation ใน config response (ไม่แตะ `findAll` include) (5) `openapi.yaml` (v3.19) — tag `user` + `GET /users` + schema `UserSummary` + `decideConfig` body/`DeviceConfigDraft` +`suggestedApproverId` · `redocly lint` ผ่าน — ดู `backend/src/user/` + `backend/src/config/config.service.ts` (`decide`) |
| 2026-09-11 | paveekornk | แก้ครั้งที่ 26 — **schema-only: ฟื้น `CAMPAIGN_ASSIGNMENT` บางส่วนกลับมา** (Sprint 3 #21 Campaign Wizard prerequisite) เจ้าของ `campaign`/`task` (A ออกแบบ, ตกลงร่วมกับ kittiphong (B) เพราะแตะ `Task` ที่ B ดูแล) — สืบเนื่องจาก PR #144 review comment (Task.configId/deviceId/assignedTo เป็น null หมดในทางปฏิบัติ เพราะไม่มีทางป้อนข้อมูลจริง) และมติใหม่จากพี่เลี้ยงว่า **Task Management (Web, #7) ยกเลิกถาวร ไม่ทำในรูปแบบไหนทั้งสิ้น** (เดิมพักไว้เฉยๆ ตาม `docs/09` — ตอนนี้ปิดขาด) การมอบหมายงานจริงย้ายไปอยู่ในขั้นตอน "มอบหมายผู้รับผิดชอบหน้างาน" ตอนสร้าง Campaign แทน: (1) `schema.prisma` — เพิ่ม `model CampaignTarget` (junction เก็บ device เป้าหมายจริงของ Campaign หนึ่งตัว — เอาแค่ตารางเดียวจาก 3 ตารางเดิมที่ถูกตัดตอนทำ Campaign MVP กลับมา, `deviceId` เป็น string หลวมๆ mirror `Task.deviceId` ไม่ใช่ Prisma relation ไปยัง `Device.id`, ไม่มี `assignedTo` ในตารางนี้ — เก็บที่ `Task.assignedTo` ตรงๆ) + `Task.campaignId String?` (nullable, `onDelete: SetNull` เหมือน `configId`) · migration additive ล้วน `20260911043203_add_campaign_target` (คอลัมน์ nullable + ตารางใหม่ว่าง ไม่ต้อง backfill) (2) **Rollout strategy (canary/batch/auto-pause) ยังตัดออกเหมือนเดิม** — ตกลงกับ B แล้วว่าไม่ใช่ MVP รอบนี้ก็เหมือนที่ comment เดิมเหนือ `model Campaign` บอกไว้ (3) **ยังไม่ทำ:** backend `campaign` module (`POST /campaigns` รับ target device list ที่ resolve จาก `GET /devices` ฝั่ง client แล้ว + payload + assignment map → สร้าง `Campaign`+`CampaignTarget[]`+`Task[]` ใน `$transaction` เดียว ไม่ต้องมี queue เพราะเป็นแค่เขียน DB ของเราเอง) + Web Campaign Wizard UI (4 ขั้น: เลือกเป้าหมาย+มอบหมาย / เลือก Payload / Rollout (แบบง่าย "ส่งพร้อมกันหมด") / ตรวจสอบ&ยืนยัน — ไม่ใช่ "ส่งอนุมัติ" เพราะ `CampaignStatus` ไม่มี approval workflow แยกจากคนอื่นเหมือน Config) — ดู `backend/prisma/schema.prisma` (`model CampaignTarget`, `model Task`) |
| 2026-09-11 | paveekornk | แก้ครั้งที่ 27 — **`GET /audit-logs` (Sprint 3 #27) + เดินสาย AuditLog write ในโมดูล `config`/`device`** เจ้าของ `audit`/`config`/`device-status` (A): (1) module ใหม่ `src/audit/` — `GET /audit-logs` (resource `audit-logs` action `Read` · filter optional `userId`/`auditModule`/`action` เรียง `createdAt desc`) grant Operation/ST/OT/Auditor/Admin (ตรงกับ Section 2 แถว "Audit Log" — SW เป็น "-" ทั้งแถว, SuperAdmin ได้อัตโนมัติจากการ copy สิทธิ์ Admin) — **การเขียนไม่ได้อยู่ในโมดูลนี้** แต่ละโมดูลเขียน `prisma.auditLog.create(...)` เองตรงจุดที่เกิด mutation จริง (mirror pattern เดิมของ `config-deletion.service.ts`) (2) `config.service.ts` — เพิ่ม AuditLog write ให้ครบ `create`/`update`/`delete`(remove)/`decide`(เฉพาะ passed:true ที่มี update จริง)/`approve`(ใน transaction เดิม)/`reject` — `update`/`remove`/`reject`/`decide` เพิ่ม param `actor: ActingUser` (ควักจาก JWT ที่ controller เหมือน `create`/`approve` เดิม) — `decide` sync กับ #142 ที่ merge เข้า main ก่อนหน้า (`suggestedApproverId`) แล้ว: signature สุดท้ายเป็น `decide(id, passed, actor, suggestedApproverId?)` (3) `device.service.ts` — `applyConfig` เพิ่ม param `actor` + AuditLog `auditModule: 'device'` `action: 'apply-config'` (log ทุกครั้งไม่ว่าผล `applied` true/false เพราะ endpoint นี้เอง fire-and-forget ไม่ persist อะไรในระบบเรา — AuditLog แถวนี้เป็นร่องรอยเดียวที่มี) — `test-connection`/`simulate-config` เป็น dry-run ไม่ log (4) **`task`/`notification` (โมดูลของ B) ยังไม่มี AuditLog write เลย** ต้องให้ B เพิ่มเองในโมดูลตัวเอง คนละ PR (แนวเดียวกับที่ `config-sync-writer` ให้แต่ละคน `.on()`/`create()` ในไฟล์ของตัวเอง — ไม่ต้องแก้ไฟล์ `audit` module) (5) `openapi.yaml` (v3.19) — tag `audit` + `GET /audit-logs` + schema `AuditLogEntry` · `redocly lint` ผ่าน — ดู `backend/src/audit/` + `backend/src/config/config.service.ts` + `backend/src/device/device.service.ts` |
| 2026-09-11 | paveekornk | แก้ครั้งที่ 28 — **implement backend `campaign` module** (Sprint 3 #21 Campaign Wizard) เจ้าของ `campaign` (A): (1) module ใหม่ `src/campaign/` — `POST /campaigns` (resource `campaign` action `Create`, **Operation เท่านั้น**) รับ `name`/`description`/`payloadType`/`configId`/`targets[]` (แต่ละเป้าหมาย = `deviceId` + `assignedTo`) → validate ครบแล้วสร้าง `Campaign`+`CampaignTarget[]`+`Task[]` (หนึ่งต่อเป้าหมาย) ใน `$transaction` เดียว, สถานะ `active` ทันที ("ส่งพร้อมกันหมด" ตามที่ตกลงไว้ตอนทำ schema แก้ครั้งที่ 26 — ไม่มี draft/approval workflow) (2) validation ก่อนเขียน DB: Config ต้องมีอยู่จริง+`approved`/`synced` (mirror `APPLICABLE_CONFIG_STATUSES` จาก `device/config-applier.ts`) · ทุกอุปกรณ์เป้าหมายต้องมีอยู่จริง+สถานะ `installed`+deviceModel/protocol ตรงกับ Config (รวมทุกปัญหาเป็น 409 เดียว ไม่ throw ทีละเครื่อง) · ทุก `assignedTo` ต้องเป็น user ที่มีอยู่จริง+active (เช็คก่อน insert เพราะ `Task.assignedTo` มี FK ไป `User.id` จริง ถ้าไม่เช็คจะเจอ FK violation ดิบๆ) · deviceId ซ้ำกันในรายการเป้าหมายเดียวกัน → 400 (3) **`payloadType: Firmware` ยังไม่รองรับ (400)** — ตัดสินใจกับทีมว่า v1 รองรับแค่ Config ก่อน เพราะยังไม่มี backend `firmware` module เลย ไม่มีทางสร้าง Firmware record ผ่าน API ให้เลือก (รอ Sprint 3 #23) — ปิด TODO ที่ค้างไว้เหนือ `model Campaign` เรื่องบังคับ mutual exclusivity ระหว่าง `configId`/`firmwareId` ที่ service layer (4) เขียน `AuditLog` (`auditModule: campaign`, `action: create`) + แจ้งเตือน `task_assigned` ให้ผู้รับผิดชอบทุกเป้าหมาย — ทั้งคู่ never-throw (mirror pattern `config`/`device`/`task`) ไม่ทำให้แคมเปญที่สร้างสำเร็จแล้วดูเหมือนล้มเหลว (5) เพิ่ม `GET /campaigns` (list, filter `status`) + `GET /campaigns/{id}` (detail) — resource `campaign` action `Read` ทุก Role ตาม Section 2 · ยังไม่ embed `CampaignTarget[]`/`Task[]` ในนี้ (รอ Campaign Monitor แถวที่ 22) (6) seed grants: `campaign.Read` ทุก Role, `campaign.Create` เฉพาะ Operation (ตาราง 4.1 ย้ายจาก 4.2) (7) `openapi.yaml` (v3.20) — tag `campaign` + 3 path + schema `Campaign`/`CampaignCreateRequest`/`CampaignTargetInput`/`CampaignStatus`/`CampaignPayloadType` · `redocly lint` ผ่าน **ยังไม่ทำ:** Web Campaign Wizard UI, Campaign Monitor (`PATCH /campaigns/{id}`, ตัว successCount/failureCount ยังไม่มี logic อัปเดต) — ดู `backend/src/campaign/` |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 29 — **แก้ scope Campaign: ตัดการมอบหมายงานให้ช่างหน้างานออกทั้งหมด** เจ้าของ `campaign` (A) — มติพี่เลี้ยง (2026-09-13/14): Campaign มีไว้สำหรับ**ติดตามและบำรุงรักษาอุปกรณ์เป็นกลุ่ม** (อัปเดต Config/Firmware ให้กลุ่มอุปกรณ์เป้าหมาย + สังเกตความผิดปกติ) **ไม่ใช่เครื่องมือมอบหมายงานให้ช่างหน้างาน** — การมอบหมายงานเป็นหน้าที่ของระบบแยกที่บริษัทมีอยู่แล้ว ทำเองจะซ้อนทับระบบ: (1) `CampaignService.create()` — ตัด step สร้าง `Task` ต่ออุปกรณ์ (พร้อม `assignedTo`) และการแจ้งเตือน `task_assigned` ออกทั้งหมด เหลือแค่สร้าง `Campaign`+`CampaignTarget[]` (2) `CreateCampaignTargetDto` — ตัด field `assignedTo` ออก เหลือแค่ `deviceId` (3) `CampaignModule` — เอา `NotificationModule` ออก (ไม่มี notification ต้องส่งจากโมดูลนี้แล้ว) (4) **ไม่แตะ schema.prisma เชิงโครงสร้าง** — `CampaignTarget` ไม่เคยมี `assignedTo` อยู่แล้ว, `Campaign.tasks`/`Task.campaignId` relation เหลือไว้เป็น relation เปล่า (ไม่มีอะไรเขียนเข้าจาก module นี้อีก) เผื่ออนาคตมีช่องทางอื่นเชื่อม Task เข้ากับ Campaign — อัปเดตแค่ comment อธิบายสถานะปัจจุบัน (5) `openapi.yaml` (v3.21) — `CampaignTargetInput` ตัด `assignedTo` ออก, อัปเดต description ของ `createCampaign`/`Task.campaignId` ให้ตรงกับพฤติกรรมใหม่ · `redocly lint` ผ่าน (6) **ผลกระทบข้ามทีม:** Task ในระบบนี้ไม่มีช่องทางสร้างได้อีกต่อไปเลย (ช่องทางเดิม Task Management #7 ก็ถูกยกเลิกถาวรไปแล้วตามแก้ครั้งที่ 26) — คุยกับ kittiphong (B) แล้วว่า Mobile/Task module จะปรับบทบาทเป็นเครื่องมือสนับสนุนช่างหน้างาน (ค้นหาอุปกรณ์, monitor สถานะอัปโหลด/sync, ติดตามงานภาคสนาม) แทนการรับงานที่ถูกมอบหมาย — รายละเอียดเป็นงานฝั่ง B แยกต่างหาก **ยังไม่ทำ:** Web Campaign Wizard UI (PR #150) ต้องตัด step มอบหมายช่างหน้างานออกให้ตรงกันด้วย (คนละ PR — ดู PR #150) — ดู `backend/src/campaign/campaign.service.ts` |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 30 — **schema-only: เพิ่ม `model Customer` + `Device.customerId`** (docs/12_CustomerScope_Proposal.md เฟส B, PR #127) เจ้าของ `device-status` (A) — **หมายเหตุเรื่องเลข:** เดิมเขียนเป็น "แก้ครั้งที่ 29" ในดราฟต์นี้ ชนกับ "แก้ครั้งที่ 29" ของ PR #152 (แก้ scope Campaign, คนละ branch แยกจาก main มาก่อน) — #152 merge เข้า main ก่อน (คงเลข 29 ของ PR นั้นไว้ตามที่ merge จริง) แถวนี้กับแถวถัดไปจึง renumber เป็น 30/31 ตอน sync branch นี้กับ main เหมือนที่เคยทำกับแก้ครั้งที่ 25/26 มาก่อน: (1) `schema.prisma` — เพิ่ม `model Customer` (`companyName` บังคับ+unique กัน dropdown filter มีชื่อซ้ำสะกดต่างกัน, `contactName`/`email`/`phone`/`priorityTier` เป็น optional ทั้งหมดเพราะยังไม่ยืนยันว่า MVP ต้องกรอกครบ — field ตรงตาม GPS_Data_Dictionary.xlsx เท่าที่ยืนยันได้จาก schema DMS เดิม §3.5) + `Device.customerId String?` (nullable FK, `onDelete: SetNull` — เดิมทุกอุปกรณ์ไม่เคยผูกลูกค้า จึงไม่มี orphan data) · migration additive ล้วน `20260914031011_add_customer_and_device_customer_id` (2) **ใช้เป็นมิติกรอง/bulk-assign บนหน้า Device Search เท่านั้น ไม่ใช่ RBAC Customer Scope** — ST/OT ยังเห็นทุกอุปกรณ์เหมือนเดิมทุกประการ (Customer Scope จริงเป็นเฟส C แยกต่างหาก ยังไม่ทำ ไม่กระทบ Section 2/4 ของเอกสารนี้เลย) (3) **ตัดไอเดีย "ผูกลูกค้าตอนลงทะเบียนอุปกรณ์" ออก** — เช็คแล้วระบบยังไม่มี `POST /devices`/`PATCH /devices/{id}` เลยสักตัว (มีแค่ GET) การลงทะเบียนอุปกรณ์ไม่ใช่ของที่ A ต้องพัฒนา จึงเลือกทาง bulk-assign บนหน้า Device Search ที่มี multi-select อยู่แล้วแทน (4) seed mock data 3 ราย (`ABC Logistics`/`Northern Fleet`/`Metro Transit` — ชื่อสมมติล้วนๆ ตาม mockup ของ A **ไม่ใช่ข้อมูลจริงจาก schema DMS เดิม** ซึ่งมี PII ต้องเก็บนอก repo เท่านั้น) ผูกกับอุปกรณ์ seed บางส่วน เหลือบางเครื่องไม่ผูกไว้ทดสอบ filter "ไม่ระบุลูกค้า" **ยังไม่ทำ:** หน้า "จัดการลูกค้า" (Admin CRUD), ปุ่ม "กำหนดลูกค้า" บน Device Search, `GET /devices` filter ตาม `customerId`, `openapi.yaml`/`GET /devices` schema ยังไม่อัปเดต (ยังไม่มี endpoint ใหม่ให้ document) — ดู `backend/prisma/schema.prisma` (`model Customer`) |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 31 — **`GET /customers` + embed `customer` ใน `GET /devices`** (docs/12 เฟส B ต่อจากแก้ครั้งที่ 30 — "ช่วงที่ 1" จาก 2 ช่วงที่วางแผนไว้ ตัดหน้า "จัดการลูกค้า" ออกเพราะเกินขอบเขต) เจ้าของ `device-status` (A): (1) module ใหม่ `src/customer/` — `GET /customers` คืน `CustomerSummary[]` (id + companyName เท่านั้น ไม่มี contactName/email/phone/priorityTier — mirror `UserSummary` ที่ตัดข้อมูล sensitive ออกเหมือนกัน) เรียงตามชื่อบริษัท · **JwtAuthGuard อย่างเดียว ไม่มี PermissionGuard/resource** (mirror `/users`) ทุก role ที่ login เรียกได้ (2) `device.service.ts` — `findAll`/`findByDeviceId` เพิ่ม `include: { customer: { select: { id, companyName } } }` คืน type ใหม่ `DeviceWithCustomer` (`Device & { customer: CustomerSummary | null }` — mirror `ConfigFieldDefinitionWithSupport`) **ยังไม่มี query param กรองตาม customerId** ในรอบนี้ (แค่แสดงผล ตาม "ช่วงที่ 1" ที่ตกลงกัน) (3) `test/integration/setup.ts` — `resetDb()` ลืมลบตาราง `Customer` ตอนแก้ครั้งที่ 30 (bug ที่เจอตอนรัน integration test — unique constraint ชนข้ามเทส) แก้แล้วโดยลบ Device ก่อน Customer (ลูกก่อนพ่อแม่ตามคอนเวนชันไฟล์นี้) (4) `openapi.yaml` (v3.21) — tag `customer` + `GET /customers` + schema `CustomerSummary` · `Device` schema เพิ่ม field `customer` (nullable) + แก้ description ที่เคยบอกว่า "ยังไม่มี FK ไป Customer" ให้ตรงกับปัจจุบัน · `redocly lint` ผ่าน **ยังไม่ทำ ("ช่วงที่ 2"):** ปุ่ม "กำหนดลูกค้า" บน Device Search (bulk-assign) — ต้องมี `PATCH /devices/{deviceId}` ใหม่ (ยังไม่มี endpoint เขียนของ Device เลยสักตัว) + ตัดสิน RBAC ว่าใครกำหนดลูกค้าได้ + ตัดสินใจว่าจะขยาย `DataTable` กลางให้รองรับ row selection หรือทำตารางแยกเฉพาะหน้า (เหมือนที่ Campaign Wizard ทำ) — ดู `backend/src/customer/` + `backend/src/device/device.service.ts` |
| 2026-09-11 | paveekornk | แก้ครั้งที่ 32 — **implement backend `firmware` module** (หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 29" ในดราฟต์นี้ ชนกับ "แก้ครั้งที่ 29" ของ PR #152 ที่ merge เข้า main ก่อนจริง — renumber เป็น 32 ต่อจากแถว 31 ของ PR #153 ที่ merge เข้า main ก่อน PR นี้ ตอน sync branch นี้กับ main, เหมือนที่เคยทำกับแก้ครั้งที่ 25/26/29-31 มาก่อน) (Sprint 3 #23 Firmware Repository) เจ้าของ `firmware` (A): (1) schema — เพิ่ม `objectKey`/`originalFilename`/`fileSizeBytes` เข้า `model Firmware` (migration additive ล้วน `20260911093712_add_firmware_storage_fields` — ตาราง `Firmware` ว่างทุก env ไม่ต้อง backfill) ปิด gap ที่ `01_GPS_Build_Reference.md` §3 ระบุไว้แต่แรกว่าต้องมี "path ใน Object Storage" แต่ schema เดิมไม่มี field เก็บ reference ไฟล์เลยสักตัว (2) module ใหม่ `src/firmware/` — `POST /firmware` (**SW เท่านั้น**, resource `firmware` action `Create`) อัปโหลดขึ้น Object Storage (MinIO ตอนนี้ → AWS S3 ตอนใช้งานจริง) จริงแบบ synchronous ผ่าน `@aws-sdk/client-s3` ไม่มี mock mode (ต่างจาก `config-sync-writer`/`DeviceSimulator` เพราะ MinIO เป็น S3-compatible storage ตัวจริงที่รันอยู่แล้วตั้งแต่ Sprint 0 ไม่ใช่ระบบที่ต่อไม่ได้แบบระบบเดิม) `uploadStatus` ตั้งตามผลจริง (`stored`/`failed`) ไม่ throw 500 ถ้า Object Storage ล้มเหลว · `FirmwareStorageService.onModuleInit()` สร้าง bucket อัตโนมัติถ้ายังไม่มี (dev/CI ใหม่ที่ MinIO ยังไม่เคย provision bucket มาก่อน) (3) `PATCH /firmware/{id}` (**SW เท่านั้น**, resource `firmware` action `Update`) — แก้ Compatibility Tag แทนที่ `deviceModelCompatibility` ทั้ง array แยกจากขั้นตอนอัปโหลด (ชื่อ dev plan แถวที่ 23 "(อัปโหลด + Compatibility Tag)" แยก 2 การกระทำไว้ชัดเจน) (4) `POST /firmware/{id}/simulate` — resource ใหม่ `firmware-simulation` action `Read` (SW/Operation/ST/OT mirror `config`/`config-simulation` กัน Auditor/Admin ที่มีแค่ `firmware.Read` เรียกได้โดยไม่ตั้งใจ) mock เช็คแค่ compatibility (env `FIRMWARE_SIMULATOR_MODE`, Interface+Mock pattern เดียวกับ `DeviceSimulator`) · 409 ถ้า `uploadStatus` ยังไม่ `stored` (5) `deviceUpdateStatus` คงเป็น `unknown` เสมอในรอบนี้ — ต้องพึ่ง `device-status` ที่ยังไม่ implement (เพิ่ม open question ใหม่ใน Section 6 อธิบายเหตุผลและแนวทางที่เสนอไว้ 2 ระดับ) (6) `AuditLog` (`create`/`update`) never-throw mirror pattern เดิม (7) seed grants: `firmware.Read` ทุก Role, `firmware.Create`+`firmware.Update` เฉพาะ SW (มีอยู่แล้วบางส่วนตั้งแต่ก่อน module นี้เกิด — เพิ่ม `firmware-simulation.Read` ให้ SW/Operation/ST/OT ที่ขาดไป) (8) `.github/workflows/backend-ci.yml` — เพิ่ม step "Start MinIO" (`docker run` ตรงๆ ไม่ใช้ `services:` เพราะ GitHub Actions services ไม่รองรับ command override ที่ image `minio/minio` ต้องการ) + `OBJECT_STORAGE_*` env (9) `openapi.yaml` (v3.23 — renumber จาก v3.21 เดิมในดราฟต์นี้ ตอน sync กับ main เพราะชนกับ v3.21 ของ PR #152 ที่ merge เข้า main ก่อนจริง, mirror แก้ครั้งที่ 30/31 ที่เจอปัญหาเดียวกันจาก PR #153) — refine draft เดิมที่ยังไม่สมบูรณ์ (ไม่มี response schema ของ `uploadFirmware`, ไม่มี `version` ใน request, ไม่มี `PATCH`) ให้ตรงกับ implementation จริง — `redocly lint` ผ่าน — ดู `backend/src/firmware/` |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 33 — **เปิดรับ `payloadType: Firmware` ใน `POST /campaigns`** (หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 31" ในดราฟต์นี้ อ้างอิงแก้ครั้งที่ 29/30 ของตัวเอง — แต่แถว Firmware module ที่เคยเป็น "แก้ครั้งที่ 30" ในดราฟต์นี้เป็นสำเนาเก่าก่อน renumber ของ PR #151 เอง (ซ้ำกับแถวข้างบนที่ merge เข้า main ไปแล้วเป็นแก้ครั้งที่ 32) ตัดสำเนาซ้ำออก แถวนี้ renumber เป็น 33 ต่อจากแถว 32 — ต่อจากแก้ครั้งที่ 29 (Campaign scope cut) และ 32 (Firmware module, Sprint 3 #23/PR #151 implement เสร็จแล้ว) จึงปลดบล็อกที่ตั้งใจไว้ตอนทำ Campaign module แก้ครั้งที่ 28) เจ้าของ `campaign` (A): (1) `CreateCampaignDto` — เพิ่ม `firmwareId?: string` (`@ValidateIf` payloadType=Firmware คู่กับ `configId` ที่ผูกกับ payloadType=Config) (2) `CampaignService.create()` — แยก validate ออกเป็น `validateConfigPayload`/`validateFirmwarePayload` ตาม payloadType แทนที่จะ throw 400 ทันทีถ้าเป็น Firmware: Firmware ต้อง `uploadStatus: stored` (มิเรอร์ `SIMULATABLE_FIRMWARE_STATUS` จาก `firmware-status.ts`) + อุปกรณ์เป้าหมายทุกเครื่องต้อง `deviceModel` อยู่ใน `deviceModelCompatibility` ของ Firmware นั้น (**ไม่เช็ค protocol** เพราะ `model Firmware` ไม่มี field นี้เลย ต่างจาก Config ที่เช็ค deviceModel+protocol ตรงเป๊ะ) — ตรรกะ "เช็ค Device มีอยู่จริง+installed" เดิมแยกเป็น `collectUnavailableDeviceProblems` ใช้ร่วมกันทั้งสองเส้นทาง (3) `openapi.yaml` (v3.24 — renumber จาก v3.22 เดิมในดราฟต์นี้ ตอน sync กับ main เพราะชนกับ v3.22 ของ PR #153 ที่ merge เข้า main ก่อนจริง และ v3.23 ก็ถูก PR #151 ใช้ไปแล้วเช่นกัน) — `CampaignPayloadType`/`createCampaign`/`CampaignCreateRequest` อัปเดตให้ตรงกับพฤติกรรมใหม่ (`firmwareId` property ใหม่ใน `CampaignCreateRequest`) · `redocly lint` ผ่าน (4) unit test เพิ่ม describe `create — payloadType Firmware` ครบ (happy path, ไม่ระบุ firmwareId, ไม่พบ Firmware, uploadStatus ไม่ใช่ stored, deviceModel ไม่ compatible) + integration test เพิ่มชุดเดียวกันผ่าน HTTP จริง **ยังไม่ทำ:** Web Campaign Wizard UI (PR #150) ยังไม่มี dropdown เลือก Firmware ในขั้น Payload (ล็อกไว้ที่ Config เท่านั้น) — ต้องทำต่อคนละ PR — ดู `backend/src/campaign/campaign.service.ts` |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 34 — **Firmware Repository Web UI** (Sprint 3 #23 ต่อจากแก้ครั้งที่ 32 ที่ทำ backend เสร็จแล้ว) เจ้าของ `firmware` (A) — **web ล้วน ไม่แตะ backend/schema/openapi.yaml เลย** ต่อ endpoint ที่มีอยู่แล้วทั้งหมด (หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 30" ในดราฟต์นี้ อ้างอิงแก้ครั้งที่ 29 ของตัวเอง — แต่แถว Firmware module backend ที่เคยเป็น "แก้ครั้งที่ 29" ในดราฟต์นี้เป็นสำเนาเก่าก่อน renumber ของ PR #151 เอง (ซ้ำกับแถวข้างบนที่ merge เข้า main ไปแล้วเป็นแก้ครั้งที่ 32) ตัดสำเนาซ้ำออก แถวนี้ renumber เป็น 34 ต่อจากแถว 33): (1) หน้ารายการ `/firmware` — แทน scaffold เดิม (DEMO_FIRMWARE) ด้วย `DataTable` จริงต่อ `GET /firmware` (คอลัมน์ เวอร์ชัน/รุ่นที่รองรับ/สถานะอัปโหลด/สถานะอัปเดตกล่อง/ขนาดไฟล์/อัปโหลดเมื่อ — filter ต่อคอลัมน์ mirror Device Search) คลิกแถวไปหน้ารายละเอียด (2) หน้าอัปโหลด `/firmware/upload` (`POST /firmware`, SW เท่านั้น) — ไฟล์ + เวอร์ชัน + รุ่นอุปกรณ์เริ่มต้น (compatibility tag แรก 1 รุ่น) เช็คขนาดไฟล์ฝั่ง client ก่อน (50MB, จับ 413 ด้วยเผื่อหลุด) mirror `/config/import` (3) หน้ารายละเอียด `/firmware/{id}` — ข้อมูลไฟล์ + แก้ Compatibility Tag แบบ chip list (`PATCH /firmware/{id}`, SW เท่านั้น, แทนที่ทั้ง array เสมอตาม backend) + แผงทดสอบ (`POST /firmware/{id}/simulate`, SW/Operation/ST/OT ตาม resource `firmware-simulation`) รับ deviceModel อิสระ (ทดสอบรุ่นนอก compatibility ดูผลไม่ผ่านได้ ไม่ได้ล็อกแค่รุ่นที่รองรับ) (4) `web/src/lib/permissions.ts` เพิ่ม `canUpdateFirmwareCompatibility`/`canSimulateFirmware` (แยกจาก `canUploadFirmware` เดิมตาม resource ที่ backend แยกไว้) (5) `web/src/lib/status-pill.ts` เพิ่ม tone map ของ `uploadStatus`/`deviceUpdateStatus` (6) ไฟล์ใหม่ `web/src/lib/format-bytes.ts` (helper เดียว ใช้ทั่วหน้านี้ ไม่มีที่อื่นต้องใช้ตอนนี้) **หมายเหตุ cross-PR:** `web/src/lib/firmware-api.ts`/`web/src/hooks/use-firmware.ts` มีอยู่แล้วแบบย่อบน branch `feat/web-campaign-wizard` (PR #150, ทำไว้แค่ `listFirmware` สำหรับ dropdown เลือก payload) — ตอน merge จะชนกัน ให้ยึดเวอร์ชันของ PR นี้ (ครบกว่า เป็น superset) — ดู `web/src/app/(app)/firmware/` |
| 2026-09-14 | paveekornk | แก้ครั้งที่ 35 — **Campaign Wizard: เพิ่มคอลัมน์/filter "ลูกค้า" ในตารางเลือกเป้าหมาย** (ต่อยอดแก้ครั้งที่ 31) เจ้าของ `campaign` (A) — Web ล้วน ไม่แตะ backend/schema เพิ่ม (หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 32" ในดราฟต์นี้ — แถวอื่นที่เคยอยู่ก่อน/หลัง (30/31/33/34) เป็นสำเนาเก่าของ #153/#151/#154 ก่อน renumber ตัวเอง ซ้ำกับแถวที่ merge เข้า main ไปแล้วทั้งหมด ตัดสำเนาซ้ำออก เหลือแถวนี้ซึ่งเป็นงานจริงของ PR นี้เพียงแถวเดียว renumber เป็น 35 ต่อจากแถว 34): ขั้นที่ 1 (เลือกเป้าหมาย) เพิ่มคอลัมน์ "ลูกค้า" + dropdown filter (derive รายชื่อจากอุปกรณ์ installed ที่โหลดมาแล้ว ไม่ยิง `GET /customers` แยก) และขั้นที่ 4 (ตรวจสอบ&ยืนยัน) เพิ่มคอลัมน์เดียวกันเพื่อความสอดคล้อง — เครื่องที่ไม่ผูกลูกค้าแสดง/กรองเป็น "ไม่ระบุ" ได้ — ดู `web/src/app/(app)/campaigns/campaign-wizard.tsx` |
| 2026-09-11 | kittiphong | แก้ครั้งที่ 36 — **เดินสาย AuditLog write ในโมดูล `task`** (follow-up ของแก้ครั้งที่ 27 — ยังไม่ merge ตอนเขียน PR เดิม #146 branch จาก `feat/audit-log-module` โดยตรง, PR นั้นถูกปิดอัตโนมัติหลัง A ลบ branch ต้นทางทิ้งตอน #145 merge จึงเปิด PR ใหม่ #149 จาก branch เดิมตรงไป `main` แทน เนื้อหาไม่เปลี่ยน — หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 29" ในดราฟต์นี้ ตอนเขียนยังไม่มีแถวอื่นชนเลขนี้ แต่ระหว่างที่ PR นี้ค้างไม่ได้ sync มีอีก 6 PR (#151-#156, #150) merge เข้า main ไปก่อน ดันเลขไปถึง 35 แล้ว จึง renumber แถวนี้เป็น 36 ต่อจากแถว 35 ตอน sync branch นี้กับ main) เจ้าของ `task`/`notification` (B): (1) `task.service.ts` — เพิ่ม `logAudit()` private helper เขียน `AuditLog` (`auditModule: 'task'`) ที่ `create()` (`action: 'create'`) และ `update()` ทั้ง 2 branch — Operation แก้ทุก field (`action: 'update'`) และ ST/OT แก้ status งานตัวเอง (`action: 'update'`, `userId` = ST/OT เอง ไม่ใช่ Operation) — เขียนหลัง mutation สำเร็จเท่านั้น (ไม่เขียนถ้า 403/404/409 มาก่อน) (2) **เลือก wrap ด้วย try/catch แบบ never-throw** (mirror `notifyTaskAssigned` เดิมในไฟล์เดียวกัน) **ไม่ใช้ pattern unguarded-await ของ `config.service.ts`** — เป็นคำถามที่ B ตั้งไว้กับ A ว่า audit write ล้มเหลวไม่ควรทำให้ mutation จริงที่สำเร็จแล้วกลายเป็น 500 ที่ client เห็น (A แก้ตามทางนี้ใน `config.service.ts`/`device.service.ts` ไปแล้วเช่นกัน — ดูแก้ครั้งที่ 27 กับ commit follow-up ของ #145) (3) **`notification.service.ts` ไม่เพิ่ม AuditLog write** — พิจารณาแล้วทุก method ไม่เข้าเกณฑ์ CLAUDE.md Audit Pattern: `send()` ไม่มี actor จริง (เรียกจาก background listener อย่าง `config-sync-failure-alert.listener.ts` ด้วย ไม่ใช่แค่จาก HTTP request) และ mutation ที่แท้จริง (เช่น task ถูก assign) ถูก log ที่ต้นทางใน `task.service.ts` อยู่แล้ว ซ้ำถ้า log ที่นี่ด้วย · `markRead()` เป็น read-level activity (ยกเว้นตาม CLAUDE.md ชัดเจน) · `registerDeviceToken()`/`removeDeviceToken()` เป็น self-service session management ไม่ใช่ mutation ข้อมูล compliance เหมือน Config/Task (4) `openapi.yaml`/`RBAC_Matrix.md` §4.1 แถว `/audit-logs` — เพิ่ม `task` ในตัวอย่าง `auditModule` (action `create`/`update` มีอยู่แล้ว) ไม่มี endpoint ใหม่ ไม่ต้อง bump version · `redocly lint` ผ่าน (ไม่มี error ใหม่) (5) ไม่แตะ `config.service.ts`/`device.service.ts`/`src/audit/` เลย — ดู `backend/src/task/task.service.ts` |
| 2026-09-17 | paveekornk | แก้ครั้งที่ 37 — **แยก Role `SW` เป็น 3 Role ตามหน้าที่จริง** (`docs/13_Role_Redesign_Proposal.md` → PR #174, กับ kittiphong) เจ้าของ `config`/`firmware` (A): (1) `backend/prisma/seed.ts` — ตัด `SW` ออก เพิ่ม `ConfigEngineer`/`FirmwareEngineer`/`QAEngineer` ใน `INITIAL_ROLES` (name/description ตรงตาม PDF ต้นฉบับ §13.1) + test user `config.test`/`firmware.test`/`qa.test` แทน `sw.test` เดิม · grant `config`/`config-simulation`/`config-decision`/`config-definition`/`campaign` ย้ายจาก `SW` → `ConfigEngineer` ทั้งหมด, `firmware`/`firmware-simulation` ย้ายจาก `SW` → `FirmwareEngineer`, เพิ่ม `QAEngineer` grant ใหม่ (`firmware`.Read, `firmware-simulation`.Read, resource ใหม่ `firmware-decision`.Approve — Firmware Approval Lifecycle ยังไม่มี endpoint จริง รอ PR ถัดไป ดูตาราง 4.2) · ตัด `grant('SW','tasks','Read')` ทิ้ง (เช็คแล้วไม่เคยใช้งานจริง) (2) **บั๊กที่เจอระหว่างทดสอบ re-seed จริง:** `Role` upsert เดิมใช้ `update: {}` (insert-only เหมือน `ConfigFieldDefinition`) ทำให้แก้ชื่อ `OT` เป็น "Operation Technician" (ตัดขีดกลาง) แล้วรัน seed ซ้ำไม่ apply เข้า DB จริง — แก้เป็น `update: { name, description }` เพราะ Role metadata ควร sync จากไฟล์นี้เสมอ ต่างจาก `ConfigFieldDefinition` ที่ตั้งใจ insert-only (เหตุผลคนละอย่างกัน มีคอมเมนต์อธิบายแยกที่แต่ละจุด) (3) `web/src/lib/permissions.ts`/`nav.ts`/`status-pill.tsx`/`demo-data.ts` — map role check ทั้งหมดจาก `"SW"` ไปยัง role ใหม่ที่ตรงโดเมน (`canSimulateFirmware` เพิ่ม `QAEngineer` เข้ามาด้วยตาม grant ใหม่) (4) `docs/api/openapi.yaml` — role enum + ทุก endpoint description ที่อ้าง `SW` (~30 จุด) · `redocly lint` ผ่าน (5) เอกสารนี้ (RBAC_Matrix.md) — Section 1/2/4/5/6 ทั้งหมด **ยังไม่ทำ:** `CLAUDE.md` §Role Enum, backend `src/**` comment (ไม่กระทบ runtime), backend test suite (`*.spec.ts` + `test/integration/*.ts` ที่อ้าง `'SW'` ตรงๆ) — ตามในลำดับถัดไปของ PR นี้ |
| 2026-09-17 | paveekornk | แก้ครั้งที่ 38 — **Firmware Approval Lifecycle** (`docs/13_Role_Redesign_Proposal.md` §3.2 — "PR 2" ในลำดับที่ตกลงกับทีม ต่อจากแก้ครั้งที่ 37) เจ้าของ `firmware`/`campaign` (A): (1) `schema.prisma` — เพิ่ม `enum FirmwareApprovalStatus { pending_review, approved, rejected }` (คนละมิติกับ `FirmwareUploadStatus` เดิม — ไม่มี `draft` เพราะอัปโหลดสำเร็จ = พร้อมให้ QA ตรวจทันที ไม่มี `synced` เพราะ concept "sync เข้าระบบเดิม" กำลังถูกตัดตาม Issue #157) + `Firmware.approvalStatus` (`@default(pending_review)`) + `Firmware.approvedBy` (nullable FK ไป User, relation `FirmwareApprovedBy`) migration additive ล้วน `20260917084641_add_firmware_approval_status` (2) module `src/firmware/` — เพิ่ม `POST /firmware/{id}/approve`/`.../reject` (resource ใหม่ `firmware-decision` action `Approve` — grant นี้ seed ไว้แล้วตั้งแต่แก้ครั้งที่ 37) ทั้งคู่ต้อง `approvalStatus: pending_review` เท่านั้น (409 ถ้าไม่ใช่ — กันกด approve/reject ซ้ำ) `approve` ตั้ง `approvedBy` เป็นผู้เรียก `reject` ไม่ตั้งค่านี้ (คงเป็น null) — mirror `ConfigService.approve`/`reject` แต่ไม่มีขั้น `decide` แยกก่อนหน้าเพราะที่นี่มีผู้ตัดสินใจคนเดียว (QA) ไม่ใช่ 2 actor ต่อกันแบบ ConfigEngineer→Operation (3) `campaign.service.ts` (`validateFirmwarePayload`) — เพิ่มเช็ค `approvalStatus === 'approved'` แยกจากเช็ค `uploadStatus === 'stored'` เดิม (คนละเงื่อนไข คนละมิติ) 409 ถ้าไม่ผ่าน — แก้ทั้ง `createCampaign` 409 description และ integration/unit test fixture (`seedStoredFirmware`/`storedFirmware` default `approvalStatus: 'approved'` ให้เทสเดิมยังผ่าน) (4) `docs/api/openapi.yaml` — `Firmware` schema เพิ่ม `approvalStatus`/`approvedBy` (required ทั้งคู่) + 2 endpoint ใหม่ `approveFirmware`/`rejectFirmware` + อัปเดต `createCampaign` description/409 · `redocly lint` ผ่าน (5) เอกสารนี้ — ปิด open question ตาราง 4.2 (แถว Firmware Approval) ย้ายขึ้นตาราง 4.1, อัปเดต Section 2 แถว Firmware Approval (6) Web: `firmware-api.ts` เพิ่ม field/ฟังก์ชันคู่กัน, `status-pill.tsx` เพิ่ม `FIRMWARE_APPROVAL_STATUS_TONE`, `permissions.ts` เพิ่ม `canDecideFirmwareApproval` (QAEngineer เท่านั้น), หน้ารายละเอียด `/firmware/{id}` เพิ่ม pill สถานะอนุมัติ + `FirmwareApprovalPanel` (ปุ่มอนุมัติ/ปฏิเสธ — mirror `ApprovalActions` ของ Approval Center) และตาราง `/firmware` เพิ่มคอลัมน์สถานะอนุมัติ (7) unit test เพิ่มครบ (`firmware.service.spec.ts`/`firmware.controller.spec.ts`/`campaign.service.spec.ts`) + integration test เพิ่มชุด approve/reject ผ่าน HTTP จริง (`firmware-http.integration-spec.ts`) และเคส 409 ของ Campaign (`campaign-http.integration-spec.ts`) — ดู `backend/src/firmware/` |
| 2026-09-18 | paveekornk | แก้ครั้งที่ 39 — **Campaign Approval** (ปิด gap ที่ System Design PDF §9.1 (Approver field) / §10.2 ("ส่งอนุมัติ" step) / §19 (Firmware Production ต้องผ่าน Approval) ต้องการ แต่ v1 ของ Campaign Wizard ข้ามไปเลย — พบระหว่างรีวิว Module Ownership แล้วเสนอกับทีม ตกลงแนวทางไว้ก่อนเริ่มเขียนโค้ด) เจ้าของ `campaign` (A): (1) `schema.prisma` — เพิ่ม `pending_approval`/`rejected` เข้า `enum CampaignStatus` + `Campaign.approvedBy`/`approvedAt` (nullable FK ไป User, relation `CampaignApprovedBy` — ต้องตั้งชื่อ relation ทั้งคู่ตอนนี้เพราะ `Campaign` มี 2 FK ไป `User` แล้ว (`createdBy`/`approvedBy`), เปลี่ยน `creator` เดิมจาก relation ไม่มีชื่อเป็น `CampaignCreatedBy` ไปพร้อมกัน) migration additive ล้วน `20260918034548_add_campaign_approval_status` (2) `campaign.service.ts` — `create()` เปลี่ยนสร้างเป็น `pending_approval` แทน `active` ทันที + เพิ่ม `approve()`/`reject()` (ทั้งคู่ต้อง `pending_approval` เท่านั้น 409 ถ้าไม่ใช่ mirror `FirmwareService.approve`/`reject`) **Separation of Duty:** เช็คว่าผู้กด approve/reject ต้องไม่ใช่ `campaign.createdBy` คนเดียวกัน (403 `ForbiddenException`) — ต่างจาก Config ที่ ConfigEngineer/Operation แยก role กันอยู่แล้วโดย RBAC (grant คนละ role), Campaign สร้างโดย Operation เอง grant `campaign.Approve` จึงให้ Operation role เดียวกันหมด ต้องเช็คตัวตนที่ service layer เพิ่มโดยเฉพาะ (ไม่ได้ทำเป็น NestJS Guard แยกเพราะต้อง query DB หา `createdBy` ก่อนตัดสิน — ทำเป็น decorator guard ธรรมดาไม่ได้ mirror pattern ที่ `config.service.ts` ใช้เช็ค business rule อื่นๆ ในนี้เหมือนกัน ไม่ใช่ Guard class) (3) `campaign.controller.ts` — เพิ่ม `POST /campaigns/{id}/approve`/`.../reject` (resource `campaign` action `Approve` เดียวกันทั้งคู่ mirror ConfigController/FirmwareController) (4) `prisma/seed.ts` — เพิ่ม `grant('Operation', 'campaign', 'Approve')` (5) `docs/api/openapi.yaml` — `CampaignStatus` enum + description อัปเดต, `Campaign` schema เพิ่ม `approvedBy`/`approvedAt` (required ทั้งคู่, nullable), `createCampaign` description/response 201 แก้เป็น pending_approval, 2 endpoint ใหม่ `approveCampaign`/`rejectCampaign` (v3.25) · `redocly lint` ผ่าน (ไม่มี error ใหม่ — 2 warning เดิม info-license/no-server-example.com ยอมได้ตาม CLAUDE.md) (6) เอกสารนี้ — Section 2 แถว Campaign Wizard เพิ่ม **A**, ตาราง 4.1 เพิ่ม 2 แถว approve/reject (7) unit test ครบ (`campaign.service.spec.ts` เพิ่ม describe `approve`/`reject`, `campaign.controller.spec.ts` เพิ่ม 2 เทส) + integration test ผ่าน HTTP จริงครบ (`campaign-http.integration-spec.ts` เพิ่ม describe ใหม่ 6 เคส: 403 role ไม่มีสิทธิ์, 200 อนุมัติสำเร็จ, 403 SoD ผู้สร้างอนุมัติเอง, 409 สถานะผิด, 200 reject, AuditLog action approve) — รัน unit (349 ผ่าน) + integration (`campaign-http` 26 ผ่าน, `config-firmware-campaign-incident` 4 ผ่าน) ครบ (8) **Web** (เพิ่มทีหลังในสาขาเดียวกัน ก่อน PR #186 merge — ไม่แยก PR ตามที่เคยระบุไว้ mirror ที่แก้ครั้งที่ 38 ก็รวม backend+web ใน PR เดียวเหมือนกัน): `campaign-api.ts` เพิ่ม `approveCampaign`/`rejectCampaign` + field `approvedBy`/`approvedAt` ใน type `Campaign` + `CAMPAIGN_STATUSES` ครบ 6 ค่า, `status-pill.tsx` เพิ่ม tone ของ `pending_approval`/`rejected` (mirror `CONFIG_STATUS_TONE`/`FIRMWARE_APPROVAL_STATUS_TONE` — `active` เปลี่ยนจาก `progress` เป็น `success` เพราะตอนนี้แปลว่า "อนุมัติแล้ว" ไม่ใช่แค่ "สร้างแล้ว"), `permissions.ts` เพิ่ม `canDecideCampaignApproval` (Operation เท่านั้น — role check อย่างเดียว ไม่พอสำหรับ SoD) ไฟล์ใหม่ `campaign-approval-panel.tsx` (mirror `firmware-approval-panel.tsx`) เพิ่มเช็ค SoD ที่ตัวเองด้วย: เทียบ `getTokenSubject(session.accessToken)` (มิเรอร์ `approval-center-view.tsx` เพราะ `AuthSession` เก็บแค่ `accessToken`/`role` ไม่มี user id) กับ `campaign.createdBy` — ถ้าเป็นคนเดียวกันซ่อนปุ่ม โชว์ข้อความอธิบายแทน ต่อเข้า `campaign-detail-view.tsx` พร้อมแสดง `approvedAt` ในตาราง info เมื่อมีค่า — verify ด้วย `next build`/`tsc --noEmit`/`eslint` ผ่านหมด + สคริปต์ smoke test เรียก API จริงผ่าน dev server ยืนยัน flow ครบ (403 role ผิด, 403 SoD ผู้สร้างอนุมัติเอง, 200 คนอื่นอนุมัติ, 409 อนุมัติซ้ำ, 200 reject) — ไม่มี browser automation tool ในสภาพแวดล้อมนี้ จึงไม่ได้ทดสอบการคลิกจริงในเบราว์เซอร์ **ยังไม่ทำ:** ปรับข้อความใน `campaign-wizard.tsx` ขั้นสุดท้าย ("ยืนยัน — สร้างแคมเปญ") ให้สื่อว่าเข้าสถานะรออนุมัติ — เดิมข้อความนี้ตั้งใจเลี่ยงคำว่า "ส่งอนุมัติ" เพราะไม่มี approval workflow (ดู comment ใน `campaign-wizard.tsx`) ตอนนี้ตรรกะเปลี่ยนแล้วแต่ยังไม่ได้แก้ข้อความ (แก้แค่ comment ให้ตรงกับปัจจุบัน) — ไม่กระทบการทำงาน แค่ copy — ดู `backend/src/campaign/campaign.service.ts`, `web/src/app/(app)/campaigns/campaign-approval-panel.tsx` |
| 2026-09-18 | paveekornk | แก้ครั้งที่ 40 — **User / Role Management (PR 3 ในลำดับที่ตกลงกับทีม ต่อจากแก้ครั้งที่ 37 — หมายเหตุเรื่องเลข: เดิมเขียนเป็น "แก้ครั้งที่ 38" ในดราฟต์นี้ เพราะ branch นี้แยกจาก main **ก่อน** #179 merge (ตอนนั้น PR "Firmware Approval Lifecycle" ก็อ้างตัวเองว่าเป็นแก้ครั้งที่ 38 เหมือนกันด้วยเหตุผลเดียวกัน) — ระหว่างที่ PR นี้ค้างรออยู่ ทั้ง #179 (Firmware Approval Lifecycle) และ #186 (Campaign Approval) merge เข้า main ไปก่อนเป็นแก้ครั้งที่ 38 และ 39 ตามลำดับ แถวนี้จึง renumber เป็น 40 ต่อจากแถว 39 ตอน sync branch นี้กับ main mirror วิธีที่แก้ครั้งที่ 30-36 เจอปัญหาเดียวกันมาก่อน)** เจ้าของ `auth` (A) — **ขอบเขตตกลงกับทีม 2026-09-18: จัดการได้แค่บัญชีทั่วไปเท่านั้น (7 role ที่ไม่ใช่ Admin/SuperAdmin) การจัดการบัญชี Admin/SuperAdmin เอง (`admin-management`/`role-management`) ยังไม่ finalize (docs/11) ตกลงพักไว้ก่อน**: (1) module `src/user/` เพิ่ม `POST /users` (สร้างบัญชีทั่วไปใหม่), `GET /users/managed` (list เต็ม username/isActive รวม inactive), `PATCH /users/{id}` (แก้ role/isActive) — resource ใหม่ `user-management`, Admin เท่านั้น (SuperAdmin ได้อัตโนมัติจากการ copy สิทธิ์ Admin) ไม่แตะ `GET /users` เดิม (dropdown ผู้อนุมัติ, ไม่มี PermissionGuard) เลย (2) `managed-user-roles.ts` — exclude-list `['Admin','SuperAdmin']` ใช้ร่วมกันทั้ง 3 endpoint: `listManagedUsers` filter ออก, `createUser`/`updateUser` ปฏิเสธ role เป็น 2 ตัวนี้ (400 — กัน escalation ผ่าน API ตรงๆ แม้ web จะไม่โชว์ใน dropdown อยู่แล้วก็ตาม), `updateUser` ที่เป้าหมายเป็น Admin/SuperAdmin คืน 404 (มองว่า "ไม่มีอยู่" จาก resource นี้ — mirror การ filter ออกของ listManagedUsers ไม่ใช่ 403 เพราะไม่อยากยืนยันการมีอยู่ของบัญชีนั้นด้วยซ้ำ) (3) ไม่มี hard delete — `isActive: false` แทน (mirror pattern soft-disable ที่ระบบใช้อยู่แล้ว) (4) รหัสผ่านเริ่มต้นตอนสร้างบัญชี **Admin เป็นคนตั้งเอง** (ตัดสินใจร่วมกับ A 2026-09-18 — ระบบยังไม่มี flow ลืมรหัสผ่าน/reset password เลย ทำ auto-generate+reveal-once ตอนนี้เกินความจำเป็น) (5) `openapi.yaml` — tag `user` เพิ่ม `createUser`/`listManagedUsers`/`updateUser` + schema `ManagedUser`/`CreateUserInput`/`UpdateUserInput` · `redocly lint` ผ่าน (6) เอกสารนี้ — ปิด open question ตาราง 4.2 ย้ายขึ้นตาราง 4.1, อัปเดต Section 2 แถว User / Role Management (7) Web: แทนที่ `DEMO_USERS` ใน `user-management-view.tsx` ด้วย API จริง (8) unit test ครบ (`user.service.spec.ts`/`user.controller.spec.ts` ใหม่) + integration test เพิ่มชุดเต็มผ่าน HTTP จริง (`users-http.integration-spec.ts`) — ดู `backend/src/user/` |
| 2026-09-21 | paveekornk | แก้ครั้งที่ 41 — **แก้ `GET /tasks` ให้ scope ปลอดภัยสำหรับทุก role** (issue #73 — kittiphong เปิดตอนรีวิว PR #72) เจ้าของ `task` ร่วมกับ B เพราะกระทบ endpoint ที่ B ดูแล แต่เป็นบั๊ก RBAC/scope จึง A แก้ (A ถืออีก 2 module ที่มี pattern เดียวกันคือ `config`/`device` อยู่แล้ว): (1) `task.service.ts` — เดิม `findAll()` ใช้ logic "ไม่ใช่ ST/OT = ไม่ scope, ใช้ `query.assignedTo` ตรงๆ" เป็น **default-allow** ที่อันตราย (role ใหม่ที่ได้ grant `tasks.Read` เพิ่มทีหลังโดยไม่ได้ตั้งใจจะหลุดเห็นทั้งตารางทันที) เปลี่ยนเป็น **default-deny**: เพิ่ม `UNSCOPED_TASK_ROLES = ['Operation', 'Auditor', 'Admin', 'SuperAdmin']` (ตรงกับ grant `tasks.Read` จริงในปัจจุบันใน `seed.ts` — role ในลิสต์นี้เท่านั้นที่เลือก `assignedTo` เองผ่าน query ได้ role อื่นทั้งหมด (รวม ST/OT) ถูกบังคับ self-scope เป็น `actor.id` เสมอ ตาม IDOR Prevention Pattern ของ CLAUDE.md — ไม่แตะ `SELF_SCOPED_ROLES`/`update()`/`isHiddenFromActor()` เดิมเลย (คนละ concern กัน ของเดิมถูกอยู่แล้ว) (2) เหตุผลที่เลือก Auditor/Admin/SuperAdmin เข้า allowlist (ไม่ตัดสิทธิ์ตามที่ issue เสนอเป็นทางเลือก): Auditor ต้องอ่านทุกอย่างเพื่อตรวจสอบย้อนหลัง (เหมือนสิทธิ์ AuditLog), Admin/SuperAdmin ต้อง troubleshoot งานของใครก็ได้ — ทั้งคู่มี grant `tasks.Read` อยู่แล้วตามตาราง Section 2 แถว Task (~) มาตั้งแต่ก่อนหน้านี้ ไม่ใช่สิทธิ์ใหม่ (3) unit test เพิ่ม 3 เคสใน `task.service.spec.ts` (Auditor ไม่ scope, Admin/SuperAdmin ไม่ scope, role อื่นที่ไม่อยู่ใน allowlist ถูก self-scope เป็น default แม้ client จะส่ง `assignedTo` มาก็ตาม) — รัน unit ครบ (371 ผ่าน) ไม่มี integration test เดิมที่ทดสอบ scope ข้าม role ของ endpoint นี้โดยตรง จึงไม่กระทบอะไรที่มีอยู่ (4) เอกสารนี้ — ตาราง 4.1 แถว `GET /tasks` อัปเดตให้ตรงกับ logic ใหม่ · **ไม่แตะ** `openapi.yaml` (ไม่มี field/endpoint ใหม่ เป็นแค่ business logic เปลี่ยนภายใน ไม่กระทบ contract) — ดู `backend/src/task/task.service.ts` |
| 2026-09-21 | paveekornk | แก้ครั้งที่ 42 — **Per-Field Config Override ACL Phase 1** (issue #185 — proposal ยืนยันกับ kittiphong 2026-09-18) เจ้าของ `config` (A): (1) `schema.prisma` — เพิ่ม `ConfigFieldDefinition.stOverridable Boolean @default(false)` (ST override ค่า field นี้ได้ไหม — ตั้งใจตั้งชื่อสื่อ "เฉพาะ ST" ตรงตัว OT ไม่มีสิทธิ์เลยไม่ว่าค่านี้เป็นอะไร) + `ConfigVersion.reason String?` (semantic overload: field เดิมหมายถึง "ผู้อนุมัติ" แต่ override ไม่ใช่การอนุมัติจริง ใช้ field เดิมเก็บ id ผู้ทำรายการเหมือนกัน แยกแยะจาก `reason` — null = มาจาก approve() ปกติ, มีค่า = มาจาก override) migration additive ล้วน `20260921072426_add_config_override_st_field` (2) module ใหม่ `src/config-override/` — `POST /config/{configId}/override` (resource ใหม่ `config-override` action `Override` — grant **ST เท่านั้น** ใน seed.ts ไม่ให้ OT เลย) body `{ fields, reason }` — `fields` เป็น partial update, `reason` บังคับกรอกเสมอ (`GPS_Config_Firmware_Center_Design.pdf` §19 ข้อ 24: "ทุกการ Override ต้องมีผู้ดำเนินการ เหตุผล และ Audit Log") ต้อง Config สถานะ `approved`/`synced` เท่านั้น (`OVERRIDABLE_CONFIG_STATUSES` ใหม่ใน `config-status.ts`) mirror การสร้าง `ConfigVersion` แบบเดียวกับ `ConfigService.approve()` ทุกประการ (versioning เผื่อ Rollback #28) + เขียน `AuditLog` บังคับทุกครั้งในทรานแซกชันเดียวกัน (ไม่ใช้ never-throws เหมือน notification เพราะเป็นส่วนหนึ่งของความถูกต้องของ action นี้เอง) (3) `ConfigDefinitionService` — เพิ่ม `validateOverridableFields()` ใหม่ (**ต่างจาก `validateFields()` เดิม 2 จุด:** เช็ค `stOverridable: true` เพิ่ม, **ไม่เช็ค required field ที่ขาด** เพราะ override เป็น partial update ไม่ใช่แทนที่ทั้งชุด) + `CreateConfigDefinitionDto`/`create()` รับ `stOverridable` ผ่าน `POST /config-definitions` ได้ (4) `docs/api/openapi.yaml` — tag ใหม่ `config-override` + path `POST /config/{configId}/override` + `ConfigFieldDefinition`/`CreateConfigDefinitionInput` เพิ่ม `stOverridable` · `redocly lint` ผ่าน (2 warning เดิมยอมได้) (5) เอกสารนี้ — Section 2 แถว "Device Config Override" แก้จากเป้าหมายเดิม (`C,R,U,O` ให้ทั้ง ST/OT เท่ากัน — aspiration ก่อนพบว่าฟีเจอร์นี้ไม่เคยมีจริง) เป็นสิ่งที่ implement จริง (`O` ให้ ST เท่านั้น, ดู footnote ⁴), ตาราง 4.1 เพิ่มแถว `overrideConfig` (6) Web: `config-definition-api.ts`/`config-api.ts` เพิ่ม field/ฟังก์ชันคู่กัน, `permissions.ts` ลบ `canOverrideDevice()` เดิม (dead code ไม่เคยถูกเรียกใช้ + เช็คผิดว่า `ST || OT` เท่ากัน) แทนที่ด้วย `canOverrideConfig()` (ST เท่านั้น), `parameter-create-form.tsx`/`parameter-library-view.tsx` เพิ่ม toggle/คอลัมน์ "ST override ได้", หน้าใหม่ `config-override-panel.tsx` ต่อเข้า `config-detail-view.tsx` — เห็นเฉพาะ role ST (ไม่ใช่แค่ disable ทั้งหน้าสำหรับ OT — OT ไม่เห็น panel นี้เลย) field ที่ `stOverridable: false` แสดงแบบ disable พร้อมเหตุผล ไม่ซ่อน (7) unit test ครบ (`config-definition.service.spec.ts` เพิ่ม describe `validateOverridableFields` + เทส `stOverridable` ใน `create`, `config-override.service.spec.ts` ใหม่ 9 เทส) + integration test ผ่าน HTTP จริงครบ (`config-override-http.integration-spec.ts` ใหม่ 7 เคส: 401, 403 OT, 200 ST สำเร็จ, 400 field ไม่ overridable, 400 ไม่ส่ง reason, 409 สถานะ draft, 404) — รัน unit (383 ผ่าน) + integration (269 ผ่าน) ครบ (8) **ทดสอบจริงในเบราว์เซอร์** (Playwright + Chrome, ผ่าน dev server จริง) ครบทุก flow: Parameter Library toggle/คอลัมน์, ST เห็น/ใช้ panel ได้จริง (แก้ APN สำเร็จ + เห็น ConfigVersion ใหม่ในประวัติ), validation เหตุผลว่างบล็อกจริง, OT ไม่เห็น panel เลย + ยิง API ตรงโดน 403 จริง, AuditLog เขียนจริง — ไม่มี console/page error (9) **ยังไม่ทำ (นอกขอบเขต Phase 1 ตามที่ตกลง):** `GET /devices/{deviceId}/config` (พักไว้จนกว่า B จะเริ่ม Phase 2 Mobile จริง เพราะ semantic "current config ของอุปกรณ์" ต้องอนุมานจาก `Task` ที่ `completed` ซึ่งเป็นการตัดสินใจข้ามโดเมนที่ควรคุยกับ B ก่อน ไม่ใช่ A เดาเอง — ไม่ block Web ของ Phase 1 นี้เลย), Mobile Override screens (Phase 2, ทีม B), Firmware Override (นอก scope ตามที่ตกลงกับ B) — ดู `backend/src/config-override/`, `web/src/app/(app)/config/config-override-panel.tsx` |
| 2026-09-22 | kittiphong | แก้ครั้งที่ 43 — **เพิ่มค่า default ให้ Parameter** (issue #202, feedback พี่เลี้ยง — มติยืนยันไว้ที่ [issue #202 comment](https://github.com/dtc-gps-team/gps-config-firmware-center/issues/202#issuecomment-5770414882)) เจ้าของ `config-definition`: (1) `schema.prisma` — เพิ่ม `ConfigFieldDefinition.defaultValue String?` (nullable, เก็บเป็น string ดิบเหมือน pattern ของ `unit`/`category` — parse ตาม `dataType` ตอนใช้งานจริง) migration additive ล้วน `20260922025837_add_config_field_definition_default_value` (2) `CreateConfigDefinitionDto`/`ConfigDefinitionService.create()` รับ `defaultValue` ผ่าน `POST /config-definitions` ได้ (optional, `@MaxLength(255)`) — **ไม่มี endpoint แก้ไข `ConfigFieldDefinition` ในระบบเลย** (เช็คโค้ดจริงแล้ว มีแค่ `findAll`/`create`) จึงไม่ต้องเพิ่ม endpoint update ตามไปด้วย (3) `docs/api/openapi.yaml` — `ConfigFieldDefinition`/`CreateConfigDefinitionInput` เพิ่ม `defaultValue` (v3.26) · `redocly lint` ผ่าน (2 warning เดิมยอมได้) (4) Web: `config-definition-api.ts` เพิ่ม field คู่กัน, `parameter-create-form.tsx` เพิ่มช่องกรอก "ค่าเริ่มต้น (Default Value)" — input type ปรับตาม `dataType` ที่เลือกในฟอร์มเดียวกัน (`boolean` → dropdown true/false, `number` → input type number, อื่นๆ → text) `config-wizard.tsx` เพิ่ม `effectiveValue()` (คำนวณตอน render/submit จาก `values[fieldName] ?? def.defaultValue` แทนการเขียนลง `values` state ตรงๆ ใน `useEffect` — กัน ESLint `react-hooks/set-state-in-effect`) ใช้ auto-fill เฉพาะตอนสร้าง Config **ใหม่จริงๆ** (`!source`) **ตอน Clone (`cloneFrom` มีค่า) และตอนแก้ (`editing`) ไม่ใช้ `defaultValue` ทับเด็ดขาด — ยังคงใช้ค่าจาก Config ต้นทาง/เดิมเหมือนพฤติกรรมก่อนหน้าทุกประการ** ตามมติที่ยืนยันไว้ใน #202 (5) unit test เพิ่มใน `config-definition.service.spec.ts` (ส่ง `defaultValue` มา -> เขียนลง DB ตามนั้น, ไม่ส่งมา -> ยังสร้างได้ปกติ) + integration test เพิ่มใน `config-definition-http.integration-spec.ts` (2 เคสผ่าน HTTP จริง) — ไม่มี test file เดิมของ `parameter-create-form.tsx`/`config-wizard.tsx` ฝั่ง Web จึงไม่ได้เพิ่ม (ตามหลักการเดิม — ไม่สร้างระบบ test ใหม่ทั้งหมด) — รัน backend unit (389 ผ่าน) + integration (279 ผ่าน) ครบ, `next build`/`tsc --noEmit`/`eslint` ฝั่ง Web ผ่านหมด — ดู `backend/src/config-definition/`, `web/src/app/(app)/config/config-wizard.tsx`, `web/src/app/(app)/parameters/parameter-create-form.tsx` |
| 2026-09-22 | kittiphong | แก้ครั้งที่ 44 — **เก็บรายละเอียด apply-config ใน AuditLog** (issue #205, feedback พี่เลี้ยง — Approach A ยืนยันไว้ที่ [issue #205 comment](https://github.com/dtc-gps-team/gps-config-firmware-center/issues/205#issuecomment-5770415152)) เจ้าของ `audit`/`device`: (1) `schema.prisma` — เพิ่ม `AuditLog.metadata Json?` (nullable, mirror `Incident.metadata` ทุกประการ) migration additive ล้วน `20260922050609_add_audit_log_metadata` — แก้ comment เหนือ model ที่เคยบันทึกมติเดิม (2026-08-31: "ยังไม่มี use case") ให้ตรงกับมติใหม่ ไม่ปล่อยให้ comment เก่าขัดกับโค้ดจริงเงียบๆ (2) `src/audit/audit-log-metadata.ts` ใหม่ — `AuditLogMetadata` interface (ทุก key optional, mirror `incident-metadata.ts`) **เก็บเฉพาะ `apply-config` เท่านั้นตามเกณฑ์ที่ตกลงไว้ใน #205** (ไม่ backfill ทุกจุดที่เขียน AuditLog อยู่แล้ว): `{ deviceId, configId, fieldNames }` — **ตั้งใจเก็บแค่ชื่อ field ไม่ใช่ค่าจริง** เพราะ `GET /audit-logs` เปิดให้ Operation/ST/OT/Auditor/Admin อ่านได้ ถ้าเก็บค่าจริงของ `Config.fields` อาจมีค่าอ่อนไหว (เช่น `COMMAND_PASSWORD`, `SOS_NUMBER_1`) รั่วออกไปให้ role ที่ไม่ควรเห็น — บทเรียนตรงจาก PR #206 ที่เพิ่งแก้ช่องโหว่คล้ายกัน (3) `DeviceService.applyConfig()` — เขียน `metadata` เข้า `AuditLog` ในจุดเดิมที่มีอยู่แล้ว (never-throws เหมือนเดิม ไม่เปลี่ยน behavior จุดนี้) (4) `AuditController`/`AuditService.findAll()` **ไม่ต้องแก้** — ไม่มี `select` ระบุ field ชัดเจน คืนทุก scalar column อัตโนมัติอยู่แล้ว `metadata` จึงไหลผ่าน `GET /audit-logs` ได้เองทันที (5) `docs/api/openapi.yaml` (v3.27) — `AuditLogEntry` เพิ่ม `metadata` (nullable object, mirror `Incident.metadata` ใน spec) · `redocly lint` ผ่าน (2 warning เดิมยอมได้) (6) Web: `audit-api.ts` เพิ่ม field `metadata` ใน type `AuditLogEntry` ให้ตรง contract — **ยังไม่มี UI แสดงผลค่านี้** ใน `audit-log-view.tsx` (ตารางเดิมไม่มีคอลัมน์สำหรับ metadata) เพิ่มได้ในอนาคตถ้าต้องการ (7) unit test เพิ่มใน `device.service.spec.ts` (2 เคส: metadata ครบตามที่คาด, fieldNames เป็นชื่อ field ไม่ใช่ค่าจริงแม้ field นั้นเป็นรหัสผ่าน) + แก้ fixture เดิมใน `audit.service.spec.ts` ที่พังเพราะ type เปลี่ยน + integration test เพิ่มใน `device-http.integration-spec.ts` (query `AuditLog` จริงจาก DB หลัง apply-config สำเร็จ ยืนยัน metadata ตรงตามที่คาด ไม่ใช่แค่เช็ค response 200) — รัน backend unit (393 ผ่าน) + integration (282 ผ่าน) ครบ, `tsc --noEmit`/`eslint`/`next build` ฝั่ง Web ผ่านหมด **จุดที่ยังไม่ครอบคลุม (ตามที่ #205 ให้ทำแค่จุดที่ชัดเจนที่สุดก่อน):** จุดอื่นที่อาจเข้าข่าย "สำคัญ/จำเป็นต่อการตรวจสอบ" แต่ยังไม่เพิ่ม metadata รอบนี้ — Firmware upload/approve/reject (`firmware.service.ts` มี `logAudit()` กลาง เขียนแค่ `action` เปล่าๆ ไม่มี `firmwareId` เลยด้วยซ้ำ) และ Config approve/reject/Campaign approve/reject (มี AuditLog write อยู่แล้วแต่ไม่มีรายละเอียด) — ไม่ได้เดาเพิ่มเองตามที่สั่ง รอ kittiphong ตัดสินใจต่อว่าจะขยายจุดไหนเพิ่ม — ดู `backend/src/device/device.service.ts`, `backend/src/audit/audit-log-metadata.ts` |
| 2026-09-22 | kittiphong | แก้ครั้งที่ 45 — **เพิ่ม flow เลือกอุปกรณ์ผ่านบริษัทสำหรับทดสอบสัญญาณ (Mobile)** (issue #204, feedback พี่เลี้ยง — มติยืนยันไว้ที่ [issue #204 comment](https://github.com/dtc-gps-team/gps-config-firmware-center/issues/204#issuecomment-5770415019) — **scope เปลี่ยนจากที่เสนอไว้เดิมในตัว issue** เป็นเลือกบริษัท→เห็นรายการอุปกรณ์แยกตามรุ่น→เลือกอุปกรณ์ ไม่ใช่แค่ช่องค้นหา) เจ้าของ `device`/Mobile: (1) `backend/src/device/dto/query-device.dto.ts` — เพิ่ม filter `customerId` (optional, `@IsUUID()`) ให้ `GET /devices` `device.service.ts` เพิ่มเข้า `where` clause ตรงๆ (`Device.customerId` เป็น field ตรงอยู่แล้ว ไม่ต้อง join ใหม่ — มีอยู่แล้วตั้งแต่แก้ครั้งที่ 31 แค่ยังไม่เปิด filter ที่ backend) เครื่องที่ `customerId` เป็น `null` จะไม่ถูกคืนเมื่อส่ง filter นี้มา (2) **`GET /customers` เดิมเพียงพอสำหรับหน้าเลือกบริษัท ไม่ต้องสร้าง endpoint ใหม่** (เช็คแล้ว — ไม่มี PermissionGuard ทุก role เรียกได้อยู่แล้ว) (3) `docs/api/openapi.yaml` (v3.28) — `listDevices` เพิ่ม query param `customerId` · `redocly lint` ผ่าน (4) Mobile — ไฟล์ใหม่ `mobile/lib/features/device_connection_test/customer_device_repository.dart` (`CustomerDeviceRepository` — `listCustomers()`/`listDevicesForCustomer()`, มี Mock impl สำหรับ `API_MOCK_MODE` mirror `MockDeviceSearchRepository`) + `company_device_picker_page.dart` (หน้าใหม่ 2 ขั้น: เลือกบริษัท → รายการอุปกรณ์แยกกลุ่มตาม `deviceModel` ด้วย section header เลือกอุปกรณ์แล้ว pop กลับพร้อม `deviceId`) route ใหม่ `AppRoutes.deviceConnectionTestPicker` (`/device-connection-test/pick-device`, เข้าถึงได้จากปุ่ม "เลือกจากรายการ" ใน `device_connection_test_page.dart` เท่านั้น ไม่ใช่ shortcut แยกจาก Home) — **เก็บ flow เดิม (พิมพ์ device ID เอง) ไว้ครบทุกจุดตามมติ** ปุ่มใหม่เป็นแค่ทางเลือกเพิ่มที่เติมค่าเข้าช่องเดิมเท่านั้น ไม่ auto-submit (5) `mobile/lib/core/api/models.dart` เพิ่ม class `Customer` (id+companyName, mirror backend `CustomerSummary`) `api_client.dart` เพิ่ม `listCustomers()` + `listDevices()` รับ `customerId` optional (ไม่กระทบ call site เดิมที่เรียกไม่มี arg) (6) **บั๊กที่พบมาก่อนหน้าและแก้พร้อมกันเพราะเกี่ยวกับ device ID history โดยตรง:** `RecentDeviceIdStore` (ทั้ง `SharedPreferences`/in-memory impl) เพิ่ม `clear()` ใหม่ เรียกจาก `AuthController.logout()` (mirror `_tokenStore.clear()`/`_profileStore.clear()` ที่มีอยู่แล้ว) — ก่อนหน้านี้ประวัติเลขเครื่องไม่เคยถูกล้างตอน logout เลย ช่างคนที่ 2 login เครื่องเดียวกันต่อจากคนแรกจะเห็นประวัติของคนแรก (7) **regression ที่พบระหว่างรัน test เต็มชุด:** `logout()` เรียก `recentDeviceIdStoreProvider.clear()` ตรงๆ ไม่มี try/catch (ต่างจาก push-notification unregister ที่ never-throws) — เทสเดิม 2 ไฟล์ (`home_page_test.dart` ปุ่ม logout, และ container เปล่าใน `auth_controller_test.dart`) ไม่เคย override provider นี้เลย พอไม่ override จะ fallthrough ไปใช้ `SharedPreferencesRecentDeviceIdStore` จริงที่ไม่มี plugin binding ในเทส ทำให้ `logout()` throw ก่อนถึง `state = ...unauthenticated` เทสเดิม fail ไปด้วย — แก้โดยเพิ่ม `recentDeviceIdStoreProvider.overrideWithValue(InMemoryRecentDeviceIdStore())` ในทุก container ที่เรียก `logout()` (ไม่ได้แก้ `logout()` ให้ swallow error เพราะการล้างประวัติควรเป็น mutation ที่เห็น error ชัดเจนถ้าล้มเหลวจริง ต่างจาก push notification ที่ตั้งใจ fire-and-forget) (8) unit/widget test เพิ่ม: `recent_device_id_store_test.dart` (`clear()`), `auth_controller_test.dart` (logout ล้างประวัติจริง), `device_connection_test_page_test.dart` (ปุ่มใหม่อยู่คู่กับช่องเดิม) — รัน `flutter test` เต็มชุด 266 ผ่าน, `flutter analyze` ไม่มี issue, `dart format --set-exit-if-changed lib test` ผ่าน (0 changed) — รัน backend unit + integration ครบเช่นกัน (ดูรายละเอียดใน PR) — ดู `backend/src/device/`, `mobile/lib/features/device_connection_test/`, `mobile/lib/core/auth/auth_controller.dart` |
| 2026-09-22 | paveekornk | แก้ครั้งที่ 46 — **Incident & Rollback Web UI: ต่อ `GET /incidents` จริง** (Tier 1 จาก backlog ล่าสุด) เจ้าของ `incident` (A) — **web ล้วน ไม่แตะ backend/schema/openapi.yaml เลย** (`GET /incidents` implement อยู่แล้วตั้งแต่แก้ครั้งที่ 24 ฝั่ง Web ไม่เคยต่อจริงจนตอนนี้): (1) ไฟล์ใหม่ `web/src/lib/incident-api.ts` (type `Incident`/`IncidentSeverity`/`IncidentStatus` + `listIncidents()` mirror `audit-api.ts`) + `web/src/hooks/use-incidents.ts` (mirror `use-audit-logs.ts` ทุกประการ ใช้ `useRefetchOnFocus`) (2) หน้าใหม่ `incidents-view.tsx` แทนที่ scaffold เดิม (`DEMO_INCIDENTS`) — filter ตาม `status`, ตารางคอลัมน์ เรื่อง/ความรุนแรง/สถานะ/เกิดเมื่อ/การดำเนินการ (`IncidentActions` เดิมไม่แก้ logic ปุ่ม แก้แค่ tooltip ที่เคยเขียนผิดว่า "ยังไม่มีใน spec" ทั้งที่ GET ต่อจริงแล้ว ให้ตรงกับปัจจุบัน — เหลือแค่ endpoint แก้ไขเชิงเทคนิค/Rollback ที่ยังไม่มี) (3) `status-pill.tsx` เพิ่ม `INCIDENT_STATUS_TONE`/`INCIDENT_SEVERITY_TONE` (4) `page.tsx` ไม่ครอบ `RoleGuard` (ตรงกับ Section 2 แถว "Incident & Rollback" = R ทุก Role ไม่มี Role ไหนถูกกันออก) (5) `demo-data.ts` ลบ `DEMO_INCIDENTS` ออก (6) ทดสอบจริงในเบราว์เซอร์ (Playwright + Chrome) — login จริงด้วย `operation.test`, เห็นรายการ incident ที่ seed ไว้ชั่วคราวถูกต้อง (ชื่อ/severity/status pill ตรง), filter สถานะทำงานจริงผ่าน API จริง (2→1 แถวตอนเลือก "Resolved"), ไม่มี console/page error — `tsc --noEmit`/`eslint`/`next build` ผ่านหมด — ดู `web/src/app/(app)/incidents/` |
| 2026-09-22 | paveekornk | แก้ครั้งที่ 47 — **DeviceModel registry** (issue #209, docs/15 — PR #196 merge แล้ว รอ implement) **หมายเหตุเรื่องเลข:** เดิมเขียนเป็น "แก้ครั้งที่ 45" ในดราฟต์นี้ ชนกับแก้ครั้งที่ 45 ของ PR #208 (Mobile company/device picker) ที่ merge เข้า main ไปก่อนแล้ว — ต่อจากแก้ครั้งที่ 45 ควรเป็น 46 แต่เลขนั้นถูกใช้ไปแล้วโดยแก้ครั้งที่ 46 (Incident Web UI, PR #213 อีก branch ที่ทำคู่ขนานกัน ณ ตอน sync — ยังไม่ merge แต่รู้เลขไว้ล่วงหน้าเพราะทำเองทั้งคู่) จึงข้ามไปใช้ 47 กันชนซ้ำรอบสอง mirror วิธีที่แก้ครั้งที่ 30/34/40 เจอปัญหาเดียวกันมาก่อน เจ้าของ `device-model` (A ร่วมกับ `config` เพราะกระทบ `ConfigService`): (1) `schema.prisma` — model ใหม่ `DeviceModel` (`name` unique, `manufacturer?`, `supportedProtocols String[]`, `status DeviceModelStatus @default(active)`, `warrantyMonths?`, `endOfSupportDate?`, `notes?`) **ต่างจาก docs/15 เดิม 1 จุด**: ตัด `hardwareVersion` ออกจาก `DeviceModel` (อุปกรณ์รุ่นเดียวกันมีได้หลาย hardware revision ต่างกันตามรอบผลิต ไม่ใช่ 1 โมเดล = 1 เวอร์ชันตายตัวตามที่ docs/15 สมมติไว้ตอนแรก) ย้ายไปเป็น `Device.hardwareRevisionCode String?` แทน (string อิสระ ไม่มีทะเบียนแยก — ยังไม่มีหลักฐานว่าต้องมี metadata/สถานะเป็นของตัวเอง) `Device` เพิ่ม `modelId` (FK ไป `DeviceModel`, `onDelete: Restrict`) migration **2 ไฟล์แยกกันตั้งใจ** (`add_device_model_registry` เพิ่ม nullable ก่อน → backfill จริงใน `seed.ts` (ไม่ auto-map เหมารวม สร้าง `DeviceModel` จาก `deviceModel` string ที่ distinct อยู่แล้ว + ตั้ง `modelId` ให้ทุกแถว) → `device_model_id_not_null` บังคับ NOT NULL หลัง backfill ครบ) — ฝึก pattern nullable-first ตาม CLAUDE.md Migration Safety แม้ตอนนี้ยังไม่มีข้อมูลจริง (มีแต่ seed data ที่คุมเองสะอาดอยู่แล้ว) เพื่อให้พร้อมใช้ pattern เดียวกันตอนมีข้อมูลจริงในอนาคต (2) module ใหม่ `src/device-model/` — `POST`/`PATCH /device-models` (resource ใหม่ `device-model`, **Admin/SuperAdmin เท่านั้น** — master/reference data ที่เพิ่มไม่บ่อย mirror `user-management` ไม่ใช่ self-service แบบ `config-definition`) `GET /device-models`/`GET /device-models/{id}` **ไม่มี PermissionGuard เลย** เปิดกว้างทุก role ที่ login แล้ว (mirror `GET /users`) — `supportedProtocols` **บังคับอย่างน้อย 1 ค่าตอนสร้าง** (`@ArrayMinSize(1)`) กันสร้างรุ่นที่ใช้งานไม่ได้ถาวร (3) `ConfigService.create()`/`update()` — เพิ่ม `validateDeviceModelProtocol()` ใหม่ (inject `DeviceModelService`) **enforce เข้มงวดจริง**: `Config.protocol` ต้องอยู่ใน `DeviceModel.supportedProtocols` ของ `deviceModel` นั้น ไม่งั้น 400 — ไม่พบรุ่นในทะเบียนเลยก็ 400 เหมือนกัน (ไม่มีทางลัดให้รุ่นที่ยังไม่ได้ลงทะเบียนข้ามไปได้ mirror หลักการเดียวกับ `ConfigDefinitionService.validateFields()`) เรียกก่อน `validateFields()` เดิมเสมอทั้ง `create()`/`update()` (4) `docs/api/openapi.yaml` (v3.28) — tag ใหม่ `device-model` + 4 path (`listDeviceModels`/`createDeviceModel`/`getDeviceModel`/`updateDeviceModel`) + schema `DeviceModel`/`CreateDeviceModelInput`/`UpdateDeviceModelInput`/`DeviceModelStatus` · `redocly lint` ผ่าน (2 warning เดิมยอมได้) (5) unit test ครบ: `device-model.service.spec.ts` ใหม่ (findAll/findOne/findByName/create/update รวม 409/404/race-condition P2025), `config.service.spec.ts` เพิ่ม mock `DeviceModelService` + 4 เคสใหม่ (deviceModel ไม่มีในทะเบียน, protocol ไม่รองรับ, protocol รองรับผ่านปกติ ทั้ง create/update) + integration test ครบ: `device-model-http.integration-spec.ts` ใหม่ (401/403/201/400 ว่างเปล่า/409 ซ้ำ/200 แก้/404 ผ่าน guard chain จริง), `config-http.integration-spec.ts` เพิ่ม 2 เคส (deviceModel ไม่มีในทะเบียน -> 400, protocol ไม่รองรับ -> 400 ผ่าน HTTP จริง) (6) **breaking change ที่ต้องแก้ตาม** — `Device.modelId` เป็น NOT NULL แล้ว กระทบทุกจุดที่สร้าง `Device` ตรงๆ ใน integration test เดิม (`campaign-http`, `device-http`, `device.integration-spec`, `task-http`) แก้ให้ครบผ่าน helper ใหม่ `getOrCreateDeviceModel()` ใน `test/integration/setup.ts` (mirror `getOrCreateRole()`) + `resetDb()` เพิ่มลบ `DeviceModel` (หลัง `Device` เพราะ FK `Restrict`) + unit fixture `device.service.spec.ts` เพิ่ม `hardwareRevisionCode`/`modelId` ให้ตรง type ใหม่ — รัน backend unit (408 ผ่าน) + integration (293 ผ่าน) ครบ, `npm run lint`/`npm run build` ผ่านหมด **⚠️ ข้อควรระวังสำหรับอนาคต (ยังไม่ implement รอบนี้ — บันทึกไว้ก่อน):** `registerDevice` (`POST /devices`, docs/14 Device Sync — ยังไม่เริ่ม) ต้องรับแค่ `modelId` จากรุ่นที่มีอยู่แล้วเท่านั้น **ห้ามทำ find-or-create** (พิมพ์ชื่อรุ่นมา ไม่เจอก็สร้างให้เลย) เพราะจะกลายเป็นช่องทางให้ role ที่ไม่ใช่ Admin สร้าง `DeviceModel` ทางอ้อมได้ ข้าม RBAC ที่ตกลงกันไว้แบบเงียบๆ (`PermissionGuard` เช็คแค่ตอน request เข้า controller ที่เรียกเท่านั้น ไม่ครอบคลุม service-to-service call ภายใน) — ดู issue #209 หัวข้อ "ข้อควรระวังสำหรับอนาคต" — ดู `backend/src/device-model/`, `backend/src/config/config.service.ts` |
| 2026-09-23 | paveekornk | แก้ครั้งที่ 48 — **เอา protocol ออกจากฟอร์มสร้าง Parameter** (issue #203 — ปิดคำถามเปิดที่เคยตั้งไว้เอง หลัง `DeviceModel` (แก้ครั้งที่ 47) implement เสร็จตามลำดับที่ตกลงกับ kittiphong ไว้) เจ้าของ `config-definition` (A) — **web ล้วน ไม่แตะ backend/schema/openapi.yaml เลย** (`POST /config-definitions` รับ `supportedModels: [{deviceModel, protocol}]` แบบเดิมทุกประการ — เปลี่ยนแค่วิธีที่ Web ประกอบ array นี้ก่อนส่ง): เลือก **approach (ข)** ที่ตกลงกันไว้ในตัว issue — ผู้ใช้เลือกแค่ "รุ่นอุปกรณ์" ไม่ต้องเลือก protocol เอง แล้วระบบสร้างคู่ `(deviceModel, protocol)` ให้ครบทุก protocol ที่รุ่นนั้นรองรับจาก `DeviceModel.supportedProtocols` ตรงๆ (ไม่ derive แบบเดาว่า 1 รุ่น = 1 protocol เสมอ ตามที่ตัดสินใจไว้): (1) ไฟล์ใหม่ `web/src/lib/device-model-api.ts` (`listDeviceModels()`, type `DeviceModel`/`DeviceModelStatus`) + `web/src/hooks/use-device-models.ts` (mirror `use-customers.ts`) — **จุดแรกที่ Web เรียก `GET /device-models` เลย** (ก่อนหน้านี้มีแต่ backend endpoint จากแก้ครั้งที่ 47 ยังไม่มีใครใช้ฝั่ง Web) (2) `parameter-library-view.tsx` — ลบ `knownPairs` เดิม (derive คู่ (deviceModel, protocol) จาก `ConfigFieldDefinition.supportedModels` ที่มีอยู่แล้ว — วิธีเดิมที่ "เดา" รุ่น/protocol จากข้อมูลเก่า ไม่ใช่ source of truth) แทนที่ด้วย `useDeviceModels()` ตรงๆ (3) `parameter-create-form.tsx` — เปลี่ยน prop จาก `knownPairs: ConfigFieldModelSupport[]` เป็น `deviceModels: DeviceModel[]`, checkbox fieldset "รุ่น/โปรโตคอลที่รองรับ" เดิม (แสดง `GT06N/TCP` เป็นคู่ให้เลือก) เปลี่ยนเป็น "รุ่นอุปกรณ์ที่รองรับ" (แสดงแค่ `GT06N` พร้อม hint protocol ข้างๆ แบบ read-only เช่น `GT06N (TCP)` — ไม่ให้เลือกแยก) รุ่นที่ `status: discontinued` แสดง tag "เลิกผลิต" กำกับไว้ (ยังเลือกได้ ไม่ซ่อน เพราะอุปกรณ์รุ่นนั้นอาจยังใช้งานอยู่จริงในสนาม) submit handler expand `selectedModelNames` เป็น `supportedModels` เต็มรูปแบบด้วย `flatMap` ตาม `DeviceModel.supportedProtocols` (4) **ทดสอบจริงในเบราว์เซอร์** (Playwright + Chrome, ผ่าน dev server จริง) — เพิ่ม protocol ที่ 2 (`UDP`) ให้ `GT06L` ชั่วคราวผ่าน `PATCH /device-models/{id}` จริง (Admin) เพื่อพิสูจน์ path ที่ 1 รุ่นรองรับหลาย protocol จริงๆ ไม่ใช่แค่ mock: เลือก `GT06L` แล้ว submit → field ใหม่ได้ `supportedModels` ครบ `GT06L/TCP` + `GT06L/UDP` ถูกต้อง (เห็นในคอลัมน์ "รุ่น/โปรโตคอลที่รองรับ" ของตาราง) ไม่มี console/page error — revert `GT06L` กลับเป็น `["TCP"]` ผ่าน API เดิมหลังทดสอบเสร็จ + ลบ field ทดสอบออกจาก DB แล้ว (5) `tsc --noEmit`/`eslint`/`next build` ผ่านหมด — ดู `web/src/lib/device-model-api.ts`, `web/src/app/(app)/parameters/parameter-create-form.tsx` |
| 2026-09-23 | paveekornk | แก้ครั้งที่ 49 — **ปิด issue #73 ข้อ 2/3 ที่เหลือ** (หมายเหตุเรื่องเลข: เดิมเขียนไว้ว่าเป็น "แก้ครั้งที่ 48" ในดราฟต์นี้ ชนกับ PR #217 (เอา protocol ออกจากฟอร์มสร้าง Parameter, issue #203) ที่เขียนตัวเองเป็นแก้ครั้งที่ 48 เช่นกัน — #217 merge เข้า main ก่อน (คงเลข 48 ของ PR นั้นไว้ตามที่ merge จริง) แถวนี้จึง renumber เป็น 49 ตอน sync branch นี้กับ main mirror วิธีที่แก้ครั้งที่ 30/34/40/47 เจอปัญหาเดียวกันมาก่อน) (ข้อ 1 GET /tasks scope ปิดไปแล้วตั้งแต่แก้ครั้งที่ 41) เจ้าของ `task` (A ถืออีก module ที่มี pattern เดียวกันคือ `config`/`device` อยู่แล้ว ตามที่ตกลงกับ kittiphong ไว้ใน #73): **(ข้อ 2 — PATCH /tasks/:id status transition)** เดิม backend รับ `status` ใดก็ได้จาก DTO (`IsIn(TASK_STATUSES)` ทั้ง 4 ค่า) แต่ Mobile จำกัด UI ให้ ST/OT เลือกได้แค่ `in_progress`/`completed` มาตั้งแต่ตอน implement (`_fieldStaffStatusChoices` ใน `task_detail_page.dart`, comment ในไฟล์นั้นชี้มาที่ issue #73 ตรงๆ) — client อื่นที่มี token ST/OT ยิงตรงเข้า `cancelled`/`pending` ได้ถ้าไม่เช็คที่ backend เพิ่ม `ST_OT_ALLOWED_STATUSES = ['in_progress', 'completed']` ใน `task.service.ts` เช็คก่อนถึง `updateMany` เสมอ (403 ถ้าไม่อยู่ในลิสต์) — เหตุผลที่ตัด `cancelled` ออก: ยกเลิกงานเป็นสิทธิ์ Operation ไม่ใช่ผู้ถูก assign ตัด `pending` ออก: เป็นสถานะเริ่มต้นที่ Operation กำหนดตอนสร้างงาน ไม่ใช่สิ่งที่ผู้ถูก assign ควรย้อนกลับไปเอง **(ข้อ 3 — GET /tasks/:id role check)** เดิม `isHiddenFromActor()` เช็คแค่ "ST/OT ที่ไม่ใช่เจ้าของงาน" ซ่อน ส่วนที่เหลือทั้งหมด **default-allow** (เห็นได้หมดไม่ว่า role นั้นจะมี use case จริงหรือไม่) — endpoint นี้ไม่มี `PermissionGuard` คุมเลย (แค่ `JwtAuthGuard`) การเช็คใน service จึงเป็นด่านเดียวที่กันได้ ต่างจาก `findAll()` ที่แก้ครั้งที่ 41 เปลี่ยนเป็น allowlist ไปแล้ว แต่ `findOne()` ไม่เคยแก้ตาม — เปลี่ยน `isHiddenFromActor()` ให้ mirror logic เดียวกับ `findAll()`: `UNSCOPED_TASK_ROLES` (Operation/Auditor/Admin/SuperAdmin) เห็นได้หมด, `SELF_SCOPED_ROLES` (ST/OT) เห็นเฉพาะงานตัวเอง, role อื่นที่ไม่อยู่ใน 2 list นี้เลย (เช่น ConfigEngineer หรือ role ใหม่ที่ได้ grant `tasks:Read` เพิ่มทีหลังโดยไม่ได้ตั้งใจ) **ถูกซ่อนเสมอ (default-deny)** — ตาราง 4.1 แถว `GET/PATCH /tasks/{taskId}` อัปเดตให้ตรงกับ logic ใหม่ทั้งคู่ · **ไม่แตะ** `openapi.yaml` (ไม่มี field/endpoint ใหม่ เป็นแค่ business logic เปลี่ยนภายใน ไม่กระทบ contract) · unit test เพิ่ม 8 เคสใน `task.service.spec.ts` (findOne: Auditor/Admin/SuperAdmin เห็นได้, ConfigEngineer 404 เสมอ · update: cancelled/pending ของ ST/OT เป็น 403, in_progress/completed ผ่าน) + integration test เพิ่ม 4 เคสผ่าน HTTP จริงใน `task-http.integration-spec.ts` (GET ConfigEngineer 404, GET Admin 200, PATCH cancelled/pending 403) — รัน unit (57 ผ่านทั้งไฟล์) + integration (32 ผ่านทั้งไฟล์) ครบ — ดู `backend/src/task/task.service.ts` |
| 2026-09-23 | paveekornk | แก้ครั้งที่ 50 — **ปิด issue #212: เพิ่ม smoke row ให้ `Device`/`DeviceModel` ใน CI** (พบระหว่างรีวิว PR #210 — ไม่ใช่ RBAC change แต่บันทึกไว้ตามธรรมเนียมไฟล์นี้เพราะแตะกลไก migration-safety เดียวกับที่เคยบันทึกไว้แก้ครั้งที่ 12/14) เจ้าของ `ci`/`device-model` (A): `.github/workflows/backend-ci.yml` มีกลไก smoke-test migration ที่แตะข้อมูลจริง (hold-back migration ใหม่ → deploy ของเดิม → insert smoke data จำลอง DB ที่มีข้อมูลอยู่แล้ว → deploy ของใหม่ทับ) แต่มีแค่ `User`/`Firmware` มาตั้งแต่ต้น ไม่เคยมี `Device` เลย — migration ในอนาคตที่แตะ `Device` (เช่นเดียวกับที่ #209 ทำกับ `modelId`) จะไม่เคยถูก smoke-test จริง เพิ่ม `INSERT INTO "DeviceModel"` ก่อน (ต้องมาก่อนเพราะ FK) แล้วค่อย `INSERT INTO "Device"` อ้าง `modelId` นั้น ตรงกับโครงตารางปัจจุบัน (หลัง #209: `modelId` เป็น NOT NULL FK ไป `DeviceModel`) — ทดสอบ SQL จริงกับ test DB ก่อน commit (insert จริง + query ยืนยัน relation ถูกต้องผ่าน Prisma + ลบออก) ยืนยันว่า syntax/FK/NOT NULL ผ่านหมด **หมายเหตุ:** PR นี้เองไม่มี migration ใหม่ จึงไม่ได้ผ่าน code path ที่เพิ่มนี้จริงใน CI ของตัวเอง (`NEW_MIGRATIONS` ว่างเปล่า → deploy ปกติ) — ค่าของ fix นี้คือเตรียมไว้ให้ PR ในอนาคตที่มี migration แตะ `Device` — ดู `.github/workflows/backend-ci.yml` |
| 2026-09-23 | paveekornk | แก้ครั้งที่ 51 — **Confirm Firmware Install** (issue #181 — ปิด Section 6 gap "device-status" บางส่วนที่เปิดค้างไว้ตั้งแต่แก้ครั้งที่ 35, มติ A/B ยืนยันในเธรด issue) เจ้าของ `device`/`firmware` (A): (1) `POST /devices/{deviceId}/confirm-firmware-install` ใหม่ — ช่างหน้างาน (ST/OT) ยืนยันเองว่าติดตั้ง Firmware เข้าอุปกรณ์เสร็จแล้วจริง **ไม่ใช่คำสั่งส่งอะไรไปอุปกรณ์เลย** ต่างจาก `applyConfigToDevice` ที่ยังมี mock fire-and-forget อยู่เบื้องหลัง — endpoint นี้เป็นแค่การบันทึก "มีการยืนยันเกิดขึ้น" ผ่าน AuditLog เท่านั้น ไม่มี mock ฝั่ง push firmware ให้ fire-and-forget ด้วยซ้ำ (2) เงื่อนไข 4xx (mirror `applyConfig`): ไม่พบ Device/Firmware → 404 · Device ไม่ใช่ `installed` → 409 · Firmware `uploadStatus` ไม่ใช่ `stored` หรือ `approvalStatus` ไม่ใช่ `approved` → 409 (reuse constant `SIMULATABLE_FIRMWARE_STATUS`/`CAMPAIGN_ELIGIBLE_FIRMWARE_APPROVAL_STATUS` จาก `firmware/firmware-status.ts` ตรงตาม pattern ที่ `campaign.service.ts` import ข้ามโมดูลอยู่แล้ว ไม่ประดิษฐ์ threshold ใหม่) · Firmware ไม่รองรับ `device.deviceModel` (`deviceModelCompatibility.includes`) → 409 (3) **ไม่ wrap try/catch เหมือน `applyConfig`** — ตัดสินใจต่างจาก apply-config โดยตั้งใจ: ที่นั่น AuditLog เป็นแค่ร่องรอยของการกระทำจริงที่สำเร็จไปแล้ว (fire-and-forget, never-throw กันไม่ให้ audit ล้มเหลวทำให้เห็น 500 ทั้งที่ผลจริงสำเร็จ) แต่ที่นี่การเขียน AuditLog **คือ** การกระทำทั้งหมด ไม่มีอย่างอื่นเกิดขึ้นเลย — ถ้าเขียนไม่สำเร็จต้อง throw 500 จริงให้ช่างรู้ว่าต้องกดยืนยันใหม่ (4) RBAC: resource ใหม่ `device-firmware-confirm` action **`Create`** (ไม่ใช่ `Read` เหมือน `device-config-apply`/`device-connection-test`/`simulate-config` — endpoint พวกนั้นไม่ persist อะไรเลยจึงใช้ Read ได้ แต่นี่เขียน AuditLog จริง) grant ST/OT เท่านั้น (mirror `device-config-apply`) (5) `AuditLogMetadata` (issue #205) เพิ่ม `firmwareId?`/`firmwareVersion?` — ไม่มีข้อมูลอ่อนไหว ไม่ต้องระวังเหมือน `fieldNames` (6) **Deviation จาก `GPS_Config_Firmware_Center_Design.pdf` §10.2-10.3 โดยตั้งใจ** (บันทึกไว้ตาม CLAUDE.md หัวข้อ Deviation) — PDF ออกแบบ Workflow Firmware เป็น state machine อัตโนมัติเต็มรูปแบบ 10 สถานะ (Pending→Pre-checking→Downloading→Verifying→Installing→Rebooting→Health Checking→Success/Rollback) ที่ backend คุยกับอุปกรณ์เองทุกขั้น — ตรวจสอบแล้วว่า PDF **ไม่มีแนวคิด "ช่างยืนยันเอง" อยู่เลยแม้แต่จุดเดียว** (ไม่มีคำว่า manual/technician/ช่างยืนยัน) เป็น placeholder ที่คิดขึ้นใหม่ทั้งหมดเพื่อแก้ปัญหาขอบเขตฝึกงาน เพราะระบบเป็น PULL model ทำ state machine อัตโนมัติแบบ PDF ไม่ได้จนกว่า production channel ของระบบเดิมจะเปิดทาง (blocker เดียวกับ config-sync-writer) — comment เหนือ `DeviceService.confirmFirmwareInstall()` อ้างอิง PDF §10.2-10.3 ตรงๆ (7) **open question ที่ตั้งใจไม่แก้รอบนี้** (mirror `ApplyConfigDto.configId` ที่เจอปัญหาเดิม): client เป็นคนส่ง `firmwareId` มาเอง ไม่มี FK เชื่อม Task/Campaign→Firmware ให้ derive อัตโนมัติได้ — ตั้งใจไม่ผูกกับ Campaign เพราะ Campaign เป็นแค่ tracking/maintenance ไม่ใช่เครื่องมือมอบหมายงานให้ช่าง (มติ 2026-09-14) ช่างต้องเลือกจากรายการ `GET /firmware` เอง — รอ B ตัดสินใจ UX ตอนออกแบบ Mobile (8) `docs/api/openapi.yaml` (v3.29) — tag `device` เพิ่ม path ใหม่ + schema `ConfirmFirmwareInstallInput`/`ConfirmFirmwareInstallResult` · `redocly lint` ผ่าน (2 warning เดิมยอมได้) (9) unit test เพิ่ม 8 เคสใน `device.service.spec.ts` (สำเร็จ+AuditLog ครบ, audit เขียนไม่สำเร็จ->throw ต่อ (ไม่ใช่ never-throw), ครบทุก 404/409) + integration test เพิ่ม 11 เคสผ่าน HTTP จริงใน `device-http.integration-spec.ts` (401/403/400/ครบทุก 404/409/200 + AuditLog แถวจริงใน DB) — รัน backend unit (425 ผ่าน) + integration (310 ผ่าน) ครบ, `npm run lint`/`npm run build` ผ่านหมด **ยังไม่ทำ (ตามมติข้อ 3 ของ issue, เจตนาจำกัด scope):** ไม่คำนวณ `Firmware.deviceUpdateStatus` จริง (ยังคงเป็น `unknown` เสมอ), ไม่มี schema/migration ใหม่, Mobile UI (ของ B) — ดู `backend/src/device/device.service.ts`, `backend/src/device/dto/confirm-firmware-install.dto.ts` |
| 2026-09-24 | kittiphong | แก้ครั้งที่ 52 — **implement Config Override Phase 2 (Mobile, issue #211)** (หมายเหตุเรื่องเลข: เดิมเขียนไว้ว่าเป็น "แก้ครั้งที่ 51" ในดราฟต์นี้ ชนกับ PR #220 (Confirm Firmware Install, issue #181) ที่ merge เข้า main ก่อนจริงและใช้เลข 51 ไปแล้ว — renumber เป็น 52 ตอน sync branch นี้กับ main mirror วิธีที่แก้ครั้งที่ 30/34/40/47/49 เจอปัญหาเดียวกันมาก่อน — `openapi.yaml` เจอปัญหาเดียวกัน: เดิมเขียน `v3.29` ชนกับ endpoint `confirmFirmwareInstall` ของ PR #220 ที่ใช้เลขนี้ไปก่อนจริง renumber เป็น `v3.30`) เจ้าของ `device`/mobile `config_override` (B ตัดสินใจร่วมกับ A เรื่อง scope backend endpoint — ตกลงกันไว้ใน #211): **(1) Backend — endpoint ใหม่** `GET /devices/{deviceId}/config` (`getDeviceCurrentConfig`) resource ใหม่ `device-current-config` action `Read` (ST/OT — mirror `device-config-apply`/`device-connection-test`, แยก resource เพราะคนละความหมาย: ตัวนี้ = "อ่าน Config ปัจจุบันของอุปกรณ์" ไม่ใช่ "ส่ง Config เข้าอุปกรณ์") — `DeviceService.getCurrentConfig()` derive จาก `Task.configId` ของ Task ล่าสุด (`updatedAt` มากสุด) ที่ `status: completed` และ `deviceId` ตรงกับอุปกรณ์ (`Task.deviceId` เทียบตรงกับ `Device.deviceId` ตาม convention ที่ยืนยันไว้ใน `create-campaign.dto.ts` — ไม่ normalize UUID เหมือนที่ mobile client ทำกับข้อมูลเก่า) ไม่พบ Task completed ที่ผูก configId เลย → 404 "อุปกรณ์นี้ยังไม่มี Config ที่ยืนยันติดตั้งแล้ว" · **soft-delete guard เพิ่มระหว่างรีวิว (comment A บน PR #222)**: Config ที่ `deletedAt != null` (docs/11 Part A) ก็ถือว่าไม่พบเช่นกัน (mirror `ConfigService.findOne()`) กัน endpoint นี้คืน 200 ให้ Config ที่ถูกลบไปแล้ว response reuse schema `DeviceConfigDraft` เดิม (เหมือน `getConfig`) `openapi.yaml` v3.30 + seed grant ใหม่ 2 แถว + unit test 6 เคส (`device.service.spec.ts`, รวมเคส soft-deleted -> 404) + integration test 6 เคสผ่าน HTTP จริง (`device-http.integration-spec.ts`) **(2) Mobile — หน้า Config Override ใหม่** `mobile/lib/features/config_override/` (mirror โครง `config_simulator/`): `config_override_repository.dart` (Real เรียก `getDeviceCurrentConfig` + `overrideConfig` ที่มีอยู่แล้ว, Mock คู่กัน), `ConfigFieldDefinition` model ใหม่ใน `core/api/models.dart` (mirror `web/src/lib/config-definition-api.ts` เป็น dart) + `ApiClient.listConfigDefinitions()`/`getDeviceCurrentConfig()`/`overrideConfig()` ใหม่ 3 method, `config_override_page.dart` mirror UX ของ `config-override-panel.tsx` ทุกจุด (แสดงทุก field, `stOverridable:true` เท่านั้นแก้ได้ตาม dataType, field แก้ไม่ได้แสดง read-only + label, เหตุผลบังคับกรอก, validate มีอย่างน้อย 1 field เปลี่ยนค่าก่อน submit, ไม่มี confirm dialog เพิ่ม, error + errorList จาก API) — **ข้อความเตือนเพิ่มระหว่างรีวิว (comment A บน PR #222 — แก้ 2 รอบ)**: override แก้ `Config.fields` ทั้งชุด ไม่ใช่แค่เครื่องที่เปิดหน้าอยู่ และ**ไม่ได้ apply เข้าอุปกรณ์ทันที** (`ConfigOverrideService.override()` แค่แก้ DB + สร้าง `ConfigVersion` + เขียน `AuditLog` ไม่เรียก `enqueueConfigSync()`) แสดงชื่อ Config (`config.name`) พร้อมข้อความอธิบายผลกระทบ — ข้อความนี้ผูกกับ issue #223 (design แก้ Config ทั้งชุด vs รายเครื่อง) เป็นการชั่วคราว รอผลตัดสินใจ — entry point ปุ่มใหม่ใน `device_detail_page.dart` เห็นเฉพาะ role ST (`AuthState.role == UserRole.st` — mirror `canOverrideConfig` ฝั่ง Web) route ใหม่ `AppRoutes.deviceConfigOverride` (`/devices/:deviceId/config-override`) ใน `app_router.dart` — comment เดิมในไฟล์นี้ที่บอกว่า "config/firmware sync status is a separate, not-yet-built endpoint" อัปเดตให้ตรงกับสถานะจริงแล้ว **(3) `ApiException` (mobile)** เพิ่ม field `details` (`List<String>`) parse จาก response body key `errors` (mirror `ApiError.details` ฝั่ง Web `api.ts`) — ของเดิมอ่านแค่ `message` เฉยๆ ไม่เคยเก็บ error list ย่อยเลย ต้องเพิ่มเพราะหน้า Override ต้องโชว์ errorList จาก `validateOverridableFields` **(4) เทส** widget test หน้าใหม่ (mirror `device_connection_test_page_test.dart`) + repository unit test + `flutter analyze`/`dart format`/backend lint+build ผ่านหมด — Section 2 footnote ⁴ ปิด "Phase 2 ยังไม่เริ่ม" แล้ว — ดู `backend/src/device/device.service.ts`, `mobile/lib/features/config_override/` |
| 2026-09-24 | kittiphong | แก้ครั้งที่ 53 — **implement Per-device Config Override (issue #223)** เจ้าของ `device`/mobile `config_override` (B ตัดสินใจร่วมกับ A: เพิ่ม mechanism ใหม่คู่ขนานกับของเดิม ไม่แก้/ไม่ลบ `overrideConfig` เดิมของ Web — ให้ A ตัดสินใจเองในคอมเมนต์ PR ว่าจะรวม/deprecate ของเดิมทีหลังไหม เพราะกระทบ Web โดยตรง): **(1) `schema.prisma`** — model ใหม่ `DeviceConfigOverride` (mirror `ConfigVersion` เกือบทั้งหมด) `id`/`deviceId` (**FK จริงไป `Device.deviceId`** — ตั้งใจต่างจาก `Task.deviceId`/`CampaignTarget.deviceId` เดิมที่เป็น string หลวมๆ ไม่มี FK เพราะเป็นตารางใหม่ไม่มี legacy data ต้อง normalize)/`configId` (FK ไป `Config.id`, `onDelete: Restrict` mirror `ConfigVersion`, denormalized ไว้ audit ว่า override ทับ Config ตัวไหน)/`versionNumber` (running ต่อ**อุปกรณ์** ไม่ใช่ต่อ Config, `@@unique([deviceId, versionNumber])`)/`fields` (Json)/`reason`/`overriddenBy` (FK User)/`overriddenAt` — migration additive ล้วน `20260924043159_add_device_config_override` (ตารางใหม่ ไม่กระทบตารางเดิม) **(2) Backend — endpoint ใหม่** `POST /devices/{deviceId}/config-override` (`overrideDeviceConfig`) resource ใหม่ `device-config-override` action `Override` (**ST เท่านั้น** mirror `config-override` เดิม, seed.ts) — base Config มาจาก `DeviceService.getBaseConfigForDevice()` (extract ออกมาจาก `getCurrentConfig()` เดิม ใช้ร่วมกัน 2 endpoint ไม่เขียนซ้ำ) reuse `ConfigDefinitionService.validateOverridableFields()` ตัวเดียวกับ `config-override` เดิมเป๊ะๆ (inject `ConfigDefinitionModule` เข้า `DeviceModule` ใหม่) — **`fields` ที่เขียนลง DB เป็นสถานะ override สะสม** (merge `dto.fields` ทับแถวล่าสุดก่อนหน้า ถ้ามี ก่อนเขียนแถวใหม่ mirror ที่ `ConfigOverrideService.override()` merge ทับ `Config.fields` เต็มก้อนก่อนสร้าง `ConfigVersion`) — **เหตุผล**: `getCurrentConfig()` merge แค่แถวล่าสุดแถวเดียวทับ base (ไม่ไล่รวมทุก version) ถ้าเก็บแค่ delta ดิบๆ ของแต่ละรอบ การ override field ใหม่รอบถัดไปจะ "ลบ" ผล override ของ field อื่นจากรอบก่อนหน้าไปเงียบๆ โดยไม่ตั้งใจ — AuditLog บังคับทุกครั้งในทรานแซกชันเดียวกัน (ไม่ใช่ never-throws) mirror `config-override` เดิม **(3) แก้ `GET /devices/{deviceId}/config` เดิม (จาก #211/#222)** — เพิ่ม logic: query `DeviceConfigOverride` ล่าสุดของอุปกรณ์ (`orderBy versionNumber desc, take 1`) ถ้ามี merge `fields` ทับ base ก่อนคืนกลับ (shallow merge, override ทับ) เพิ่ม field ใหม่ `hasDeviceOverride: boolean` ใน response — เปลี่ยน return type จาก `Config` เป็น `ConfigWithDeviceOverride` (`Config & { hasDeviceOverride }`) ทั้ง `getCurrentConfig()`/`overrideDeviceConfig()` **(4) `docs/api/openapi.yaml`** (v3.31 — v3.30 ชนกับ `getDeviceCurrentConfig` ของแก้ครั้งที่ 52 ไปแล้ว) schema ใหม่ `DeviceCurrentConfig` (`allOf` `DeviceConfigDraft` + `hasDeviceOverride`) ใช้เป็น response ทั้ง `GET /devices/{deviceId}/config` (แก้ให้ชี้ schema ใหม่) และ path ใหม่ `POST /devices/{deviceId}/config-override` · `redocly lint` ผ่าน (2 warning เดิมยอมได้) ตาราง 4.1 เพิ่มแถวใหม่ + แก้แถว `overrideConfig`/`getDeviceCurrentConfig` เดิมให้ note ว่ามี mechanism คู่กันแล้ว footnote ⁴ เพิ่มคำอธิบายมติ #223 **(5) Mobile** `ApiClient` เปลี่ยน `overrideConfig(configId,...)` (`POST /config/{configId}/override` เดิม) เป็น `overrideDeviceConfig(deviceId,...)` (`POST /devices/{deviceId}/config-override` ใหม่) — **แก้ endpoint ที่เรียกจริง ไม่ใช่เพิ่มเมธอดคู่ขนาน** (ไม่มี call site อื่นเรียก method เดิมอยู่แล้ว ตรวจแล้วก่อนลบ) `DeviceConfigDraft` เพิ่ม field `hasDeviceOverride` (default `false` — endpoint อื่นที่ไม่มี key นี้ใน response ยังพาร์สได้ปกติ) `ConfigOverrideRepository.overrideConfig()` เปลี่ยน param จาก `configId` เป็น `deviceId` ทั้ง interface/Real/Mock impl `config_override_page.dart`: เอา null-check ของ `config.id` ออก (ไม่จำเป็นอีกต่อไป — เรียกด้วย `widget.deviceId` ที่ไม่มีทาง null ได้อยู่แล้ว) เพิ่ม badge `_DeviceOverrideBadge` แสดงเมื่อ `config.hasDeviceOverride == true` แก้ข้อความเตือน + comment เหนือข้อความที่ผูกกับ #223 จาก PR #222 (ตอนนั้นบอกว่า "ชั่วคราว รอผลตัดสินใจ") ให้สะท้อนมติจริง: override เป็น**รายเครื่อง**แล้ว ไม่กระทบอุปกรณ์อื่น **(6) เทส** backend unit เพิ่มใน `device.service.spec.ts` (merge logic ใน `getCurrentConfig`, `overrideDeviceConfig` ครบ: สร้างสำเร็จ/สะสม version/AuditLog/validation reject/404 ครบ) + integration เพิ่ม 12 เคสใหม่ใน `device-http.integration-spec.ts` (401/403 OT/403 ConfigEngineer/404 ทั้งคู่/400 field ไม่ overridable/400 ไม่ส่ง reason/200 สำเร็จ/สะสม 2 รอบ/**อุปกรณ์อื่นที่ใช้ Config เดียวกันไม่เห็น override ของกันและกัน — พิสูจน์ per-device จริง**) — รัน backend unit (439 ผ่าน) + integration (327 ผ่าน) ครบ, `tsc --noEmit`/`eslint`/`nest build` ผ่านหมด · mobile widget/repository test อัปเดต + เพิ่มเคส badge (313 ผ่านทั้งชุด), `flutter analyze` ไม่มี issue, `dart format` ผ่าน — ดู `backend/prisma/schema.prisma` (`model DeviceConfigOverride`), `backend/src/device/device.service.ts`, `mobile/lib/features/config_override/` |
